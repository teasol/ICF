# One-axis difficulty ablation: only latent class separation is restored to
# the historical Medium range. Everything else is the promoted v76 setting.
base_config: train_v76_cv_learnable_p_dd_ct_mlp_1pop_linear_1536.yaml

data_overrides:
  dataset_kwargs:
    class_separation: [0.5, 1.4]

logger_overrides:
  experiment_name: v76_axis_classsep_medium
