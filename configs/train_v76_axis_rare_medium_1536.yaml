# One-axis difficulty ablation: enable historical Medium rare responses only.
base_config: train_v76_cv_learnable_p_dd_ct_mlp_1pop_linear_1536.yaml

data_overrides:
  dataset_kwargs:
    rare_response_probability: 0.15
    rare_response_fraction: [0.02, 0.08]

logger_overrides:
  experiment_name: v76_axis_rare_medium
