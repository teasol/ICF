# Hard synthetic data with a finite bank of fixed nonlinear manifold maps.
base_config: train_v76_classsep_hard_1536.yaml

data_overrides:
  dataset_kwargs:
    manifold_mode: mlp_bank
    manifold_bank_size: 128
    mlp_num_layers: 3

logger_overrides:
  experiment_name: v76_hard_mlpbank128
