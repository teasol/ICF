# 50% fresh orthogonal linear manifolds + 50% fixed 3-layer MLP bank members.
base_config: train_v76_classsep_hard_1536.yaml

data_overrides:
  dataset_kwargs:
    manifold_mode: mixed_linear_mlp_bank
    manifold_bank_size: 1024
    manifold_linear_probability: 0.5
    mlp_num_layers: 3

logger_overrides:
  experiment_name: v76_hard_mixed_linear_mlpbank1024
