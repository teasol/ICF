seed: 42
ckpt_path:

data: minimum
model: default
optimizer: adamw
scheduler: warmup_reduce_on_plateau
trainer: minimum_ddp8
logger: wandb
callbacks: default

model_kwargs:
  training_targets_per_episode: [4, 8]
  ranking_loss_weight: 0.10
  routing_sparsity_weight: 0.0
  routing_balance_weight: 0.01
