# Claude Code Project Guidelines

@AGENTS.md

## Claude Code Specific Shortcuts
- Use `/handoff-update` to trigger the handoff update skill.
- Use `/resume-handoff` to catch up and resume the session.
