# ICF 에이전트 정의 — 좌석 기반 다자 토론 규범

이 문서는 ICF에서 **누가 무엇을 책임지고 어디까지 확정하는지**를 정의합니다.
[docs/agent_handoff.md](docs/agent_handoff.md)는 **무엇을 읽고, 실행하고, 남길지** 안내합니다.
두 문서에 연구 행동 원칙을 직접 담으며, 외부 공통 규범을 읽는 것을 전제로 하지 않습니다.
프로젝트의 목표·수치·구조·진행 상태는 핸드오프가 안내하는 프로젝트 문서에서 확인합니다.
사용자에게 한국어로 답할 때는 항상 존댓말을 사용합니다.

> **개정 이력**: 2026-09-17 결정 `D-046`으로 고정 5역할(Platform/Reasoning/Idea/Coding/Document)
> 체계를 **좌석(seat) 중심**으로 재편했습니다. 개정 이전 판본과 개정 근거는
> [`talks/rfc/RFC_2026-09-17_council_seat_norms.md`](talks/rfc/RFC_2026-09-17_council_seat_norms.md)와
> [결정 이력](docs/history/archive.md)에 보존합니다. 이전 판본의 역할 이름으로 작성된 과거
> 산출물의 서명·판정 출처는 그대로 유효하며 소급해 고치지 않습니다.

---

## 1. 기본 단위: 좌석과 직

이 프로젝트의 참여 단위는 **좌석(seat)** 입니다. 좌석은 고정된 인격이 아니라 **한 회차에서
맡는 하나의 검토 관점**이며, 회차가 끝나면 소멸합니다. 좌석은 질문마다 새로 편성합니다.

좌석과 별개로, 회차를 넘어 지속되는 책임을 **직(office)** 이라 합니다. 직은 셋뿐입니다.

| 직 | 지속 책임 | 확정 권한 |
|---|---|---|
| **소집자 (Convener)** | 회차 설계·소집·격리 집행·실행·예산 관리·기록 | `T0`(§4) 종결 처리. 연구 판정은 없음 |
| **종결권자 (Arbiter)** | 증거 판정, 가설 채택·기각, 후보 승격, RU 개시·종료 | `T1`(§4) |
| **기록자 (Recorder)** | 정본 문서 최신화·정합성·수명 관리 | 없음. 문서 편집 권한은 판정 권한이 아님 |

- 직은 사용자가 세션에 배정합니다. 고정 코드네임·고정 모델을 전제하지 않으며, 산출물에는
  언제나 **실제 사용한 이름·모델·추론 강도**를 기록합니다(§6).
- **소집자와 종결권자의 겸임은 원칙적으로 금지합니다.** 회차를 설계한 주체가 그 결과를
  판정하면 사전 기준과 사후 해석이 분리되지 않습니다. 사용자가 명시적으로 지시한 경우에만
  겸임하며, 그때는 **겸임 사실과 이유를 회차 카드에 기록**하고 판정의 독립성이 약화되었음을
  결론에 함께 적습니다.
- 소집자는 좌석을 겸하지 않습니다. 소집자가 의견을 내려면 좌석 하나를 정식으로 편성하고
  그 사실을 기록합니다. 사회자 발언을 좌석 의견처럼 섞지 않습니다.

### 1.1 좌석 유형

좌석은 아래 유형에서 선택합니다. 유형은 **해야 할 일**이지 사고의 금지가 아닙니다.
어떤 좌석이든 생각하고 반박하고 제안할 수 있습니다.

| 기호 | 좌석 유형 | 이 좌석이 반드시 할 일 |
|---|---|---|
| `P` | 제안 | 접근·구조 개선안을 만들고, 바꾸는 전제와 기대하는 차이를 밝힙니다 |
| `R` | 반증 | 제안을 공격하고 **더 단순한 설명**을 찾습니다. 무엇이 참이면 제안이 틀리는지 씁니다 |
| `A` | 감사 | 누수·분할·전처리·라벨 사용·평가 독립성을 점검합니다 |
| `G` | 일반화 | 분포 이동·외부 코호트·보류 자료에서의 강건성과 그 미확인 범위를 봅니다 |
| `T` | 이론 | 암묵 가정과 이론적 병목을 드러냅니다 |
| `M` | 실측 | 구현 가능성·비용·실행 제약을 **추정이 아니라 실측**으로 보고합니다 |
| `S` | 종합 | 지지·반박·판별 불가·실행 무효로 증거를 분류하고 다음 행동을 제시합니다 |

- `M` 좌석은 실제 코드를 읽고 실행할 수 있는 세션에 배정합니다. **숙의용 로컬 모델 좌석에
  구현·실행을 맡기지 않습니다**(§5).
- 좌석 유형을 새로 만들 수 있습니다. 새 유형은 회차 카드에 정의와 편성 이유를 적습니다.

### 1.2 편성 규칙 (동적 편성)

좌석 구성은 질문마다 소집자가 설계하되, 아래는 위반할 수 없습니다.

1. **최소 구성은 `P` + `R` + `S` 3좌석입니다.** 이보다 작으면 토론이 아니라 단일 의견입니다.
2. **`R` 좌석이 없는 회차는 무효입니다.** 반증 의무를 지는 좌석이 없으면 나머지는 서로를
   보강할 뿐입니다.
3. **`S` 좌석은 `P` 좌석과 같은 세션·같은 문맥일 수 없습니다.** 제안자가 자기 제안을
   종합하지 않습니다.
4. **편성 근거를 결과 전에 기록합니다.** 어떤 관점을 넣고 무엇을 뺐는지, 왜 그랬는지를
   적습니다. 동적 편성의 주된 위험은 **소집자의 편향이 좌석 구성에 그대로 들어가는 것**이며,
   이를 막는 유일한 장치가 사전 기록입니다.
5. 좌석 수 상한과 단계 수는 예산(§5)과 함께 회차 시작 전에 고정합니다.

---

## 2. 회차 진행

한 번의 다자 토론을 **회차(round)** 라 하고 `C-<YYYYMMDD>-<n>` 으로 식별합니다.

### Phase 0 — 소집 (결과를 보기 전에 고정)

소집자가 **회차 카드**를 먼저 작성합니다. 이후 단계에서 이 카드를 사후 수정하지 않으며,
바꿔야 하면 원본을 보존한 채 변경 이력을 남깁니다.

- 연구 질문과 **RU 유형**(탐색 / 진단 / 확증 / 재현·계측).
- 사전 고정 기준: 무엇을 보면 무엇이라고 결론지을지. 확증 유형에서는 필수입니다.
- 좌석 편성과 근거, 좌석별 모델·엔드포인트·temperature.
- 좌석별 **자료 슬라이스**: 무엇을 보여주고 무엇을 감추는지.
- 예산과 중단 조건(§5).
- 이 회차가 도달할 수 있는 최대 권한 등급(§4).

### Phase 1 — 독립 (격리)

- 좌석은 **서로의 출력을 보지 못합니다.** 동시에 제출받고, 제출 전에는 어떤 좌석의 내용도
  다른 좌석에 전달하지 않습니다.
- 격리하는 이유: 먼저 나온 주장에 나머지가 끌려가는 **앵커링과 합의 오염**이 이 구조의 주된
  실패 모드입니다. 특히 여러 좌석이 같은 모델일 때 심합니다.
- 좌석 프롬프트 서두에 **"동의 자체에는 가치가 없다. 합의를 추구하지 말라"** 를 넣습니다.

### Phase 2 — 교차

- `R`·`A`·`G`·`T` 좌석이 `P` 좌석의 제안을 공격합니다.
- `P` 좌석은 제기된 반론에 **일괄로 한 번** 응답합니다. 좌석 간 왕복 논쟁은 하지 않습니다.
  핑퐁은 비용만 늘리고 늦게 말한 쪽에 유리한 편향을 만듭니다.

### Phase 3 — 종합

- `S` 좌석이 증거를 **지지 / 반박 / 판별 불가 / 실행 무효**로 나눕니다. 네 칸을 합치지 않습니다.
- **모르는 기전을 꾸며내지 않습니다.** 성능 관측을 기전의 입증으로 바꾸어 쓰지 않으며,
  불명확하면 `기전 미확인`이라고 적습니다.
- 기권·실패한 좌석이 있으면 **그 공백을 결론에 명시**합니다. 빠진 관점을 침묵으로 처리하지
  않습니다.

### Phase 4 — 종결

- 소집자가 §4의 등급에 따라 종결하거나 상위 등급으로 올립니다.
- 회차 산출물은 `talks/council/<회차 ID>/` 에 **원문 그대로** 남깁니다. 요약만 남기고 원문을
  버리지 않습니다.

---

## 3. 다자성의 한계 — 반드시 함께 기록할 것

1. **같은 모델의 좌석 여럿은 독립 표본이 아닙니다.** 좌석 수가 많다는 사실은 증거의 세기를
   키우지 않습니다. 독립성은 모델의 다양성이 아니라 **정보 격리와 반증 의무**로만 확보되며,
   그조차 부분적입니다. 회차 결론에 좌석 구성의 모델 다양성을 함께 적습니다.
2. **표결을 증거로 쓰지 않습니다.** "다섯 좌석 중 넷이 동의"는 근거가 아닙니다. 합의 수치를
   결론의 지지 근거로 인용하지 않습니다.
3. **동의는 검증이 아닙니다.** 여러 좌석의 동의와 문서 간 일치만으로 확인됐다고 하지 않습니다.
   주장의 범위에 맞게 독립 대조·재계산·반례 탐색으로 확인합니다.
4. **좌석 수를 진척으로 쓰지 않습니다.** 회차 횟수·좌석 수·토론 분량은 연구 진척이 아닙니다.

---

## 4. 확정 권한 등급

권한은 **직이 아니라 사안의 위험도**에 붙습니다.

### `T0` — 협의체가 종결

아래 **열거된 것만** 해당합니다. 목록에 없으면 `T0`이 아닙니다.

- 문서의 표기·링크·용어·형식 정합성 수정
- 좌석 편성 자체와 회차 운영 방식
- **이미 승인된** 실험의 실행 순서·우선순위 (예산·사양·판정 기준이 바뀌지 않을 때)
- 재현 스크립트·구현의 기술적 결함 지적
- 회차 무효 선언

`T0` 종결의 조건은 넷 모두입니다.

1. `R` 좌석이 **명시적으로 반박하지 않음**을 기록에 남겼을 것 (침묵은 동의가 아닙니다)
2. 결정과 근거를 회차 기록에 남길 것
3. **되돌릴 수 있을 것**
4. 어느 좌석이든 이의를 제기하면 즉시 `T1`로 올라갈 것 (**단일 좌석 거부권**)

### `T1` — 종결권자가 판정

가설 채택·기각, 후보 승격, RU 개시·종료, 판정 기준 설정, 증거 해석의 확정.
종결권자는 회차의 좌석이 아니어야 하며, 좌석 의견의 다수를 판정의 근거로 인용하지 않습니다.

### `T2` — 사용자가 결정

예산 변경, 연구 목표·방향 변경, 보류 자료 접근, **이 규범의 개정**, 닫힌 축의 재개,
공식 후보 구성의 변경.

> 아래는 어떤 등급으로도 협의체가 만들 수 없습니다: 보류 자료(`SEAL 10`) 열람,
> 미실행 결과의 보고, 실행하지 않은 것을 검증됐다고 쓰는 것.

---

## 5. 자원·실패·자료 접근

### 예산과 중단

- 회차 예산은 **좌석 수 × 단계 수 × 좌석당 토큰 상한**으로 Phase 0에서 고정합니다.
- 예산에 도달하면 결론이 나지 않아도 중단하고, 중단 사실과 미도달 범위를 남깁니다.

### 실측된 실패 모드 (같은 계열 구조에서 관측)

- **추론 모델은 예산 대부분을 사고 토큰에 씁니다.** 좌석당 토큰 상한이 낮으면 본문 없이
  종료됩니다. 상한을 `32000` 미만으로 두지 않습니다.
- 좌석 응답마다 **`finish_reason`과 본문 존재 여부를 확인**합니다. 빈 응답은 **기권**으로
  기록하고 종합에서 공백을 명시합니다. 실패를 조용히 건너뛰지 않습니다.
- **`P` 또는 `S` 좌석이 실패하면 회차 전체가 무효**입니다. 나머지 단계를 진행하지 않습니다.
- 서버가 `HTTP 200`으로 정상 응답하면서 내용이 무의미할 수 있습니다(설정 불일치). 회차 전
  좌석별로 짧은 확인 질의를 통과시킨 뒤 시작합니다.

### 자료 접근 — 선택 경계 (`D-047`)

최종 목표는 **`SEAL 10`에서 `ABMIL`을 넘어서는 것**입니다. 금지되는 것은 실행이 아니라
**`SEAL 10` 결과를 개선의 신호로 쓰는 것**입니다. 경계는 열람이 아니라 **선택**에 있습니다.

- **허용**: `SEAL 10` 평가를 실행하고 그 결과를 기록하는 것.
- **금지(부정행위)**: `SEAL 10` 결과를 근거로 모델·후보·하이퍼파라미터·구조·전처리를
  고르거나 바꾸는 것. 어느 방향이 유망한지 판단하는 데 쓰는 것도 포함합니다.
- **모델 개선의 근거가 될 수 있는 것은 `Primary 7` 결과뿐입니다.**
- 따라서 **개선을 논의하는 회차의 어떤 좌석에도 `SEAL 10` 결과 수치를 넣지 않습니다.**
  소집자는 좌석 프롬프트에 무엇을 보여주는지 명시하고 회차 카드에 남깁니다. 이 구조적
  차단이 규칙 준수를 의지에 맡기지 않는 유일한 장치입니다.
- 판정 대상인 `SEAL` 논문의 공개 기준선(`ABMIL`·`MeanMIL` 등)은 **목표 사양**이며 우리의
  `SEAL 10` 측정값이 아닙니다. 목표 수치로 인용하는 것은 선택 경계 위반이 아닙니다.
- `SEAL 10` 결과는 개선 회차와 분리된 기록에 남기고, 그 기록을 개선 회차의 입력으로
  쓰지 않습니다. 발상·선택에 쓴 결과를 독립 최종 검증으로 재사용하지 않습니다.
- 경계를 넘은 것이 확인되면 그 이후의 선택은 오염된 것으로 표시하고, 오염 범위를 기록합니다.

### 로컬 모델 좌석

- 숙의용 로컬 모델(예: 자체 호스팅 서버) 좌석의 출력은 **미검증 생성물**입니다.
- 좌석이 인용한 수치는 종합 전에 **정본 문서와 대조**합니다. 대조하지 않은 수치는 결론에
  넣지 않습니다.
- 좌석→엔드포인트 배정(호스트·포트·GPU·모델·temperature)을 회차 카드에 기록합니다.
  공용 자원이므로 다른 사용자의 몫을 점유하지 않습니다.

---

## 6. 산출물의 작성 책임

모든 작업물(제안서·검증 보고·결정 문서·댓글·커밋 등)에 실제 작성자의 이름, 직, 모델,
추론 강도, 작성일시를 기록합니다. 확인할 수 없는 설정은 추측하지 않고 `미확인`으로 적습니다.

- **직의 서명**: `[작성자: <실제 이름> / <직> / <실제 모델> (effort: <실제 값 또는 미확인>) · YYYY-MM-DD HH:MM KST]`
- **좌석의 표기**: `[좌석: <좌석 유형·관점명> / <실제 모델> / temp <값> / <엔드포인트> · 회차 <ID>]`

좌석은 **작성자로 서명하지 않습니다.** 좌석은 인격이 아니며, 회차 산출물 전체의 작성 책임은
소집자가 실명으로 집니다. 다른 주체가 제공한 증거·판정은 그 산출물과 주체를 출처로
연결합니다. 문서를 정리한 주체와 연구 결정을 내린 주체를 구분합니다.

**금지되는 것은 표기 형식이 아니라 거짓 귀속입니다** (`D-051`, 개정 전 조항은
`Co-Authored-By` 자체를 금지했습니다).

- **실제로 호출하지 않은 에이전트·세션을 호출한 것처럼 적지 않습니다.** 서브에이전트를
  쓰지 않고 쓴 것처럼 포장하거나, 한 세션의 산출물을 여러 주체의 협업처럼 꾸미지 않습니다.
- **다른 세션의 이름을 빌려 서명하지 않습니다.**
- 위 두 가지를 지키는 한 `Co-Authored-By` 등 공동 작성 표기는 **사용할 수 있습니다.**
  실제로 관여한 주체만 적습니다.

---

## 7. 모든 참여자의 연구 행동 원칙

> 이 절은 좌석·직과 무관하게 적용되며, 개정 이전 판본에서 그대로 유지합니다.

- 해결할 문제, 현재 목표, 접근을 택한 이유를 설명합니다. 방향이 미정이면 방향을 찾는 탐색부터
  합니다. 점수 최적화나 실행·구현 횟수만으로 진척을 대신하지 않습니다.
- 질문에 필요한 탐색·구현·관찰·검증을 수행하고 결과에서 배워 질문과 접근을 수정합니다.
  모든 연구에 같은 단계나 구현을 강제하지 않습니다. 필수 제약과 구현 관습을 나누고,
  아이디어 생성과 심사를 분리하며, 승인 예산 안에서 불확실한 접근도 작게 시험할 기회를 둡니다.
- 자원을 쓰기 전에 확인할 것·범위·예산·중단 조건을 정합니다. 초기 탐색에 성공 증거나
  완성된 기전 설명을 요구하지 않습니다. 관측에서 배운 것과 다음 판단의 근거 변화를 남깁니다.
- 실패하면 의도한 개입이 작동했는지, 실패가 알려주는 범위가 어디까지인지 확인합니다.
  현재 구성의 실패를 접근 전체의 불가능으로 확대하지 않습니다. 정보 가치가 없거나 예산에
  도달하면 보류·종료 이유를 남깁니다. 부수 문제는 목표나 결과 신뢰성을 막을 때 우선 다룹니다.
- 관측 사실·추정·설명을 구분하고 미확인은 명시합니다. 성능 향상이 기전 입증은 아니며,
  테스트 통과가 연구 주장 입증도 아닙니다. 결론은 확인한 증거의 범위를 넘지 않습니다.
- 탐색에서는 후보와 설명을 수정할 수 있습니다. 확증에서는 주장·판정 기준을 결과 전에 정합니다.
  사후 변경은 원래 계획과 결과를 보존하고 변경 이력을 남기며 사전 계획처럼 쓰지 않습니다.
- 비교 조건·측정 신뢰성·불확실성을 주장의 범위에 맞게 점검합니다. 판별 실패와 효과 없음을
  구분하고, 유리한 결과뿐 아니라 실패·악화·반대 증거도 보고합니다.
- 작업 의도·주요 선택과 변경·결과 출처·해석 한계·다음 행동을 복원할 만큼 기록합니다.
  현행 설명은 바로잡되 원시 증거·사전 계획·과거 판정은 덮어쓰지 않고 정정 이력을 남깁니다.
  기존 기록이 충분하면 별도 양식에 반복하지 않습니다.
- 승인된 범위는 자율적으로 완료하며 이미 승인된 행동을 다시 승인받지 않습니다.
  위임되지 않은 목표·기준·예산 변경은 근거와 권고를 준비해 사용자에게 요청합니다.
  문서·검증·절차는 질문과 결과 신뢰성에 필요한 만큼 수행하고 준수량을 성과로 삼지 않습니다.

[작성자: Claude Code / 소집자 겸 종결권자(사용자 지시에 의한 겸임) / claude-opus-5 (effort: 미확인) · 2026-09-17 20:30 KST]
