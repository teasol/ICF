# 공식 7-branch baseline 재생성과 오염 검사 경로 결함 (재현·계측)

- **RU 유형**: 재현·계측. 판정이 아니다.
- **동기**: 회차 `C-20260918-6`이 확정한 후보 사전 등록안은 공식 7-branch score를 입력으로
  요구하는데, `predictions/`에 `ru90_shape_triple` 산출물이 **한 건도 없었다.**
- **실행**: `bash scripts/run_ru90_shape_triple.sh` (GPU 4 단독, 스크립트의 유휴 GPU 규약이
  자동 선택). 2026-09-18 01:35\~02:49, 7과제 × 50 fold.

## 1. 결과

| 과제 | fold-mean AUROC |
|---|---:|
| `ARID1A_mutation` | 0.5795 |
| `Histologic_Grade` | 0.6665 |
| `KEAP1_mutation` | 0.6248 |
| `KRAS_mutation` | 0.7031 |
| `SMAD4_mutation` | 0.4661 |
| `progression_regression` | 0.7772 |
| `PBRM1_mutation` | 0.5408 |
| **macro** | **0.6226** |

`predictions/pathobench_*_ru90_shape_triple_official50_bf16.pt` 7건이 생성됐다.

## 2. 공식 7-branch 값은 재현된다

`PROJECT.md` §3.2의 공식 기준선 macro는 `0.6227`이다. 재생성 macro는 `0.6226`으로,
**차이 `0.0001`은 per-task 4자리 반올림값을 평균한 데서 나오는 오차 범위 안**이다.
정밀 대조는 저장된 per-fold 예측으로 다시 해야 한다(미수행).

## 3. 결함 — 오염 검사 경로가 문서대로 동작하지 않는다

`scripts/run_ru90_shape_triple.sh` 머리말은 `ICF_SHAPE_SCREEN_ONLY=1`이 BS/SH/SJ를 모든
집계 풀에서 제외하므로 **이 실행의 확률값이 곧 공식 5-branch BASE arm**이며, 따라서
`RU-90` 층 1 적합성 검사(macro `0.6171`, SMAD4 `0.4421`, PBRM1 `0.5553`)를 겸한다고 적고 있다.

**관측은 그렇지 않다.**

| 항목 | 문서가 기대하는 값(5-branch 앵커) | 이번 실행 | 일치 |
|---|---:|---:|:---:|
| macro | `0.6171` | 0.6226 | ✗ |
| SMAD4 | `0.4421` | 0.4661 | ✗ |
| PBRM1 | `0.5553` | 0.5408 | ✗ |

관측된 macro `0.6226`은 5-branch `0.6171`이 아니라 **7-branch `0.6227`에 일치**한다.
즉 이 실행은 5-branch BASE arm을 만들지 않았다. 가능한 원인 두 가지이며 **판별하지 않았다**:

1. 스크립트 머리말이 낡았다 — 공식 구성이 7-branch로 바뀐 뒤 `SCREEN_ONLY`의 의미가 달라져
   더는 5-branch 기저를 재현하지 않는다.
2. `ICF_SHAPE_SCREEN_ONLY`가 현재 코드에서 `SH`/`SJ`를 풀에서 빼지 못한다.

**기전 미확인.**

## 4. 무엇이 막히는가

`PROJECT.md` §3.4는 오염 검사 상수가 **소수 4자리까지 일치해야** 한다고 고정하고, `D-042`는
공식 구성이 7-branch로 바뀐 뒤에도 이 앵커를 그대로 유지한다고 정한다. 회차
`C-20260917-1`·`C-20260917-5`는 **오염 검사 상수 불일치 상태에서 후보 성능을 판단하는 실행은
무효**라고 분류했다.

따라서 **현재 상태에서는 오염 검사를 수행할 경로가 없다.** 후보(`C-20260918-6` 사전 등록안)의
실행 전 필수 감사 항목 하나가 충족될 수 없다. 이는 후보의 결함이 아니라 **기존 검사 경로의
결함**이며, 원인을 판별하기 전에는 그 검사를 통과했다고도 실패했다고도 쓸 수 없다.

## 5. 다음에 확인할 것

- `ICF_SHAPE_SCREEN_ONLY` 처리 경로를 코드에서 읽어 `SH`/`SJ`가 집계 풀에서 실제로 빠지는지 확인.
- 5-branch 기저(`BRANCHES_V121_5`)를 직접 지정해 재생성하고 앵커 2건과 4자리 대조.
- 저장된 per-fold 예측으로 공식 macro를 4자리 이상 정밀도로 재대조.

[작성자: Claude Code / 소집자 / claude-opus-5 (effort: 미확인) · 2026-09-18 03:00 KST]

## 6. 앵커 검증 결과 — 문서의 조작 knob이 모두 무효다 (2026-09-18 03:11)

`SH`/`SJ`/`BS` 가중치를 **명시적으로 `0.0`으로** 두고 `SMAD4`·`PBRM1`만 재실행했다
(`ICF_FIXED_HEAD_{BS,SH,SJ}_WEIGHT=0.0`).

| 과제 | 앵커(문서) | 1차 실행(가중치 1.0) | 2차 실행(가중치 0.0) |
|---|---:|---:|---:|
| SMAD4 | `0.4421` | 0.4661 | **0.4661** |
| PBRM1 | `0.5553` | 0.5408 | **0.5408** |

**가중치를 0으로 두어도 소수 4자리까지 값이 바뀌지 않는다.** 결과는 두 실행 사이에
결정론적으로 동일하므로 잡음이 아니다.

### 확인된 것

- `ICF_SHAPE_SCREEN_ONLY`는 비테스트 코드 어디에서도 읽히지 않는다(grep 확인).
- `ICF_FIXED_HEAD_{BS,SH,SJ}_WEIGHT` 역시 출력에 아무 영향이 없다.
- 따라서 **문서와 스크립트가 제공한다고 적은 조작 knob으로는 브랜치 집합을 바꿀 수 없다.**
- 기록된 5-branch 앵커 `0.4421`/`0.5553`은 **현재 코드로 재현되지 않는다.**

### 추론 (미검증)

관측 macro `0.6226`이 5-branch `0.6171`이 아니라 7-branch `0.6227`에 맞는 점과, 가중치
조작이 무효인 점을 함께 놓으면 **러너가 항상 7-branch 구성을 산출하고 있을 가능성**이 있다.
그렇다면 과거 `SCREEN_ONLY` 실행은 실제로는 screen-only가 아니었고, 오염 검사는 한 번도
의도한 대상을 검사하지 않은 것이 된다. **집계 코드를 읽어 확인하지 않았으므로 추론이다.**

### 결론

오염 검사는 **현재 수행 불가**이며, 이는 앵커 값의 문제가 아니라 **브랜치 집합을 통제하는
경로 자체의 문제**다. `PROJECT.md` §3.4·`D-042`가 고정한 검사 절차를 그대로 쓸 수 없다.
사용자 결정이 필요하다.

## 7. 별건 정정 — 회차 질문에 방법론 오류가 있었다

회차 `C-20260918-8`의 질문에 "남은 격차 약 `0.10`"이라고 적었다. 이는 `Primary 7` macro
`0.6227`과 `SEAL 10` `ABMIL` `0.7266`의 절대값 차이인데, **둘은 과제 집합이 다르다.**
`PROJECT.md` §3.3은 브랜치 집합·과제 집합이 다른 구성 사이의 절대값 직접 비교를 금지하고
판정은 같은 fold에서 짝지은 Δ로만 하도록 정한다. 협의체 감사 좌석이 이를 잡아냈다.

**이후 회차 질문과 보고에서 이 표현을 쓰지 않는다.** 목표와의 거리는 절대값 차이가 아니라
`hold-out 미검증` 상태로만 기술한다.

[작성자: Claude Code / 소집자 / claude-opus-5 (effort: 미확인) · 2026-09-18 03:15 KST]

## 8. 코드 확인 결과 — §6의 추론은 틀렸고, 실제 결함은 더 넓다 (2026-09-18 03:30)

§6은 "러너가 항상 7-branch를 산출할 가능성"을 추론으로 적었다. **코드를 읽어 확인한 결과
그 추론은 틀렸다.** 실제는 반대다.

### 확인된 사실 (모두 코드 대조)

1. **`src/`는 환경변수를 하나도 읽지 않는다.** `grep -rln "os.environ\|getenv" --include="*.py" src/`
   결과가 **0개 파일**이다.
2. **`scripts/eval_seal_tasks.sh`에 `ICF_` 참조가 0건이다.**
3. **`scripts/evaluate_pure.py`의 `ICF_SHAPE_SCREEN_ONLY` 언급은 독스트링 1줄뿐**(223행)이며
   코드에서 읽지 않는다.
4. **`scripts/eval_v121.sh`는 정의상 5-branch arm**이다(머리말: `CV + BM + BD + QA + DS, CT=OFF`).
   `icf_arm_v121()`은 `icf_arm_v120` 호출 뒤 `ICF_FIXED_HEAD_CT_WEIGHT=0.0`을 export할 뿐이다.
5. **`src/models/config.py` 기본값이 `weight_sh = 0.0`, `weight_sj = 0.0`, `weight_bs = 0.0`** 이고,
   사용된 YAML(`train_v98_p1_reverse_1536_1gpu.yaml`)은 브랜치 가중치를 설정하지 않는다.

### 따라서

**실행 스크립트가 export하는 모든 `ICF_*` 환경변수는 평가 코드에 도달하지 않는다.**
설정 표면 전체가 무효다. `eval_v121.sh`를 거치는 모든 실행은 호출 스크립트가 무엇을
export하든 **5-branch 구성**이다.

이로부터 세 가지가 따라온다.

- **`run_ru90_shape_triple.sh`는 shape margin을 만들 수 없다.** 이번에 생성한
  `predictions/pathobench_*_ru90_shape_triple_official50_bf16.pt` 7건은 **공식 7-branch가 아니라
  5-branch 산출물이며, 공식 태그로 잘못 이름 붙었다.** macro `0.6226`이 7-branch `0.6227`에
  가까운 것은 **우연**이다.
- **공식 7-branch 기준선(`0.6227`)을 재현할 경로가 현재 없다.** `SH`/`SJ`를 켜는 문서화된
  수단이 동작하지 않는다.
- **5-branch 앵커도 재현되지 않는다.** 현재 5-branch는 SMAD4 `0.4661`, PBRM1 `0.5408`,
  macro `0.6226`이고 기록값은 `0.4421`, `0.5553`, `0.6171`이다. 이는 설정 표면 결함과
  **별개의 두 번째 드리프트**다.

### 즉시 따라오는 차단

회차 `C-20260918-6`의 사전 등록 후보는 **공식 7-branch score를 입력으로 요구**한다.
그 입력을 만들 수 없으므로 **후보는 현재 실행 불가**다. 후보 설계의 결함이 아니다.

### 아직 확인하지 않은 것

- 과거 `ru90_shape_triple` 실행이 **당시에는** shape margin을 포함했는지. 그렇다면 그 뒤에
  설정 표면이 끊어진 것이고, 언제·어느 커밋에서 끊어졌는지 미확인이다.
- 5-branch 드리프트의 원인(코드 변경 / 매니페스트 / 전처리 / 수치 정밀도). 미확인.
- 이 결함이 `D-042` 승격 판정(`+0.56%p`, 7-branch 채택)에 미치는 영향. **판정하지 않았다.**

[작성자: Claude Code / 소집자 / claude-opus-5 (effort: 미확인) · 2026-09-18 03:30 KST]

## 9. §6·§8을 철회한다 — 계측은 망가지지 않았다 (2026-09-18 12:40, 실행 검증)

§6과 §8은 "설정 표면 전체가 무효", "공식 7-branch를 재현할 경로가 없다", "생성된 산출물은
5-branch가 공식 태그로 잘못 붙은 것"이라고 적었다. **셋 다 틀렸다.** 아래는 전부 실행으로
확인한 것이다.

### 확인된 사실

1. **YAML 설정은 정확히 파싱된다.** `TrainingFreeConfig.from_yaml('configs/baseline/
   v121_7branch_active.yaml')` → `weight_sh = 1.0`, `weight_sj = 1.0`.
   `_flatten_dict`가 `branches.sh.weight` → `weight_sh`로 변환한다(`config.py:266`).
2. **코드는 설정을 지킨다.** 합성 데이터로 `margins()`를 두 구성에 호출해 비교하니
   `max|diff| = 2.525e-01`. `sh`/`sj`를 `1.0`에서 `0.0`으로 바꾸면 출력이 달라진다.
3. **두 산출물은 같은 구성이었다.** `eval_v121.sh` 경로의 산출물과 7-branch 설정을 명시해
   실행한 산출물의 확률이 **비트 단위로 동일**하다(`max|Δprob| = 0.000e+00`, 2,459개 값,
   NaN 0개).
4. **원인은 폴백이다.** `scripts/eval_seal_tasks.sh:29`가 넘겨받은 설정에 `model:` 블록이
   있으면 `configs/baseline/v121_7branch_active.yaml`로 교체한다. `eval_v121.sh`가 넘기는
   archive 설정에 그 블록이 있어 항상 공식 7-branch로 실행됐다. **의도된 안전장치**이며,
   학습용 설정이 평가 경로로 새는 것을 막는다.
5. **5-branch 설정은 존재한다.** `configs/baseline/v121_active.yaml`의 활성 브랜치는
   `cv, bm, bd, qa, ds`다.

### 따라서 정정한다

| §6·§8의 주장 | 실제 |
|---|---|
| 설정 표면 전체가 무효 | **YAML 경로는 정상.** 무효인 것은 `ICF_*` 환경변수뿐이다 |
| `ru90_shape_triple` 산출물은 5-branch | **7-branch가 맞다.** 공식 태그가 정확했다 |
| 공식 7-branch 재현 경로가 없다 | **이미 재현했다.** macro `0.6226`이 공식 `0.6227`과 일치한 것은 우연이 아니다 |
| 5-branch 앵커를 재현할 수 없다 | 5-branch **설정**은 있다. 앵커 재현 여부는 별건이며 검증 중 |

### 여전히 사실인 것

- `src/`에서 환경변수를 읽는 파일이 **0개**다. `run_ru90_shape_triple.sh`의 `SCREEN_ONLY`와
  가중치 export는 아무 효과가 없다. 그 스크립트의 머리말은 낡았다.
- 저장된 예측 파일에 branch list·가중치·코드 해시가 **없다**. provenance 부재는 그대로이며,
  이번 오진이 그 필요성을 오히려 키웠다 — 산출물만 봐서는 어느 구성인지 알 수 없었다.

### 방법론적 교훈

코드 네 겹(파싱·계산 게이팅·집계·호출 경로)이 모두 정상인데 출력이 같았고, 그 시점에
"원인 미확인"으로 멈췄다. **먼저 했어야 할 검사는 두 가지였다**: (a) 서로 다른 구성이 서로
다른 출력을 내는지 합성 데이터로 토글, (b) 두 산출물의 확률을 비트 단위로 비교. 둘 다 몇 초
걸리고 결론을 즉시 가른다. 코드를 읽어 내려가는 것보다 **출력 차이를 먼저 재는 것**이 빨랐다.

[작성자: Claude Code / 소집자 / claude-opus-5 (effort: 미확인) · 2026-09-18 12:40 KST]

---

## §10 폴백이 언제 발동하는가 — 사슬을 끝까지 따라간 결과

§9는 원인을 `eval_seal_tasks.sh`의 폴백으로 지목했지만, **어떤 실행이 그 폴백을 밟는지는
확인하지 않았다.** 확인해 보니 두 baseline 설정 중 어느 쪽도 폴백 조건을 만족하지 않는다.

```
grep -c "model:" configs/baseline/v121_active.yaml          -> 0
grep -c "model:" configs/baseline/v121_7branch_active.yaml  -> 0
```

폴백 조건은 `[ ! -f "$cfg" ] || grep -q "model:" "$cfg"`이다. baseline 설정을 직접 넘기면
폴백은 발동하지 않는다. 그렇다면 왜 산출물이 비트 단위로 같았는가.

### 사슬

`scripts/eval_v121.sh`가 넘기는 설정은 baseline 설정이 아니다.

```
scripts/eval_v121.sh:22
CONFIG="configs/archive/v94_v102_cell_value/train_v98_p1_reverse_1536_1gpu.yaml"
```

이것은 **학습 설정**이고 `model:` 블록을 갖는다(`grep -c "model:"` → `1`). 따라서
`eval_seal_tasks.sh`가 이를 `configs/baseline/v121_7branch_active.yaml`로 교체한다.

`eval_v121.sh`의 머리말은 첫 줄부터 `v121 = 5-Branch Fast Baseline (CT=OFF)`이라고 적고
있고, 실행 직전에 `CV=offdiag ... CT=off w=0.0`을 출력한다. **그 출력은 실제 실행 구성과
무관하다.** 실제로는 7-branch가 돈다.

### 범위 — 어느 러너가 영향을 받는가

| 러너 | 넘기는 설정 | `model:` | 실제 실행 |
|---|---|---|---|
| `eval_v121.sh` | `train_v98_p1_reverse_1536_1gpu.yaml` | 1 | 7-branch |
| `run_v120_clean_loo_experiments.sh` | 같음 | 1 | 7-branch |
| `run_v121_salience_anchor.sh` | 같음 | 1 | 7-branch |
| `eval_v120.sh` | `$ICF_CONFIG` | 1 | 7-branch |
| `eval_ct_alone.sh` | `$ICF_CONFIG` | 1 | 7-branch |
| `eval_ds_aug.sh` | `$ICF_CONFIG` | 1 | 7-branch |

`ICF_CONFIG`는 `scripts/node_env.sh:95`에서 같은 학습 설정으로 기본값이 잡힌다. 즉
**여섯 러너 전부가 같은 하나의 설정으로 수렴한다.** 이름이 다른 arm들이 서로 다른 구성을
돌린 적이 없다.

두 번째 층도 있다. 각 러너는 `icf_arm_v120` / `icf_arm_v121` 등으로 `ICF_*` 환경변수를
export 해 arm을 고른다. `src/`에서 이 변수를 읽는 코드는 0개다(§9에서 확인). 따라서
**arm 선택도 무효**다. 설정 경로와 환경변수 경로, 두 경로 모두 arm을 전달하지 못했다.

### 직접 실행한 5-branch

`evaluate_pure.py`를 직접 호출해 `v121_active.yaml`(5-branch)로 돌리면 결과가 달라진다.

```
config: aggregation=trimmed_mean weights(cv=1.0,bm=1.0,bd=1.0,qa=1.0,ds=1.0,sh=0.0,sj=0.0,ct=0.0,dd=0.0)
cptac_pda/SMAD4_mutation  fold-mean AUROC: 0.4426  n_folds=50
```

같은 과제를 7-branch로 돌린 값은 `0.4661`이다. **두 설정은 실제로 다른 결과를 낸다.**
비트 동일성은 설정이 무력했기 때문이 아니라 두 실행이 같은 설정으로 교체됐기 때문이다.

문서에 앵커로 적힌 값은 SMAD4 `0.4421`이다. 직접 실행한 5-branch는 `0.4426`으로
4자리에서 일치하지 않는다(차이 `0.0005`). 7-branch `0.4661`과는 훨씬 멀다. 즉 앵커는
5-branch 계열에서 나온 것으로 보이나 **현재 코드로 재현되지 않는다.** 재현 실패의 원인이
코드 변경인지 데이터 변경인지는 미확인이다.

PBRM1도 끝났다. 두 과제를 함께 보면 이렇다.

| 과제 | 문서 앵커 | 5-branch 직접 실행 | 차이 |
|---|---:|---:|---:|
| SMAD4 | `0.4421` | `0.4426` | `+0.0005` |
| PBRM1 | `0.5553` | `0.5546` | `-0.0007` |

**부호가 반대이고 크기는 `5e-4` 수준이다.** 균일한 이동이 아니므로 단순한 스케일 변경이나
집계 방식 변경으로는 설명되지 않는다. `evaluate_pure.py`는 closed-form ridge이고 seed를
받지 않으므로 이 실행은 결정론적이다. 따라서 이 차이는 실행 변동이 아니라 **앵커가 기록된
시점과 지금 사이에 실제로 무언가 바뀌었다**는 뜻이다. 무엇이 바뀌었는지는 미확인이다.

동시에 이 결과는 앵커 설계 자체의 문제를 드러낸다. `PROJECT.md` §3.4는 **소수 4자리 일치**를
통과 기준으로 요구한다. 그런데 구성을 바꾸지 않고 같은 설정으로 돌린 지금 실행이 이미 4자리를
벗어난다. **요구된 정밀도가 시스템이 실제로 유지하는 정밀도보다 높다.** 이 검사는 구성 변화가
없어도 실패하므로, 통과/실패 어느 쪽도 오염에 대해 알려주는 바가 없다.

한편 5-branch가 맞는 구성이라는 것은 이 결과가 지지한다: 7-branch 값(`0.4661` / `0.5408`)은
앵커에서 `0.024` / `0.015` 떨어져 있고, 5-branch는 `0.0005` / `0.0007`이다. 두 자릿수 차이다.

### 이것이 바꾸는 것

§9는 "공식 태그가 정확했다"고 썼다. 그 문장은 유지된다 — `ru90_shape_triple` 산출물은
7-branch가 맞다. 다만 §9가 몰랐던 것은, **7-branch로 태그되지 않은 과거 arm 비교들도
전부 같은 7-branch였다**는 점이다. arm 간 차이를 보고했던 과거 실행은 구성 차이가 없는
실행들을 비교한 것이므로, 거기서 나온 차이는 전부 적합 변동이다.

이 결론의 범위: 위 표의 여섯 러너를 거친 실행에 한정한다. `evaluate_pure.py`를 직접 호출한
실행과 `scripts/analysis/` 프로브는 이 사슬을 타지 않으므로 별도 판정이 필요하다.

[작성자: Claude Code / 소집자 / claude-opus-5 · 2026-09-18 13:05 KST]
