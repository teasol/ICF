# 질문
1. provenance writer의 구현 상태와 기록 필드 목록은 무엇인가? §2가 완료 조건으로 지정했으나 구현 여부가 문서에 없다.
2. `evaluate_pure.py`가 두 arm과 설정 파일을 받는 CLI 인자 형식, 그리고 저장 예측 파일의 스키마(어떤 키에 fold별 AUROC와 branch list가 들어가는가)는 무엇인가?
3. RU-99의 디바이스 상주화가 SMAD4 fold 10 외의 과제·fold에서 비트 동일성을 확인한 기록이 있는가?
4. `scripts/slurm/` 아래에 Primary 7 전 과제를 한 잡으로 도는 제출 스크립트가 이미 있는가?
5. `scripts/evaluate_pure.py`에서 basis/PCA/subsample의 fit이 train fold에만 국한되는지, 그리고 그 fit 범위가 코드 어느 줄에서 결정되는가?
6. 저장 예측 파일에 현재 기록되는 메타데이터 필드의 실제 목록은 무엇인가(provenance 재설계가 요구하는 branch list·가중치·코드 해시·데이터 버전·fold manifest 해시 중 몇 개가 이미 있는가)?
7. `D-042` 승격 판정 당시 Δ의 집계 단위가 fold였는지 과제 군집이었는지 기록이 있는가?
8. `evaluate_pure.py`에 config 경로를 넘기는 정확한 CLI 플래그와 인자 형식은 무엇인가?
9. `scripts/analysis/parity_paired_delta.py`의 입력 형식과 CLI는 무엇인가?
10. §4의 판정(지지/반박/판별 불가)과 승격 기준 충족의 관계 — 판별 불가가 승격을 막는지 — 를 명시한 결정 기록이 있는가?
11. 공식 구성의 강등 규칙이 문서에 존재하는가?
12. `D-042` 승격 당시 사용한 집계 단위(fold 독립 / 과제 군집)는 기록되어 있는가?
13. 7-branch의 fold 적합 단가는 측정된 적이 있는가?

# 원문 응답 (검증 전)
[1]
**계측 복구.** provenance writer, `SH`/`SJ` branch control 경로, 오염 검사 재설계, 단위 테스트. 복구 전에는 게이트·승격 비교를 `verified`로 수행할 수 없다(`D-050`). `RU-97`은 음성으로 종료됐다(`D-052`)
**재설계 방향**: 값 일치가 아니라 **provenance 기록**을 검사 대상으로 삼는다 — 산출물에 branch list·가중치·코드 해시·데이터 버전·fold manifest 해시를 저장하고 이를 대조한다.

[2]

[3]
- 과제 하나(SMAD4) fold 10에서만 전후 비교했다. 나머지 6과제·전체 fold는 외삽이다.

[4]

[5]
- **PCA·subsample 사양이 문서에 없다.** 회차 16이 v1 안을 냈으나 기록되지 않았다.

[6]
저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.
저장된 예측 파일에 provenance가 없어 과거 산출물을 구성에 귀속시킬 수 없다는 점도 그대로다.
**재설계 방향**: 값 일치가 아니라 **provenance 기록**을 검사 대상으로 삼는다 — 산출물에 branch list·가중치·코드 해시·데이터 버전·fold manifest 해시를 저장하고 이를 대조한다.

[7]
과거 판정이 어느 단위를 썼는지는 이 자료로 알 수 없다(`기전 미확인`이 아니라 `기록 없음`).

[8]

[9]

[10]
- 판정은 `지지 / 반박 / 판별 불가 / 실행 무효`로 적고, 채택 여부(운영 결정)와 분리한다.

[11]

[12]
과거 판정이 어느 단위를 썼는지는 이 자료로 알 수 없다(`기전 미확인`이 아니라 `기록 없음`).

[13]
`PROJECT.md` §2가 선언한 `20.0`초는 **5-branch 값이며 7-branch는 미측정**이라는 것도 그대로다.
- 7-branch는 측정하지 않았다.