# 질문
1. RU-88의 "분산비"(F1 1.31 / F2 0.20 / F3 125.9·48.0)의 정의는 무엇이며(분자·분모), F3의 두 값은 각각 어느 표적에 대한 것인가?
2. 저장된 예측에 슬라이드 단위 마진이 남아 있는가 — RU-85의 "query 슬라이드-fold 쌍 3,061개"는 슬라이드별 마진에서 산출된 것인가?
3. 50-fold에서 한 슬라이드가 query로 등장하는 fold 수의 분포는 무엇이며, 슬라이드당 실현이 2 미만인 과제가 있는가?
4. `SCREEN_ONLY` 게이트 ① 실행 경로의 현재 상태는 무엇인가 — §3-I는 "현재 무효다(`D-050`)"라 하고 `current_status.md`는 "계측 복구 완료"라 한다. 어느 쪽이 최신이며, 라벨 무관 스크린을 지금 돌릴 수 있는가?
5. RU-88 F3(Task-Geometry 계열)의 판별 불가 3건은 각각 어떤 통계량이며 그 수치는 무엇인가?
6. RU-102의 oracle 상한 `+0.0053`에 대응하는 SE 값은 무엇인가("SE 미만"으로만 기록돼 있다)?
7. §225 DS 강도 스윕의 과제별 Δ 전체 표(6강도 × 7과제)는 어디에 있는가 — `ARID1A +2.97%p` / `PBRM1 −6.64%p` 외 값은 문서에 없다.
8. `N_ctx`의 과제별 실측값은 무엇인가(범위 `90~261`만 기록돼 있다)?
9. SJ 악화 3과제(`Histologic_Grade`·`progression_regression`·`PBRM1`)의 fold별 Δ 분포는 저장돼 있는가 — 점추정만 문서에 있다.
10. RU-86 부호 반전의 fold 수준 분포(과제 내 부호 안정성)는 저장 마진에 남아 있는가?
11. Primary 7의 50-fold에서 슬라이드 ID가 fold 간에 어떻게 배치되는가 — 각 슬라이드는 정확히 한 fold의 test에만 등장하는가, 여러 fold의 test에 재등장하는가? fold manifest 파일의 경로와 형식은 무엇인가?
12. 저장된 예측·마진 파일에 슬라이드 ID가 보존되어 있는가? 보존되어 있다면 어떤 파일·필드인가?
13. `D-049`의 허용 조항("승인된 ensemble readout이 context 라벨만으로 fold별 닫힌 형태 해를 풀어 계수를 정하는 것")은, readout 밖의 외부 선택기가 context 라벨로 강도를 고르는 경우까지 포함하는가?
14. `src/models/common/solvers.py`의 ridge 해가 슬라이드 제거 잭나이프를 rank-1 갱신으로 지원하는가?
15. `RU-88`의 F1(이상치 근접도)·F2(context↔query MMD)·F3(Task-Geometry 계열)가 각각 **어떤 양을 어떤 단위(과제/fold/슬라이드)에서** 계산했는지, 그리고 진입 기준 `|ρ| ≥ 0.3`의 `ρ`가 무엇과 무엇의 상관인지 — 문서에는 분산비와 판정 결과만 있고 정의가 없다.
16. `RU-103`의 "context 자기 AUROC(in-sample 퇴화)"와 `RU-104`의 "label-free 합의 상관(전 과제 양수)"의 **실측 수치**가 문서에 없다. 두 신호가 각각 어떤 통계량이었고 값이 얼마였는지.
17. Primary 7 각 과제의 **슬라이드 수**와 **슬라이드당 fold 등장 횟수**가 문서에 없다. 위 첫 실행 스텝의 판정에 직접 필요하다.
18. §225 DS 강도 스윕에서 과제별 최적 강도의 **실제 값**(ARID1A `+2.97%p`, PBRM1 `−6.64%p` 외 나머지 5과제)이 문서에 없다. 병목 3의 "켜진 비율 × 조건부 이득" 산술을 검증하려면 필요하다.

# 원문 응답 (검증 전)
[1]
새 지문 F1(이상치 근접도)·F2(context↔query MMD)는 두 표적 모두에서 구간이 관심 크기 `0.3`을 배제해 **반박**됐고, 대조군 F3(Task-Geometry 계열)에 **판별 불가** 3건이 몰렸다 ([보고](reports/RU-88_car1_fingerprint_panel.md))
설계 전제가 부분적으로만 성립했다: F1·F2는 과제 내 변동이 커서(분산비 1.31·0.20) fold 해상도가 정밀도를 샀으나, F3는 사실상 과제 수준 회귀변수여서(125.9·48.0) 사지 못했다.

[2]
- **확인된 사실 (RU-85)**: `cptac_luad/KRAS_mutation` 50 fold, query 슬라이드-fold 쌍 3,061개에서 각도 산포 스펙트럼의 정규화 엔트로피 `H_q`가 **슬라이드의 100%에서 0.99를 초과**하고 슬라이드 간 표준편차가 **0.0017**이다(평균 0.9963).

[3]

[4]
- **실행 선행 조건**: 게이트 ① 실행 경로(`SCREEN_ONLY`)가 현재 무효다(`D-050`). 계측이 복구된 뒤 라벨 무관·저비용 게이트 ①을 먼저 돌린다.
| **Status** | **D-042 verified** · 형상 브랜치/TGW 계보 종료(`RU-101~104`, `D-058`) · 오염 검사 L1 · 계측 복구 완료 · 자동 잡무 중단(`D-056`) · `SEAL 10` 보류(`D-057`) |
| **현재 목표** | **계측 복구.** provenance writer, `SH`/`SJ` branch control 경로, 오염 검사 재설계, 단위 테스트. 복구 전에는 게이트·승격 비교를 `verified`로 수행할 수 없다(`D-050`). `RU-97`은 음성으로 종료됐다(`D-052`) |

[5]
새 지문 F1(이상치 근접도)·F2(context↔query MMD)는 두 표적 모두에서 구간이 관심 크기 `0.3`을 배제해 **반박**됐고, 대조군 F3(Task-Geometry 계열)에 **판별 불가** 3건이 몰렸다 ([보고](reports/RU-88_car1_fingerprint_panel.md))

[6]
`RU-102`(oracle 상한 `+0.0053`, SE 미만)

[7]
이득이 전역이 아니라 과제별이고
(ARID1A +2.97%p vs PBRM1 −6.64%p), 과제별 강도를 고를 **선택 신호가 없다.**
`ARID1A +2.97%p` 대 `PBRM1 −6.64%p`

[8]
실측 context 슬라이드 수는 **90~261장**이다(`D-026`)

[9]
**악화된 과제(전량)**: `Histologic_Grade` `-1.08%p` · `progression_regression` `-1.13%p` ·
`PBRM1` `-1.44%p`. 세 과제는 `SH` 단독·`SJ` 단독에서도 같은 방향으로 악화된다. **기전 미확인.**

[10]
2차 탐색에서 **부호 반전** 관측 — `Grade`는 50 중 40 fold가 0.5 **미만**, `KRAS`는 41 fold가 초과이며 `|중앙값−0.5|`가 0.0821 대 0.0861로 크기가 같고 방향이 반대다.

[11]
- **Fold는 PathoBench 공식 분할 그대로 고정한다.** fold 수 증대나 재분할로 분해능을 높이는 것은 **금지**한다.

[12]

[13]
- **허용**: 승인된 ensemble readout(Dual Ridge 등)이 context 라벨만으로 fold별 닫힌 형태
  해를 풀어 여러 강도 중 계수를 정하는 것.
- **금지(Oracle, §224)**: 결과 AUROC를 **보고 나서** 강도·변형을 고르는 것. 과제별 최적
  강도를 성능 조회 뒤에 선택하는 모든 경로를 포함한다.

[14]

[15]
진입 기준(구간 0 배제 그리고 `|ρ|>=0.3`) 충족 **0/8**.
설계 전제가 부분적으로만 성립했다: F1·F2는 과제 내 변동이 커서(분산비 1.31·0.20) fold 해상도가 정밀도를 샀으나, F3는 사실상 과제 수준 회귀변수여서(125.9·48.0) 사지 못했다.

[16]
`RU-103`(context 자기 AUROC 퇴화)·`RU-104`(label-free 합의 상관이 전 과제 양수 → 전이 실패)

[17]
KRAS 312 슬라이드만 저장 예측 파일에서 확인된다.

[18]
이득이 전역이 아니라 과제별이고
(ARID1A +2.97%p vs PBRM1 −6.64%p), 과제별 강도를 고를 **선택 신호가 없다.**
`ARID1A +2.97%p` 대 `PBRM1 −6.64%p`