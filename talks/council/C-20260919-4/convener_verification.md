# C-20260919-4 · 소집자 검증과 종결 (선택 신호 벽)

소집자: opencode / deepseek-v4.1-flash. 원문은 수정하지 않는다.

## 회차 운영

- Phase B 2좌석 × 3턴: **가설 18건 · 이견 6턴.** 협의체 P/R/A/T/S, 호출 16/24, 552.4초,
  Phase Q 18건 중 인용 검증 12·폐기 4. 기권 없음. 모델 다양성 0.
- **이 회차는 검증된 새 신호를 내지 못했다.** 제안 1\~3의 원안 지지는 Phase 2 교차에서
  크게 줄었고, 남은 다음 행동은 신호 설계가 아니라 **GPU-0 사실 확인**이다. 있는 그대로 적는다.

## 소집자가 실행한 GPU-0 확인

| # | 확인 | 결과 |
|:--|:--|:--|
| 1 | `evaluate_pure` 저장 레코드 필드 | `per_fold`에 `fold`·`slide_id`·`label`·`probability`가 있다(**슬라이드 수준 확률**). 브랜치별 슬라이드 margin은 공식 러너에 없고 `predictions/margins_7b`(RU-101)에만 있다 |
| 2 | fold 배정 구조 | **분할이 아니라 반복 랜덤 test 표집.** Primary 7에서 슬라이드당 test 등장 평균 `9.55`\~`10.16`회(test 비율 약 20%). fold 독립 가정 위반이 정량화된다(`§4.1` 설계효과 `5.12`배와 일관) |
| 3 | `RU-88` 분산비 정의 | **과제 간 분산 ÷ 과제 내 분산.** F3a `125.9` · F3b `48.0`(사실상 과제 수준 회귀변수, 실효 표본 ≈7) · F1 `1.31`. 정의는 문서에 있다 |
| 4 | `RU-86` 부호 반전의 저장 재집계 | MDX margin이 저장돼 있지 않아 **불가**(신규 실행 필요). 하지 않았다 |
| 6 | 문서 정합 | `closed_axes §3-I`("게이트 ① `SCREEN_ONLY` 경로 무효")는 **현재도 유효**하다. `current_status`의 "계측 복구 완료"는 과한 표현이어서 **"부분(gate-① `SCREEN_ONLY` 경로 부재)"으로 정정**했다 |

## 결정 (T1)

- S의 증거 분류와 다음 행동을 채택한다. 다만 이 회차의 산출은 **새 신호가 아니라 제약의 정량화**다.
- 핵심 제약: 공식 50 fold는 슬라이드가 여러 fold의 test에 재등장하는 랜덤 표집이므로, 슬라이드
  수준 규칙이 test 라벨에 **복수 적응**할 위험을 안는다(회차 §하지 말 것 1). 어떤 slide-level
  신호든 이 제약을 먼저 통제해야 한다.
- `P5 #22`(선택 신호 부재)는 **열리지 않았다.** 라벨 무관 신호를 새 정보원에서 만들어야 하며,
  이는 별도 RU 또는 방향 결정 사안이다.
- 문서 정정 1건(gate-① 경로 상태)을 반영했다.

[작성자: opencode / 소집자 / deepseek-v4.1-flash (effort: 미확인) · 2026-09-20 04:25 KST]
