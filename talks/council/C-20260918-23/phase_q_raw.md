# 질문
1. BM·QA·DS 각각의 과제별 단독 AUROC는 얼마인가 — §5.1의 QA ARID1A `0.4692` 외에 나머지 값이 어느 문서에 있는가?
2. 게이트 ①이 기존 공식 구성에 소급 적용된 적이 있는가, 아니면 §5 문구대로 신규 후보에만 적용돼 왔는가?
3. `RM` 기각에서 결정적 근거는 `max |r| = 0.690`이었는가, 아니면 CV가 투영 공간 전체의 공분산이라 꼬리 부분공간을 이미 읽고 있었다는 정보 논거였는가?
4. fold 적합 단가가 5-branch `20.0`초/fold 외에 다른 브랜치 수(4·6·7)에 대해 측정된 값이 있는가?
5. BM·QA·DS의 fold별 margin에서 공통 성분을 제거한 잔차 상관이 저장 자료로 계산된 적이 있는가?
6. §3.2의 악화 3과제가 SH 단독·SJ 단독에서도 같은 방향으로 악화된다는 기록의 실제 수치는 어디에 있는가?
7. `talks/reports/2026-09-18_branch_redundancy.md`에 적힌 유효 랭크(참여비)의 정의식과 계산 절차는 무엇인가 — `PROJECT.md` §3.2·§5에는 "참여비 정의"라는 이름만 있고 식이 없다.
8. 게이트 ①의 임계값 `0.6`은 어느 결정 레코드·어느 실측에서 정해졌는가 — `closed_axes.md` `CA-09`의 `RM` 탈락(max |r| = 0.690)이 유일한 인용 실적이며, 임계값 자체의 출처는 문서에 없다.
9. Trimmed Mean(최저·최고 1개 절사 후 중앙값 평균)에서 동일 계열 구성원 3개가 차지하는 가중이 어떻게 결정되는지, 그리고 그 규칙이 문서화돼 있는가.
10. Trimmed Mean의 절사 개수가 branch 수와 무관하게 고정 1인지, 비례인지.
11. 5-branch arm과 7-branch arm이 같은 fold manifest·전처리·readout을 썼다는 기록이 있는지.
12. 유효 랭크의 산출 단위(과제별/통합)와 "참여비 정의"의 정확한 식.
13. 5-branch 구성의 유효 랭크 값.
14. `20.0`초/fold가 순수 적합 시간인지 eval 포함인지, 그리고 현재 잔여 GPU-hour.
15. 유효 랭크의 "참여비 정의"는 무엇인가 — 문서에 정의가 있는가?
16. 5→7 비교에서 악화된 3개 과제의 이름은 무엇이며, `PROJECT.md` §3.2가 기록한 3개(`Histologic_Grade`, `progression_regression`, `PBRM1`)와 동일한가?
17. §5 게이트 ①의 `max |r|`은 후보-기준 쌍들의 최댓값인가, 기준 집합 내부 쌍까지 포함하는가 — 명문이 있는가?
18. BD·SJ를 앙상블에서 제거한 대응 비교 결과가 저장되어 있는가?
19. 게이트 ①(`SCREEN_ONLY`) 실행 1회의 실측 비용(GPU-hour 또는 벽시계)이 기록된 문서가 있는가?
20. BM·QA·DS 병합 또는 2개 제거 구성의 Primary 7 성능 측정이 어느 문서에도 없는가?

# 원문 응답 (검증 전)
[1]
기존 브랜치들 자체가 ②a를 만족하지 못한다 — `CV`는 ARID1A `0.4308`, `BD`는 3개 과제, `QA`는 ARID1A `0.4692`로 우연 이하이며, 그럼에도 정식 브랜치다.

[2]

[3]
- **기각 기전**: `BM`(사영) · `QA`(분위수) · `DS`(살리언스 재가중) · `RM`(꼬리 사영)이 **모두 한 계열로 수렴**했다. 위치(Location) 계열의 상호 상관은 0.25~0.93이고, `BM`·`QA`·`DS` 셋의 유효 랭크는 `1.29 / 3`이다. `RM`은 max |r| = 0.690으로 게이트 ① 탈락했고, 그 원인은 `CV`가 256차원 투영 공간 **전체**의 공분산이라 꼬리 부분공간을 이미 읽고 있었기 때문이다.

[4]
| **fold 적합 단가** | **`20.0`초/fold** (5-branch, GPU 단독, 과제 3개 평균 `18.2`\~`22.0`). 한 arm Primary 7(350 fold) `1.94` GPU-h · **2 arm paired 확증 `3.89` GPU-h = 예산의 97%** · `R=4` 2×2 `31.1` GPU-h. 7-branch는 더 비싸므로 이 값은 하한이다. 출처: [fold 적합 비용 보고서](../talks/reports/2026-09-18_foldfit_cost.md) |

[5]

[6]
**악화된 과제(전량)**: `Histologic_Grade` `-1.08%p` · `progression_regression` `-1.13%p` · `PBRM1` `-1.44%p`. 세 과제는 `SH` 단독·`SJ` 단독에서도 같은 방향으로 악화된다. **기전 미확인.**

[7]

[8]

[9]
| 집계 | Trimmed Mean (최저·최고 1개 절사 후 중앙값 평균) |

[10]
| 집계 | Trimmed Mean (최저·최고 1개 절사 후 중앙값 평균) |

[11]

[12]

[13]

[14]
### fold 적합 1회 = `20.0`초 · 확증 프로브는 예산에 안 들어간다
한 arm Primary 7 `1.94` GPU-hour, **2 arm paired `3.89`(예산 4h의 97%)**, `R=4` 2×2는 `31.1`로 7.8배다.
| **예산** | NEXGEM B200 1장 합계 최대 4 GPU-hour. 배경 GMM은 기존 3M 표본, CPU thread 8, `max_iter=100` 범위만 사용한다 |

[15]

[16]
**악화된 과제(전량)**: `Histologic_Grade` `-1.08%p` · `progression_regression` `-1.13%p` · `PBRM1` `-1.44%p`. 세 과제는 `SH` 단독·`SJ` 단독에서도 같은 방향으로 악화된다. **기전 미확인.**
그러나 과제 군집(`df=6`) 95% CI가 **`[-0.0115, +0.0228]`로 0을 포함**하고 **7개 중 3개 과제가 악화**된다.

[17]

[18]

[19]

[20]