# 질문
1. `predictions/parity_*.pt`에 실제로 저장된 필드 목록(arm 식별자, branch list, fold 인덱스, 과제별 Δ 7개 값 전체)은 무엇인가 — 문서에 없음.
2. 5→7 비교의 과제별 Δ 7개 값 전체는 무엇인가 — §3.2는 악화 3건(`-1.08`/`-1.13`/`-1.44`%p)만 적고 나머지 4건의 값을 적지 않았다.
3. 5-branch 구성의 유효 랭크(참여비) 값은 얼마인가 — §3.2는 7-branch의 `3.52/7`만 적고 5-branch 값을 적지 않았다.
4. `scripts/analysis/branch_screen.py` 1회 실행이 요구하는 fold 적합 횟수는 얼마인가?
5. 7-branch 구성의 fold 적합 단가는 얼마인가?
6. 게이트 ②b(단독 AUROC 40/50) 판정의 실행 경로와 비용은 무엇인가?
7. 후보 순서화 규칙을 사전 고정하는 절차가 문서에 정의된 바 있는가?
8. `RU-97` §8의 seed 분산 `0.0200` 측정에 쓰인 구성은 공식 7-branch인가, 학습 파라미터를 포함한 구성인가?
9. fold 적합 시간의 branch 수 의존성(예: 3·5·7-branch에서 초/fold)이 측정된 적이 있는가, 있다면 그 값은 무엇인가?
10. 게이트 ① `SCREEN_ONLY` 스크린 1회의 비용이 fold 적합 몇 회로 환산되는가?
11. 5v7 비교 외에, 임의 후보의 대응 Δ에 적용할 수 있는 과제 수준 sd의 일반 추정치가 문서 어디에 있는가?
12. 5→7 대응 비교의 과제 간 Δ sd `0.0185`와 RU-80의 대응 SE(과제 군집 7) `0.022%p`가 30배 차이나는 이유는 무엇인가? 두 값은 같은 설계를 재는가, 다른 설계를 재는가?
13. branch 수에 따른 fold 적합 시간(7-branch 초/fold, branch당 증분)이 측정된 적이 있는가?
14. MDE 계산에 쓸 t 임계값(df=6, α=0.05, power 0.80)이 문서에 선언된 적이 있는가?
15. parity 비교(5-branch 대 7-branch)를 실제로 돌린 러너는 무엇이며, 두 arm이 서로 다른 branch list·가중치로 실행됐음을 보여주는 로그나 설정 파일이 있는가?
16. `predictions/parity_*.pt`에 branch list·가중치·코드 해시·fold manifest 해시가 저장돼 있는가?
17. 5→7 비교의 과제 간 sd `0.0185`는 어떻게 계산됐으며, 과제별 Δ 7개의 값은 각각 얼마인가?
18. branch 수에 따른 fold 적합 시간 의존성을 측정한 기록이 있는가?
19. `CA-R1` 후보 2(컨텍스트 라벨 판별 통계)와 후보 5(다중 강도 동시 투입)의 예상 비용이 fold 적합 몇 회로 산정된 적이 있는가?

# 원문 응답 (검증 전)
[1]
> 저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.
> 저장된 예측 파일에 provenance가 없어 과거 산출물을 구성에 귀속시킬 수 없다는 점도 그대로다.
> **fold 수준 짝지은 Δ 미산출.** `predictions/parity_*.pt`로 가능하나, 과제 군집 CI가 이미 0을 포함하므로 우선순위는 낮다.

[2]
> Primary 7 macro 차이는 **`+0.0057`**이고, [`PROJECT.md`](PROJECT.md)의 공식 기준선·계보값·승격 근거가 **전부 재현된다.**
> 그러나 과제 군집(`df=6`) 95% CI가 **`[-0.0115, +0.0228]`로 0을 포함**하고 **7개 중 3개 과제가 악화**된다.
> 과제 간 sd `0.0185`가 평균 효과의 3배다.
> **악화된 과제(전량)**: `Histologic_Grade` `-1.08%p` · `progression_regression` `-1.13%p` · `PBRM1` `-1.44%p`. 세 과제는 `SH` 단독·`SJ` 단독에서도 같은 방향으로 악화된다. **기전 미확인.**

[3]

[4]

[5]
> | **fold 적합 단가** | **`20.0`초/fold** (5-branch, GPU 단독, 과제 3개 평균 `18.2`\~`22.0`). 한 arm Primary 7(350 fold) `1.94` GPU-h · **2 arm paired 확증 `3.89` GPU-h = 예산의 97%** · `R=4` 2×2 `31.1` GPU-h. 7-branch는 더 비싸므로 이 값은 하한이다. 출처: [fold 적합 비용 보고서](../talks/reports/2026-09-18_foldfit_cost.md) |
> ### fold 적합 1회 = `20.0`초 · 확증 프로브는 예산에 안 들어간다
> 한 arm Primary 7 `1.94` GPU-hour, **2 arm paired `3.89`(예산 4h의 97%)**, `R=4` 2×2는 `31.1`로 7.8배다.

[6]

[7]
> **탐색·후보 선별과 성능 확증을 분리한다.** §214의 문제는 같은 자료로 고르고 같은 자료로 성공을 판정한 **선택 편향**이었다.
> 성능 확증은 사전 고정한 규칙과 선택·적응을 반영한 유효한 설계로 수행한다.
> 라벨을 쓰지만 **후보당 사전 선언된 통계**이므로, 앙상블 구성을 탐색해 최댓값을 고르는 §214식 절차와 구분된다.

[8]
> **적합 분산 보고**: 결정론(`seed std 0.00000`)이 더는 성립하지 않으므로, 모든 비교는 fold 분산과 **적합 분산을 함께** 다룬다. 단일 적합에 대한 per-fold 구간은 실제 불확실성을 과소평가한다(`RU-97` §8 실측: 동일 구성에서 seed만 바꿔 macro 폭 `0.0200`).
> `RU-97` §8은 동일 구성에서 seed만 바꿔 macro 폭 `0.0200`, 대응 Δ가 양방향으로 0을 배제하는 것을 실측했다.

[9]
> | **fold 적합 단가** | **`20.0`초/fold** (5-branch, GPU 단독, 과제 3개 평균 `18.2`\~`22.0`). 한 arm Primary 7(350 fold) `1.94` GPU-h · **2 arm paired 확증 `3.89` GPU-h = 예산의 97%** · `R=4` 2×2 `31.1` GPU-h. 7-branch는 더 비싸므로 이 값은 하한이다. 출처: [fold 적합 비용 보고서](../talks/reports/2026-09-18_foldfit_cost.md) |

[10]

[11]
> **이 절의 값은 소급 적용하지 않는다.** RU-80의 정밀도는 그 저장 예측 비교의 측정값이며 모든 후보의 공통 MDE가 아니다.
> 새 후보는 해당 대응 Δ의 불확실성을 산출한다.
> MDE를 관측 후 성공 컷오프로 쓰지 않는다.

[12]
> 과제 간 sd `0.0185`가 평균 효과의 3배다.
> | 대응 SE (과제 군집 7) | `0.022%p` |

[13]
> | **fold 적합 단가** | **`20.0`초/fold** (5-branch, GPU 단독, 과제 3개 평균 `18.2`\~`22.0`). 한 arm Primary 7(350 fold) `1.94` GPU-h · **2 arm paired 확증 `3.89` GPU-h = 예산의 97%** · `R=4` 2×2 `31.1` GPU-h. 7-branch는 더 비싸므로 이 값은 하한이다. 출처: [fold 적합 비용 보고서](../talks/reports/2026-09-18_foldfit_cost.md) |

[14]
> | **MDE** (α=0.05 양측 · power 0.80 · 군집 t, df=6) | **`0.072%p`** |

[15]
> `eval_v121.sh`가 **학습 설정**을 넘기고 `eval_seal_tasks.sh`가 `model:` 블록을 보고 7-branch로 **조용히 교체**했다. 나머지 다섯도 같다. **arm 이름이 다른 실행들이 서로 다른 구성을 돌린 적이 없다.**
> ### 5-branch 대 7-branch — 판별 불가 (처음으로 실제로 나눠 돌렸다)
> `evaluate_pure.py`에 `configs/baseline/v121_active.yaml`을 직접 넘긴 5-branch 실행은 SMAD4 `0.4426`, PBRM1 `0.5546`이다.

[16]
> 저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.
> 저장된 예측 파일에 provenance가 없어 과거 산출물을 구성에 귀속시킬 수 없다는 점도 그대로다.

[17]
> 과제 간 sd `0.0185`가 평균 효과의 3배다.

[18]
> | **fold 적합 단가** | **`20.0`초/fold** (5-branch, GPU 단독, 과제 3개 평균 `18.2`\~`22.0`). 한 arm Primary 7(350 fold) `1.94` GPU-h · **2 arm paired 확증 `3.89` GPU-h = 예산의 97%** · `R=4` 2×2 `31.1` GPU-h. 7-branch는 더 비싸므로 이 값은 하한이다. 출처: [fold 적합 비용 보고서](../talks/reports/2026-09-18_foldfit_cost.md) |

[19]