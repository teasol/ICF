# 질문
1. 저장된 per-branch margin의 키 수준(과제/fold/브랜치)과 파일 경로는 무엇인가.
2. 공식 집계(Trimmed Mean)의 정확한 정의는 무엇인가 — 브랜치 마진에 대한 표준화·정규화 단계가 포함되는가, 절사 후 평균의 대상은 무엇인가.
3. context 재적합 마진(context-fit margin)이 저장 산출물에 포함돼 있는가.
4. fold별 ridge 계수(coefficient)가 저장돼 있는가.
5. RU-101의 SJ 단독 AUROC ≈`0.49`는 어느 파일에서 산출됐고, fold 수준 분포가 보존돼 있는가.
6. 저장된 per-branch margin의 저장 단위는 fold 수준인가 arm 수준인가, 그리고 fold별 절사 마스크(어느 브랜치가 절사됐는지)가 함께 저장돼 있는가?
7. RU-102의 per-task oracle 상한 `+0.0053`은 어떤 선택 집합(브랜치 on/off인지, DS 강도 6종인지, 다른 것인지) 위에서 계산됐는가?
8. RU-101이 산출한 SJ 단독 AUROC의 per-fold 값과 그 산포(표준편차 또는 분위수)는 저장돼 있는가?
9. RU-97 §8의 seed 변동 macro 폭 `0.0200`은 어떤 구성(학습 파라미터 포함 여부, 어떤 브랜치 집합)에서 측정된 것인가?
10. 저장된 Primary 7 per-branch margin의 파일 경로와 키 스키마(arm 수준 / fold 수준 / 브랜치 수준 구분)는 무엇인가?
11. 저장 산출물에 fold별 Ridge 계수·브랜치 가중치가 기록돼 있는가?
12. `RU-101`의 SJ 단독 AUROC 과제별 정확값과 fold 분포는 무엇인가?
13. context 셔플 null의 설계(셔플 대상, fit 범위, fold 구조 유지 여부)가 문서화된 곳이 있는가?
14. 저장된 per-branch margin 산출물에 fold별 **절사 대상 브랜치**와 **fold별 계수**가 포함되는가?
15. `RU-97` §8의 seed별 macro 산출물이 저장돼 있는가, 그리고 그 구성은 5-branch인가 7-branch인가?
16. `RU-100`의 `+0.0057` 재현에 적합 분산 보고가 포함됐는가?
17. SJ의 7-branch 절사율이 측정된 적 있는가?

# 원문 응답 (검증 전)
[1]
- 저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.
- `evaluate_pure.py`가 저장 예측에 provenance(branch list·가중치·config/code 해시·fold manifest 해시)를 기록하게 됐고
- 재집계 대상은 `v121_ds_sweep` 실행이 저장한 여섯 DS 강도 arm(`m_ds_full, m_ds_f070, m_ds_f050, m_ds_f030, m_ds_f010, m_ds_f000`)이며, 신규 파이프라인 실행이 아니다.

[2]
- | 집계 | Trimmed Mean (최저·최고 1개 절사 후 중앙값 평균) |

[3]
- 구체적으로 게이트 ① 실행 시 저장하는 마진에서 **Context 재적합 마진과 Query 마진의 fold 수준 상관**을 함께 산출한다(저장 마진 오프라인 재집계, GPU 0h).

[4]

[5]
- **7-branch `3/7` 악화 기전 부분 확인(RU-101).** SJ 단독 AUROC이 악화 3과제에서 ≈`0.49`(무정보)·나머지 `0.56`.
- **RU-101**: SJ 단독 AUROC이 7-branch 악화 3과제에서 ≈`0.49`(무정보).

[6]
- 재집계 대상은 `v121_ds_sweep` 실행이 저장한 여섯 DS 강도 arm(`m_ds_full, m_ds_f070, m_ds_f050, m_ds_f030, m_ds_f010, m_ds_f000`)이며, 신규 파이프라인 실행이 아니다.

[7]
- **RU-102**: oracle per-task 상한 Δ `+0.0053`(임계 초과, SE `0.0070` 미만).
- RU-102 oracle 상한 Δ `+0.0053`(임계 초과, SE 미만).

[8]

[9]
- `RU-97` §8 실측: 동일 구성에서 seed만 바꿔 macro 폭 `0.0200`
- `RU-97` §8은 동일 구성에서 seed만 바꿔 macro 폭 `0.0200`, 대응 Δ가 양방향으로 0을 배제하는 것을 실측했다.

[10]
- 저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.
- `logs/`·`predictions/`에 신규 산출물이 생기지 않는다

[11]
- 저장된 예측 파일에도 branch list·가중치·코드 해시가 기록돼 있지 않아 과거 산출물을 특정 구성에 귀속시킬 수 없다.

[12]
- **7-branch `3/7` 악화 기전 부분 확인(RU-101).** SJ 단독 AUROC이 악화 3과제에서 ≈`0.49`(무정보)·나머지 `0.56`.

[13]

[14]

[15]
- `RU-97` §8 실측: 동일 구성에서 seed만 바꿔 macro 폭 `0.0200`

[16]
- `evaluate_pure.py`가 저장 예측에 provenance(branch list·가중치·config/code 해시·fold manifest 해시)를 기록하게 됐고, 5-branch(`sha256 2a2ec33a97ab3e45`) 대 7-branch(`d8c8ae4dca9b8ecf`)를 **같은 잡·같은 세션**에서 Primary 7 × 50 fold로 재실행해 Δ_macro `+0.0057` · 과제군집 CI `[-1.15%p, +2.28%p]`(0 포함) · 악화 `3/7`을 재현했다.
- 협의체 C-20260919-1의 규정대로 **RU-100**이 두 arm을 같은 잡·세션에서 재실행해 `+0.0057`(§4 충족)·CI·악화 `3/7`을 재현하고 provenance와 함께 **D-042를 verified**로 올렸다.

[17]
- §218 §4가 7-branch 절사율을 직접 측정한 결과 신규 후보는 **BS 22.3% / SH 22.3%로 7-branch 균등 기대 28.6%보다 오히려 덜 절사**되었다(같은 표의 기존 브랜치는 CV 78.8% / BM 5.1% / BD 46.6% / QA 13.5% / DS 11.5%).
- 부수 산출로 **신규 직교 브랜치가 우선 절사된다는 통념도 반증**됐다(7-branch에서 `BS` 22.3% / `SH` 22.3%로 균등 기대 28.6% **미만**).