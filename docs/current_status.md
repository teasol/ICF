# Current Status

- **Last Updated**: 2026-09-04 00:30 (KST)
- **Status**: CLEAN
- **Host / Node**: gnode3 (5x NVIDIA RTX A5000, 24GB VRAM)
- **Environment**: uv venv `.venv` | Python 3.12.11 (PyTorch 2.14.0+cu130, Lightning 2.6.5)
- **Active Session / Job**: None
- **비교 기준**: CT 제외 **5-branch** (`CV, BM, BD, QA, DS`) / 공식 기준선 **Primary 7 Macro `0.6171`** (`v121_baseline`)
- **SEAL 10-task hold-out**: 사용자 결정으로 **유보 중** → 아래 모든 판정은 `hold-out 미검증`

### Immediate Next Command
```bash
# §216 착수: CV+BD+DS 후보를 5-fold 이상 조건에서 재검증
.venv/bin/python scripts/analysis/branch_diagnostics.py --tag v121_baseline --redundancy --ablate
```

---

## 2026-09-03 — §214 재현 검증 및 판정 정정, 연구 방향 재설정 (§214-V)

### 1. §214 판정 정정
§214에서 도입한 `adaptive_trimmed` / `hard_gated`를 저장 예측 파일로부터 독립 재계산했습니다.

- **수치는 정확합니다.** 재현값이 문서값과 소수점 4자리 내 일치 (adaptive `0.6205` vs 문서 `0.6204`, hard_gated `0.6191` 일치). 계산 오류나 조작은 없습니다.
- **그러나 승격 기준에 미달합니다.** 두 방식 모두 **sign agreement 4/7** (승격 요건 ≥5/7).

| 과제 | Trimmed (기준) | adaptive_trimmed | hard_gated |
| :--- | :---: | :---: | :---: |
| ARID1A | 0.5530 | 0.5330 **(−2.00%p)** | 0.5752 (+2.23%p) |
| Grade | 0.6774 | 0.6733 **(−0.41%p)** | 0.6489 **(−2.86%p)** |
| KEAP1 | 0.6042 | 0.6168 (+1.26%p) | 0.6088 (+0.46%p) |
| KRAS | 0.7004 | 0.7221 (+2.16%p) | 0.7026 (+0.22%p) |
| SMAD4 | 0.4420 | 0.4713 (+2.93%p) | 0.4904 (+4.84%p) |
| Prog | 0.7882 | 0.7930 (+0.48%p) | 0.7809 **(−0.74%p)** |
| PBRM1 | 0.5561 | 0.5339 **(−2.22%p)** | 0.5266 **(−2.95%p)** |
| **Macro** | **0.6173** | 0.6205 (+0.32%p) | 0.6191 (+0.17%p) |
| **sign agreement** | — | **4/7 ❌** | **4/7 ❌** |

정정 4건 (상세: `docs/history/archive.md` §214-V):
1. "전체 1위 달성" → 비교한 집계 방식 8종 중 1위이며, 승격 기준 미달.
2. `current_status.md`가 상승 3개만 기재하고 **회귀 3건을 누락** → `agent_handoff.md` 불변식 5 **보고 무결성 계약** 신설.
3. "350-Fold 전수 실측" → 신규 실행 아님. §213 저장 마진의 **오프라인 재집계** (22:50 이후 신규 `predictions`·`logs` 산출물 0건).
4. `adaptive_trimmed` 기본값 승격 제안 → **취소**.

기술 부채: `adaptive_trimmed`의 `(c_min <= tau)` 절은 `ratio * c_med`에 흡수되어 사실상 죽은 코드입니다 ($\tau=0.05$와 $0.08$의 macro가 4자리까지 동일).

### 2. 신규 실측 — 문제의 실제 위치
`scripts/analysis/branch_diagnostics.py` (신규) 로 `v121_baseline` 50-fold 전수 재집계.

| 과제 | CV | BM | BD | QA | DS | Oracle | Trimmed | 격차 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| ARID1A | 0.4308 | 0.4990 | 0.6084 | 0.4692 | **0.6193** | 0.6193 | 0.5509 | +0.0684 |
| Grade | 0.6288 | 0.6640 | 0.4665 | 0.6726 | **0.7008** | 0.7008 | 0.6773 | +0.0235 |
| KEAP1 | 0.6154 | **0.6227** | 0.5516 | 0.5729 | 0.5701 | 0.6227 | 0.6041 | +0.0186 |
| KRAS | 0.7122 | 0.6756 | 0.4736 | **0.7341** | 0.6379 | 0.7341 | 0.7004 | +0.0337 |
| SMAD4 | **0.5483** | 0.4212 | 0.5322 | 0.4202 | 0.4283 | 0.5483 | 0.4421 | +0.1061 |
| Prog | 0.7631 | 0.7513 | 0.5956 | **0.7779** | 0.7779 | 0.7779 | 0.7892 | −0.0113 |
| PBRM1 | 0.5041 | 0.5904 | 0.4520 | **0.5923** | 0.5057 | 0.5923 | 0.5553 | +0.0370 |
| **Macro** | | | | | | **0.6565** | **0.6171** | **+0.0394** |

1. **Oracle 격차 +3.94%p**: 과제별 최상 단독 브랜치를 고를 수 있다면 `0.6565`. 집계 함수 조정으로 얻은 +0.32%p보다 **12배 큽니다.** 이것이 실제 미회수 성능입니다.
2. **앙상블이 최상 브랜치를 이기는 과제는 7개 중 1개(Prog)뿐**입니다. 나머지 6개에서 앙상블은 신호를 파괴하고 있습니다.
3. **SMAD4는 구조적 실패**: BM `0.4212`, QA `0.4202`, DS `0.4283` — 3개 브랜치가 50-fold 평균에서 강하게 역상관. 잡음이 아니라 context→query 일반화 방향이 뒤집힌 것입니다.
4. **BD의 과제별 편차가 극단적**: Grade `0.4665` / KRAS `0.4736` / PBRM1 `0.4520`으로 우연 이하인 반면, ARID1A에서는 `0.6084`로 2위.
5. **브랜치 부분집합 31종 전수**: 최고는 `CV+BD+DS` `0.6274`(+1.04%p)이나 sign agreement **3/7**. **집계 8종·부분집합 31종 통틀어 5/7 도달은 0건**입니다.

→ 이는 개별 후보의 실패가 아니라 **Primary 7 벤치마크가 1%p 부근 차이를 분해하지 못한다**는 뜻입니다. `agent_handoff.md` 불변식 3에 **분해능 하한** 조항으로 등록했고, **집계 함수 변형 탐색은 Closed Axis로 종료**했습니다.

---

## 2026-09-04 — §215: SMAD4 역전 원인 규명 → 브랜치 중복성 근본 원인 발견

Phase 1을 수행하다 SMAD4에 국한되지 않는 **아키텍처 수준 근본 원인**에 도달했습니다. 전부 저장 마진 오프라인 재집계이며 GPU를 쓰지 않았습니다. (상세: `docs/history/archive.md` §215)

### 1. SMAD4 역전은 계통적입니다 (Phase 1 판정 완료)
- BM 38/50 fold, QA 38/50, DS 35/50에서 AUROC < 0.5 (이항검정 p = 1.5e-4, 1.5e-4, 3.3e-3).
- fold 독립성 확인: 고유 환자 105명, fold 쌍 Jaccard 중복 평균 0.114, 동일 환자집합 fold 쌍 0건.
- 환자 단위로 재계산해도 동일 (BM 0.4206 / QA 0.4126 / DS 0.4338). 전체 105명 풀에서는 BM **0.3906**, QA **0.3729**, DS **0.3959**.
- → **fold 무작위가 아닌 계통적 역전.** §214-V 판정 기준에 따라 "브랜치 설계 결함" 경로 확정.

### 2. 근본 원인 — 브랜치가 상보적이지 않습니다
| | 유효 랭크 | 명목 대비 |
| :--- | :---: | :---: |
| CV+BM+BD+QA+DS (현 기준선) | **2.26 / 5** | **45%** |
| BM+QA+DS | **1.29 / 3** | 43% |
| CV+BD+DS | 2.53 / 3 | 84% |
| CV+BD | 1.99 / 2 | 100% |

- BM–QA r = **0.90~0.93**, BM–DS 0.65~0.85, QA–DS 0.57~0.80. 셋 다 **동일한 top-32 PCA 부분공간의 1차 모멘트**이므로 구조적으로 중복입니다.
- 나머지와 직교하는 브랜치는 **BD 뿐**(|r| = 0.01~0.16).
- `agent_handoff.md` §2.2 "Six **Complementary** Branches" 전제를 실측 기반으로 정정했습니다.

### 3. 기전 — Trimmed Mean이 독립 신호를 우선 폐기합니다
슬라이드별 min/max로 절사되는 비율 (균등 기대 40%):

| CV | BM | BD | QA | DS |
| :---: | :---: | :---: | :---: | :---: |
| **83.6%** | 13.3% | **62.1%** | 18.9% | 22.1% |

독립 신호를 나르는 CV·BD가 계통적으로 삭제되고, 중복 블록(BM/QA/DS)이 78~87% 생존합니다. 상관된 다수가 분포 중앙을 점유하므로 직교 브랜치는 항상 극단값이 되기 때문입니다. **§213의 "Max-Drop 함정"은 이 현상의 특수 사례였고, §213/§214는 증상을 다뤘습니다.**

### 4. 독립 증거의 수렴
§214-V 부분집합 절제 1위였던 `CV+BD+DS`(0.6274, +1.04%p)는 유효 랭크 효율도 84%로 최고입니다 — 각 상관 군집에서 대표를 하나씩 뽑은 조합입니다. 무차별 성능 탐색과 구조 분석이 독립적으로 같은 답에 도달했으므로, **`CV+BD+DS`는 §214식 평가셋 튜닝의 산물이 아니라 구조적 근거를 가진 후보**입니다. 다만 sign agreement 3/7로 여전히 승격 요건 미달입니다.

### 5. SMAD4 재해석
BM/QA/DS의 역전은 3개의 독립 실패가 아니라 **1개 신호의 실패가 3번 계수된 것**입니다. 오라클 확인(라벨 필요, 배포 불가): 부호 반전 시 SMAD4 Trimmed AUROC **0.4421 → 0.6041**.

---

## 2026-09-04 — §216 라벨 무관 선택 절차 / §217 RM 후보 기각 및 브랜치 분류 체계

### §216 — 중복성은 아키텍처 불변 속성
- 독립 실행 3건(`v121_baseline`, `v121_salience_anchor`, `v120_clean_context_loo`)에서 유효 랭크가 **2.26/5로 소수점 둘째 자리까지 동일**. 특정 실행의 우연이 아닙니다.
- 부수 발견: §213 Salience Anchor는 DS의 중복도를 전혀 바꾸지 못했습니다(BM–DS 0.788 → 0.789). **DS 단독 성능만 올렸을 뿐 앙상블에 새 정보를 더하지 않았습니다.**
- **라벨 무관 선택 절차 확립**: 랭크 효율로 선택 → Primary 7은 사후 확인만. 선택 결과 `CV+BD+DS`(효율 84%)로, §214-V 무차별 탐색 1위와 일치 → 평가셋 튜닝 산물이 아님이 확인되었습니다.
- **판정**: 3/7로 승격 요건 미달이며, 가지치기는 정보를 더하지 않고 재가중할 뿐입니다(최대 Macro 0.6274 vs Oracle 0.6565). `CV+BD+DS`는 **구조적 근거를 갖춘 보류 후보**로 기록하고 5-branch 기본을 유지합니다.

### §217 — RM 후보 기각, 그리고 그것이 드러낸 구조
- **설계**: `sketch_dim=256`인데 BM/QA/DS는 상위 32열만 읽으므로, `basis[:, 32:256]`의 bag-mean(RM)이 직교할 것으로 예상했습니다.
- **절차**: `ICF_RM_SCREEN_ONLY=1`로 앙상블에 넣지 않은 채 마진만 기록하며 350 fold 실행(검증: PBRM1 fold-mean 0.5553으로 기준선과 동일 → 앙상블 무영향 확인).
- **결과: 기각.** max |r| = **0.690 (RM–CV)** > 0.6. 규칙에 따라 **성능은 조회하지 않았습니다**(`rm_screen.py`가 절차적으로 차단). 랭크 효율도 45% → 43%로 하락합니다.
- **원인**: 꼬리 224열은 버려져 있지 않았습니다. **CV가 투영 공간 256차원 전체의 공분산이므로 이미 그 부분공간을 읽고 있었습니다.**

### §217 산출물 — 브랜치 분류 체계
| 부류 | 브랜치 | 상호 상관 | 성격 |
| :--- | :--- | :---: | :--- |
| **위치(Location)** | CV, BM, QA, DS, ~~RM~~ | 0.25~0.93 | 표현이 **어디에 놓이는가** |
| **형상(Shape)** | **BD 단독** | \|r\| ≤ 0.16 | 분포의 **퍼짐/이질성** |

CV는 2차 통계임에도 위치 계열에 묶입니다(0.44~0.69). **프로젝트 역사상 발견된 직교 축은 BD 하나뿐**입니다.

이 절은 **§214식 실패를 절차가 실제로 차단한 첫 사례**입니다. §214였다면 성능부터 보고 macro가 오른 지점을 찾았을 것입니다.

---

## 연구 계획 (개정 — §218 이후)

### Phase A (§218) — 형상 계열 확장 · **유일하게 남은 방향**
- **근거**: 위치 축은 포화되었습니다(BM 사영 / QA 분위수 / DS 재가중 / RM 꼬리사영이 모두 한 계열로 수렴). 유효 랭크를 올리려면 **위치와 무관한 통계**가 필요합니다.
- **후보 방향**: 슬라이드 내 이질성(모드 수·클러스터 분산), 토큰 분포 기하, 순위/순서 통계 등.
- **필수 절차**: 설계 단계에서 위치 계열을 먼저 배제하고, GPU 실행 시 `SCREEN_ONLY`로 마진만 기록해 **max |r| ≤ 0.6 (BD 포함)** 과 **랭크 효율 비하락**을 모두 통과해야 성능을 조회합니다.
- **비용 절감**: 설계 단계 배제가 GPU 실행 후 기각보다 훨씬 쌉니다(§217은 1회 실행 = 5 GPU × 약 20분을 소모했습니다).

### Phase B (§219) — SMAD4 교란 변수 규명 (§214-V Phase 3에서 이월)
- PDA에서 top-32 PCA 1차 모멘트가 SMAD4와 역상관하는 원인(간질 비율 가설). 임베딩 접근 필요.
- **선행 작업 권장**: `test_pathobench.py`가 **context 측 마진도 저장**하도록 확장하면 이후 분포 이동 진단이 GPU 없이 가능해집니다.

### 종료된 축 (Closed)
- 집계 함수 변형(§214-V), In-Episode Context LOO(§212), **평균의 사영·재가중 계열 신규 브랜치(§217)**.

### Blockers & Tech Debt
- 평가 프로토콜 확정: fold 고정 / SEAL은 최종 hold-out(선택 사용 금지) / Primary 7으로만 선택 → **분해능 하한은 영구 제약**이며 라벨 무관 선택 기준이 필수입니다.
- 모든 판정 `hold-out 미검증`.
- `adaptive_trimmed`의 `adaptive_tau`는 무효 인자(죽은 코드).
- RM은 `test_pathobench.py` 평가 경로에만 구현된 스크리닝 전용입니다(`src/models`에는 미통합). 기각되었으므로 통합하지 않으며, 향후 후보의 스크리닝 템플릿으로 남겨둡니다.
- 133개 전 테스트 통과.

_by Claude Opus 5 on gnode3 at 2026-09-04 00:30:00_
