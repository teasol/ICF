# Current Status

- **Last Updated**: 2026-09-05 (KST)
- **Status**: CLEAN (코드/문서 커밋 완료, 137개 테스트 통과) / **GPU 실행 차단 — 노드 이동 대기**
- **Host / Node**: 현재 **nexgem-s1 (8x RTX A5000 23GB, 드라이버 550.127.08)** → **gnode4로 복귀 예정**
- **Environment**: uv venv `.venv` | Python 3.12.11 (PyTorch 2.14.0+cu130, Lightning 2.6.5)
- **Active Session / Job**: None (tmux `agy`/`codex`/`claude-code`는 에이전트 세션이며 학습 작업 아님)
- **Read First**: 아래 `2026-09-05 — 멀티에이전트 연구방향 라운드` 절
- **비교 기준**: CT 제외 **5-branch** (`CV, BM, BD, QA, DS`) / 공식 기준선 **Primary 7 Macro `0.6171`** (`v121_baseline`)
- **SEAL 10-task hold-out**: 사용자 결정으로 **유보 중** → 아래 모든 판정은 `hold-out 미검증`

> 🛑 **[BLOCKER — 2026-09-05 실측] 이 노드에서 GPU를 쓸 수 없다.**
> `.venv`의 `torch 2.14.0+cu130`은 CUDA 13.0 런타임(드라이버 ≥ 580)을 요구하는데 `nexgem-s1`의 드라이버는 **550.127.08 (CUDA 12.4)**이다.
> `torch.cuda.is_available()`이 **False**이며 `device_count()`는 8을 반환하지만 실제 할당에서 `RuntimeError: The NVIDIA driver on your system is too old`로 실패한다.
> 폴백 후보인 `~/miniconda3/envs/BagPFN/bin/python`도 `torch 2.12.1+cu132`로 동일하게 불가하다. `node_env.sh`는 `.venv`를 고르고 `NGPU=5`를 보고하나 둘 다 현재 노드와 맞지 않는다.
> 2026-09-04 19:14까지의 GPU 산출물(`v121_ds_sweep`)은 동일 torch 빌드로 성공했으므로, **노드가 교체된 것이지 패키지가 바뀐 것이 아니다.**
> 해소 경로: (a) CUDA 12.4 대응 torch 재설치, (b) **gnode4 복귀 (선택된 경로)**, (c) nexgem-s1 드라이버 상향. 셋 중 하나 전까지 **350-fold 스윕·SCREEN_ONLY 실행은 집행 불가**하며, 저장 마진 오프라인 재집계와 회귀 테스트(CPU)는 정상 동작한다.

### Immediate Next Command
```bash
# gnode4 도착 후 최초 1회 — GPU 차단이 실제로 풀렸는지부터 확인한다 (아래 BLOCKER 절 참조)
nvidia-smi --query-gpu=name,driver_version --format=csv,noheader | head -1 && \
  .venv/bin/python -c "import torch;print('cuda',torch.cuda.is_available(),'| build',torch.version.cuda);\
import torch as t;x=t.randn(8,8,device='cuda');print('alloc OK',float((x@x).sum())!=0)"
```
`cuda True` + `alloc OK True`가 나와야 Phase 1로 진입한다. 실패하면 torch를 드라이버에 맞춰 재설치할 것.

---

## 2026-09-05 — 멀티에이전트 연구방향 라운드 (§226)

### Completed
- **연구방향 후보 20건 생성·교차 채점.** 7개 에이전트(Claude Opus 5 ×3, Codex `gpt-5.6-sol` ×2, agy `gemini-3.1-pro-high`/`claude-opus-4-6-thinking` ×2)가 동일 브리핑 팩 위에서 독립 제안하고, 세 벤더가 **자사 제안을 제외**하고 교차 채점했다. 결과는 [`docs/research_directions.md`](research_directions.md) (340줄).
- **게이트 ① 도구 결함 수정** (`scripts/analysis/branch_screen.py`). STEP 1의 기준 집합이 `BRANCHES`(공식 5-branch) + 형제 후보뿐이라, **이미 채택된 `SH`(§218)·`SHJ`(§220)와의 상관을 한 번도 재지 않았다.** 후보가 마주한 적 없는 심사를 통과할 수 있는 상태였다. `--adopted`(기본 `m_sh,m_shj`)를 추가해 STEP 1에만 반영했고, `BRANCHES`는 불변식 3에 따라 그대로 두었다. 해당 마진이 없는 태그에서는 경고를 출력한다.
- **낡은 문서 서술 3건 정정 (§226)** — `agent_handoff.md` §2.2, `current_status.md` §215·§217. 아래 Findings 참조.
- 137개 회귀 테스트 CPU 경로 재확인 (exit 0).

### Findings & Decisions
- **Tier 1 채택 권고: `AKS`(평균 23.0) · `MDX`(21.5) · `LID`(20.5).** 셋 다 `shj.py`가 `whitened.norm(dim=1)`로 32D 구름을 1차원 반경으로 붕괴시키며 버리는 성분(각도 / 모드 수 / 국소 내재차원)을 읽는다. §2.2가 지목한 "형상 계열 확장"과 일치하며, CT의 k-means 병목을 피해 전부 슬라이드별 닫힌 형태다.
- **단, 세 건 모두 동일 에이전트(claude_shape) 한 배치 산출이고 자사 채점 제외 구조상 Claude 평가를 받지 않았다.** 제안 품질이 아니라 한 에이전트의 문서 작성 규약 숙련도가 점수에 반영됐을 가능성은 배제되지 않았다. 독립 비교군이 필요하다.
- **채점자 전원이 `promotion_realism`에 0~3점만 부여했고 20건 중 4점 이상은 0건이다.** 세 벤더 누구도 어떤 제안이 macro +1.0%p와 sign agreement 5/7을 동시에 넘으리라 보지 않는다.
- **절차 위반 합의 기각 2건**: `P2-SELECTOR-CEILING`(§224 Oracle 금지 — oracle 상한을 연구 방향 기각 근거로 사용), `P1-RESIDUAL-GATE`(§147·§195 DD 부활).
- **문서 정정 (§226) — 이번 라운드가 드러낸 실제 부채.** 브리핑 팩이 저장소에서 낡은 사실 2건을 옮겨와 일곱 에이전트 전부에 전파했다.
  1. "직교 축은 `BD` 하나뿐" — §217 시점 사실. §218 `SH`(max |r| 0.418)·§220 `SHJ`(0.227) 이후 무효. 현 형상 계열은 **`BD`·`SH`·`SHJ` 3개**.
  2. "Trimmed Mean이 독립 신호를 우선 폐기한다" — **§218 §4가 신규 직교 브랜치로의 확장을 실측 반증**했다(`BS` 22.3% / `SH` 22.3% vs 7-branch 균등 기대 28.6%, "측정 전 예상과 반대였다"). 형상 계열의 병목은 절사가 아니라 **정보량**이다. 균등 기대치는 5/6/7-branch = 40% / 33.3% / 28.6%로 구분할 것.
  두 정정 모두 정본은 `agent_handoff.md` §2.2다.
- **비용 추정은 채점자 재산출값을 쓴다.** 저자 추정과 크게 어긋난 건: `P1-PH` 8h → **10~20h**, `UMLA` 0.5h → **2.5~3.5h**, `SO` 1h → 1.8~2.5h. 반대로 `saf_cov_filtration`은 2.4h → 0.85~1.5h로 과대 추정이었다.
- **기준 단가 정정**: `~45분`은 **CT 포함 구성의 5-GPU 병렬 wall clock**이며 5-branch 단일 GPU 수치가 아니다. 실측은 §219 로그(`logs/v121_sh_variants_orchestrator.log`) 기준 **3,262 GPU-초 ≈ 0.9 GPU-h**(SH 변형 7종 포함).

### Blockers & Open Issues
- **GPU 실행 차단** — 상단 BLOCKER 절. gnode4 복귀로 해소 예정이나 **미검증**이다. 도착 후 Immediate Next Command로 먼저 확인할 것.
- **벤더 간 fatal 판정 충돌 3건 `미해결`** — 임의로 한쪽을 채택하지 않고 양쪽 논거를 병기해 두었다. 사람 판단이 필요하다.
  | 제안 | codex | agy | 쟁점 |
  | :--- | :---: | :---: | :--- |
  | `P3-LIMIT-CURVE` | fatal | 25점(40개 채점값 중 최고) | §214식 평가셋 하이퍼파라미터 선택인가 |
  | `FC` | fatal | 23점 | §217 "평균의 재가중 브랜치" 재포장인가 |
  | `PSW`·`TGW` | 18점 | fatal | §214-V 집계 함수 변형 재포장인가 |
- **적대적 검증 2건 미실행** (세션 한도 오류). `research_directions.md`에 `미검증`으로 명시했다.
- **`LSAK`의 §199 Non-linear KRR 저촉 `미해결`** — `K_int = K̃_L ⊙ K̃_S`는 2차 다항 커널이라 형식상 비선형이나, 유한차원 다항 커널에서 §199 기전이 재현되는지 실측이 없다. GPU 집행 전 규범 판정 필요. (`QJF`는 선형 그람뿐이라 **비저촉으로 판정**했다.)
- 기존 블로커 유지: 과제 특화 활용의 선택 신호 부재(§221·§225), 모든 판정 `hold-out 미검증`, `adaptive_tau` 죽은 코드.

### Next Steps — gnode4 도착 후 실행 계획
GPU가 필요 없는 트랙은 지금도 진행 가능하다. Phase 번호와 비용은 `research_directions.md` §6과 일치한다.

| 단계 | 내용 | GPU | 판정 |
| :--- | :--- | :---: | :--- |
| **Phase 0** | ✅ 완료 — 도구 수정 + 문서 정정 | 0 | — |
| **Phase 0.5** | 미해결 3건 규범 판정 + `LSAK` §199 저촉 판정 + 적대적 검증 2건 재실행 + 선택 편향 교정(codex·agy에 형상 계열 재의뢰) | 0 | 후보 목록 확정 |
| **Phase 1** | `AKS`·`MDX`·`LID`의 **라벨 무관 조기 사망 조건** 검사. 예: `AKS`는 각도 산포 엔트로피 `H_q`가 슬라이드 90% 이상에서 > 0.99이고 편차 0.01 미만이면 "백색화 후 등방 → 각도에 변별 정보 없음"으로 즉시 종료 | 0.30~0.38 | 생존 건만 Phase 2 |
| **Phase 2** | `SCREEN_ONLY=1` 게이트 ①. 오염 검사(SMAD4 0.4421 / PBRM1 0.5553 4자리 일치) 통과 필수. `max\|r\| ≤ 0.6`(**`SH`·`SHJ` 포함**) 및 6-branch eff.rank ≥ **2.72**(2.26/5 = 45.2% 비하락) | 1.50~5.55 | 미달 시 §217 RM 선례대로 **성능 조회 없이 종료** |
| **Phase 3** | 게이트 ②b — 1개 과제에서 50 fold 중 ≥40개 단독 AUROC > 0.5 (이항검정 p < 0.01). 저장 마진 오프라인 재집계 | 0 | §224에 따라 브랜치 간 최댓값 비교 사용 금지 |
| **Phase 4** | §223 통제 비교 — 동일 실행·동일 basis·동일 fold에서 후보만 토글한 paired OFF/ON | 0.8~1.1 / 건 | macro와 `n/7` 병기, 회귀 과제 전량 명시 |

- **총 예산**: 하한 약 1.8 / 상한 약 10.73 GPU-h (기준 단가 0.7~1.0 GPU-h 환산 1.8~15.4회분).
- **사전 예측**: 승격(+1.0%p 및 5/7 동시 충족) 도달 확률은 낮다. 전례가 0건이고 §218이 실측한 병목이 정보량이기 때문이다. 현실적 최선은 `②b 과제 특화 채택 / hold-out 미검증`이다.
- **세 벤더 공통 지적 공백** (아직 아무도 다루지 않음): ① 직교성과 Context→Query 라벨 정보 전이는 별개인데 대부분의 제안이 전자를 후자로 확대 해석한다(§218·§221이 이미 실측), ② 20건 전부가 브랜치 기하 조작에 몰려 UNI2 기저 임베딩 자체의 정보 누락 가능성을 건드리지 않는다, ③ **AUROC라는 순위 기반 지표의 분해능 한계를 우회할 평가지표적 접근이 없다** — 분해능 하한이 영구 제약이므로 가장 근본적인 축이다.

---

## 2026-09-03~04 — §214-V 판정 정정 / §215 브랜치 중복성 (아카이브 이관)

전문은 [`docs/history/archive.md`](history/archive.md)의 `[2026-09-05 아카이브 이관]` 절로 옮겼다. 핵심만 남긴다.

- **§214-V**: `adaptive_trimmed`·`hard_gated`는 수치는 정확하나 sign agreement 4/7로 승격 기준 미달. 집계 8종·부분집합 31종 전수에서 5/7 도달 0건 → **집계 함수 변형 탐색은 Closed Axis**. 보고 무결성 계약 신설.
- **§215**: 5-branch 유효 랭크 **2.26 / 5 (45%)**, `BM`·`QA`·`DS`는 상호 r = 0.57~0.93으로 사실상 한 신호. SMAD4는 그 한 신호가 3번 계수된 계통적 역전(BM 38/50 · QA 38/50 · DS 35/50 fold에서 AUROC < 0.5).
- 두 절의 직교성·절사 서술은 §218·§220 이후 낡았으며, **§226 정정**이 `agent_handoff.md` §2.2에 반영되어 있다. 그쪽을 정본으로 볼 것.

## 2026-09-04 — §216 라벨 무관 선택 절차 / §217 RM 후보 기각 및 브랜치 분류 체계

### §216 — 중복성은 아키텍처 불변 속성
- 독립 실행 3건(`v121_baseline`, `v121_salience_anchor`, `v120_clean_context_loo`)에서 유효 랭크가 **2.26/5로 소수점 둘째 자리까지 동일**. 특정 실행의 우연이 아닙니다.
- 부수 발견: §213 Salience Anchor는 DS의 중복도를 전혀 바꾸지 못했습니다(BM–DS 0.788 → 0.789). **DS 단독 성능만 올렸을 뿐 앙상블에 새 정보를 더하지 않았습니다.**
- **라벨 무관 선택 절차 확립**: 랭크 효율로 선택 → Primary 7은 사후 확인만. 선택 결과 `CV+BD+DS`(효율 84%)로, §214-V 무차별 탐색 1위와 일치 → 평가셋 튜닝 산물이 아님이 확인되었습니다.
- **판정**: 3/7로 승격 요건 미달이며, 가지치기는 정보를 더하지 않고 재가중할 뿐입니다(최대 Macro 0.6274 vs Oracle 0.6565). `CV+BD+DS`는 **구조적 근거를 갖춘 보류 후보**로 기록하고 5-branch 기본을 유지합니다.

### §217 — RM 후보 기각, 그리고 그것이 드러낸 구조
- **설계**: `sketch_dim=256`인데 BM/QA/DS는 상위 32열만 읽으므로, `basis[:, 32:256]`의 bag-mean(RM)이 직교할 것으로 예상했습니다.
- **절차**: `ICF_RM_SCREEN_ONLY=1`로 앙상블에 넣지 않은 채 마진만 기록하며 350 fold 실행(검증: PBRM1 fold-mean 0.5553으로 기준선과 동일 → 앙상블 무영향 확인).
- **결과: 기각.** max |r| = **0.690 (RM–CV)** > 0.6. 규칙에 따라 **성능은 조회하지 않았습니다**(`rm_screen.py`가 절차적으로 차단). 랭크 효율도 45% → 43%로 하락합니다.
- **원인**: 꼬리 224열은 버려져 있지 않았습니다. **CV가 투영 공간 256차원 전체의 공분산이므로 이미 그 부분공간을 읽고 있었습니다.**

### §217 산출물 — 브랜치 분류 체계
| 부류 | 브랜치 | 상호 상관 | 성격 |
| :--- | :--- | :---: | :--- |
| **위치(Location)** | CV, BM, QA, DS, ~~RM~~ | 0.25~0.93 | 표현이 **어디에 놓이는가** |
| **형상(Shape)** | **BD 단독** | \|r\| ≤ 0.16 | 분포의 **퍼짐/이질성** |

CV는 2차 통계임에도 위치 계열에 묶입니다(0.44~0.69). **§217 시점까지 발견된 직교 축은 BD 하나뿐**이었습니다. **[§226 정정]** 이후 §218 `SH`(max |r| = 0.418)와 §220 `SHJ`(max |r| = 0.227)가 형상 계열에 추가되어, 현 시점 형상 계열은 **BD·SH·SHJ 3개**입니다.

이 절은 **§214식 실패를 절차가 실제로 차단한 첫 사례**입니다. §214였다면 성능부터 보고 macro가 오른 지점을 찾았을 것입니다.

---

## 2026-09-04 — §220~§225 요약

### §220 게이트 개정 / SHJ 채택
게이트 ②a(전 과제 단독 > 0.5)를 기존 브랜치에 소급 적용하니 **현행 5개 전부 불통과**(BD는 4/7로 SHJ와 동일)여서, 신규 후보에만 엄격한 이중잣대였습니다. **②b 과제 특화 경로**를 신설하고 SHJ를 채택했습니다.

### §221 Context LOO 재검토 — 진단 확인, 활용 반증
ρ(특징 차원, LOO 팽창) = **+0.667**로 §212의 용량 진단은 확인됩니다. 그러나 무편향인 SHJ조차 fold 수준 ρ = −0.091로, **편향을 없애도 예측력이 돌아오지 않습니다.** §212 폐기는 용량과 무관하게 확정입니다.

### §222 SHJ 통합 / bf16 수치 취약성 수정
`src/models/branches/shj.py` 신설, config·집계·테스트 배선(137개 통과). 통합 중 **bf16 autocast에서 백색화가 오차를 ~100배 증폭**하는 문제를 발견해 float32를 강제했습니다. ARID1A fp32 재검증 0.6403(48/50)으로 §220 채택 유지.

### §223 §213 측정 오류 정정
§213의 "DS Standalone Full" 열이 §212의 v120 6-branch 값이었습니다. **"+7.65%p 폭등"의 실제 값은 +0.43%p**입니다. 보고 무결성 계약에 **통제 비교 의무** 신설.

### §224 Oracle 기준 폐기 (사용자 지적)
"과제별 최상 단독 브랜치"는 ① **상한이 아니고**(Prog에서 앙상블 0.7892 > 최상 0.7779), ② 평가 데이터로 선택해 **승자의 저주로 +0.0063 상향 편향**(split-half 측정)됩니다. 판정 기준에서 배제하고, 게이트 ②b의 "최상 브랜치 능가" 조건을 **fold 재현성(≥40/50)** 으로 교체했습니다. **기존 4개 판정은 그대로 재현**됩니다.

### §225 Subsampling 강도 스윕 — §223 종료 판정 철회
| 유지 패치 | ARID1A | KRAS | PBRM1 | Prog | Macro Δ | ICC |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 100% (full) | 0.6193 | 0.6379 | 0.5057 | 0.7779 | — | — |
| 74.5% (§213 설정) | 0.6236 | 0.6329 | 0.5131 | 0.7750 | +0.0002 | 0.9998 |
| 40.5% | 0.6366 | 0.6232 | 0.5115 | 0.7665 | −0.0016 | 0.9997 |
| **15.0% (앵커만)** | **0.6490** | 0.6000 | **0.4393** | 0.7467 | **−0.0226** | 1.0000 |

**4개 과제에서 완벽한 단조 용량반응**(|ρ|=1.000): ARID1A는 강할수록 상승(+2.97%p), KEAP1·KRAS·Prog는 단조 하락, PBRM1이 최대 손실(−6.64%p).

- **§223의 축 종료 판정을 철회**합니다. 기법이 무효였던 게 아니라 §213의 설정이 너무 약했습니다.
- **신호 B(draw 안정성) 기각**: ICC가 모든 강도에서 0.9996~1.0000으로 판별력이 없습니다. draw를 평균하는 구조상 남은 패치가 수백 개면 어떤 강도에서도 일치합니다.
- **§213의 설계 의도는 사실과 반대**였습니다 — 앵커가 "KRAS 같은 국소 변이를 보호"한다고 했으나, KRAS는 앵커 집중에 따라 완전 단조로 하락합니다.

---

## 연구 교착의 현황 (§225 시점)

> 다음 실행 계획은 이 절이 아니라 상단 `2026-09-05 — 멀티에이전트 연구방향 라운드 (§226)`의 **Next Steps** 표를 정본으로 본다.

**확인된 것**: subsampling에는 실재하는 과제별 이해상충이 있습니다(ARID1A +2.97%p vs PBRM1 −6.64%p). macro는 full 부근이 최대이므로 **전역적 이득은 없고 가치는 과제별**입니다.

**막힌 것**: 이를 활용하려면 과제별 강도 선택기가 필요한데, **가용한 선택 신호가 없습니다.**
- 신호 A(짝지은 context LOO)·C(살리언스 집중도): 기존 실험에서 기각
- 신호 B(draw 안정성): §225에서 기각
- Context LOO 일반: §212·§221에서 확정 폐기

이는 §221의 SHJ 교착과 **동일 구조**입니다 — 과제 특화 이득은 실재하나 라벨 없이 과제를 판별할 수단이 없습니다.

## 종료된 축 (Closed)
집계 함수 변형(§214-V), In-Episode Context LOO(§212·§221), 평균의 사영·재가중 브랜치(§217), BS(§218), 형상통계 차원 확대·로버스트화·왜도/첨도 분리(§219), **draw 안정성 선택 신호(§225)**.
※ Salience Anchor Subsampling은 §223에서 종료했다가 **§225에서 철회**했습니다.

## Blockers & Tech Debt
- **[신규] GPU 실행 차단** — nexgem-s1 드라이버 550.127.08 vs torch cu130 요구 ≥580. 상단 BLOCKER 절 참조.
- **과제 특화 활용의 선택 신호 부재** — §221(SHJ)과 §225(subsampling)가 같은 벽에 막혔습니다.
- **[§226] 벤더 간 fatal 판정 충돌 3건 `미해결`** — `P3-LIMIT-CURVE` / `FC` / `PSW`·`TGW`. 사람 판단 필요. 상단 §226 절의 표 참조.
- **[§226] `LSAK`의 §199 Non-linear KRR 저촉 `미해결`** — GPU 집행 전 규범 판정 필요. `QJF`는 비저촉으로 판정됨.
- **[§226] 적대적 검증 2건 미실행** (세션 한도 오류) — `research_directions.md`에 `미검증` 명시.
- **[§226] Tier 1 3건의 선택 편향 `미해결`** — `AKS`·`MDX`·`LID`가 전부 동일 에이전트 한 배치 산출이며 Claude 자기 채점을 받지 않았다. 독립 비교군 필요.
- 평가 프로토콜: fold 고정 / SEAL은 최종 hold-out(선택 사용 금지) / Primary 7으로만 선택.
- 모든 판정 `hold-out 미검증`.
- §217~§221의 SHJ 수치는 bf16 경로 산출값으로 fp32와 최대 0.5%p 차이날 수 있습니다.
- `adaptive_trimmed`의 `adaptive_tau`는 무효 인자(죽은 코드).
- 137개 전 테스트 통과 (2026-09-05 CPU 경로 재확인, exit 0).
- **[§226] 게이트 ① 도구 결함 수정**: `branch_screen.py`가 이미 채택된 `SH`(§218)·`SHJ`(§220)와의 상관을 측정하지 않고 있었다. `--adopted`(기본 `m_sh,m_shj`)를 추가해 STEP 1 기준 집합에 포함시켰다. `BRANCHES`(공식 5-branch 비교 기준)는 불변식 3에 따라 변경하지 않았다. 해당 마진이 없는 태그에서는 경고를 출력한다(무단 통과 방지).

_by Claude Opus 5 on nexgem-s1 (→ gnode4 복귀 예정) at 2026-09-05_
