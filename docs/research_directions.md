# ICF 연구 방향 후보 (7개 에이전트 20건 제안 · 3개 벤더 교차 채점 종합)

- 작성 시점: 2026-09-05
- 기준선: v121 5-branch (`CV, BM, BD, QA, DS`), Primary 7 Macro fold-mean AUROC **0.6171**, 승격 기준 sign agreement ≥ 5/7 (현행 기준선은 비교 원점이므로 sign agreement 정의 대상 아님)
- 채점 구조: 제안 20건을 3개 벤더(claude, codex, agy)가 교차 채점하되 **자사 제안은 자기 채점에서 제외**했다. 따라서 제안 1건당 점수는 2개다. 만점은 30점(novelty 5 / mechanism_rigor 5 / gate1_credibility 5 / promotion_realism 5 / cost_efficiency 5 / falsifiability 5).
- 모든 판정은 `hold-out 미검증`이다. SEAL 10-task는 유보 상태이며 본 문서의 어떤 항목도 SEAL로 확인되지 않았다.
- **적대적 검증 2건이 세션 한도 오류로 실행되지 못했다.** 어느 제안에 대한 반대 심문이 결여되었는지는 입력 자료에 기록이 없다. 아래 순위는 20건 중 2건분의 적대 심문이 빠진 상태에서 산출된 것이며, 이 결손은 `미검증`으로 남는다.
- 비용은 전부 **채점자 재산출값**을 쓴다. 저자 자가 추정과 재산출이 다른 경우 양쪽을 병기한다. 재산출이 존재하지 않는 건은 `재산출 부재`로 명시한다. 기준 단가는 Primary 7 스윕 1회(350 fold)를 **0.7~1.0 GPU-h**로 폭을 두어 표기한다. 근거는 두 가지다 — §214-V가 기록한 CT 제외 5-branch의 **5-GPU 병렬 12분**(≈1.0 GPU-h)과, §219 SH 변형 실행 로그(`logs/v121_sh_variants_orchestrator.log`, 12:21:07~12:36:56)의 **과제별 소요 합계 3,262 GPU-초 ≈ 0.9 GPU-h**(SH 변형 7종 포함, wall clock 949초)다. 같은 §214-V의 `~45분`은 **CT 포함 구성의 5-GPU 병렬 wall clock**이며 5-branch 단일 GPU 수치가 아니므로 기준 단가로 쓰지 않는다. **하한 0.7의 근거**: 두 실측 앵커는 각각 1.0과 0.906으로 둘 다 0.9 이상이며, 0.7은 실측값이 아니라 §219 값이 SH 변형 7종 arm을 함께 산출한 상한이라는 점을 근거로 순수 5-branch가 더 쌀 것으로 본 **추정 하한**이다. 실측 앵커만 쓸 경우 단가는 0.9~1.0이며, 예산표는 이 경우 하한이 약 1.3배로 올라간다.

---

## 1. 요약

20건은 **하나의 축으로 수렴**했다. `all_proposals.json` 20건을 직접 재집계하면, §2.2가 유일하게 열어둔 "형상 계열 확장"에 놓인 것이 **12건**이다(나머지 8건은 형상 브랜치가 아니다 — UMLA·PSW·TGW의 앙상블 라우팅·가중, QJF·LSAK의 커널 통합, P1-RESIDUAL-GATE·P2-SELECTOR-CEILING·P3-LIMIT-CURVE의 게이트·진단). 그 12건 중 **8건**이 슬라이드 토큰 구름의 고차 기하를 읽는 동일 계보다 — 각도(AKS), 모드 수(MDX·MM), 국소 차원(LID), 그래프 스펙트럼(sw_dgs), 특성함수(P3-RFF), 최적수송(P2-SW), 위상(P1-PH). 나머지 4건(SO, sbca_logcorr, saf_cov_filtration, FC)은 공분산·평균 도함수 계열이다. 여덟 건 전부가 슬라이드 자체 백색화를 쓰는 것은 아니다 — MM은 슬라이드 내부 정규화가 없고, P2-SW의 백색화는 슬라이드 자체가 아니라 Context 전역 통계다.

두 번째 수렴은 절차 위생이다. 제안서 본문 문자열 기준 재집계는 **`SCREEN_ONLY` 선행 명시 14건 / 게이트 ②b 사전 선언 15건 / §223 통제 비교 언급(문자열 `paired`·`동일 실행`·`통제 비교`·`COMPARE=1`·`§223` 중 하나 이상) 9~12건**(단일 판정 문자열로는 재현되지 않아 폭으로 표기한다)이며, **세 가지를 모두 명시한 것은 4건**(FC, sbca_logcorr, sw_dgs, saf_cov_filtration)뿐이다. 초안 단계의 "16건이 셋을 스스로 걸었다"는 집계는 재집계로 확인되지 않아 폐기한다.

**갈라진 축은 세 가지다.** (i) 교착의 진단: 다수는 "선택 신호 부재"를 전제로 우회로를 설계했으나, 3건(P2-SELECTOR-CEILING, P3-LIMIT-CURVE, P1-RESIDUAL-GATE)은 전제 자체를 반증 대상으로 삼았다. 이 중 **2건(P2-SELECTOR-CEILING, P1-RESIDUAL-GATE)만 양쪽 벤더 합의로 절차 위반 기각**되었고, **P3-LIMIT-CURVE는 벤더 간 판정이 엇갈려 `미해결`**이다(§4.2). (ii) 게이트 ①의 성격: "통계가 서로의 함수가 아님"을 구조적으로 증명한 건(AKS)과 경험적 기대에 머문 건(대다수)이 갈렸다. (iii) 승격 현실성: 채점자 전원이 promotion_realism에 0~3점만 부여했고, 20건 중 4점 이상은 0건이다. 즉 세 벤더는 **어떤 제안도 macro +1.0%p와 sign agreement 5/7을 동시에 넘을 것으로 보지 않는다.**

---

## 2. 채택 권고 (Tier 1)

세 벤더 교차 평균 상위이면서 fatal 판정이 하나도 없는 건은 **AKS(23.0), MDX(21.5), LID(20.5)** 세 건이다. codex가 지목한 상위 3건(AKS, LID, MDX)과 정확히 일치하고, agy 상위 3건 중 2건(LID, MDX)과 겹친다.

> **선택 편향 경고.** 세 건은 모두 동일 에이전트(claude_shape)의 한 배치에서 나왔고, 자사 채점이 제외된 구조상 이 세 건은 claude의 자기 평가를 받지 않았다. 세 건이 상위를 점유한 것은 제안 품질의 증거인 동시에 **한 에이전트의 문서 작성 규약 숙련도가 점수에 반영된 결과**일 수 있다. 이 가능성은 배제되지 않았다.

### 2.1 AKS — 백색화 토큰 구름의 각도·4차 텐서 스펙트럼 (평균 23.0, codex 23 / agy 23)

**무엇을 하는가.** 슬라이드 bag `X ∈ R^{N×1536}`을 Context 전용 PCA 상위 32열로 사영해 `P`를 얻고, 슬라이드 자체 평균으로 중심화한 `C`의 공분산 `S = CᵀC/N`을 float64 eigh로 분해해 `Z = (C V) Λ^{-1/2} ∈ R^{N×32}` (`ZᵀZ/N = I`)로 백색화한다. 여기서 두 개의 32×32 행렬을 만든다. 하나는 각 토큰을 단위구로 사영한 방향 산포 행렬 `M = UᵀU/N` (`u_i = z_i/‖z_i‖`)로 **반경을 완전히 버린다**. 다른 하나는 4차 모멘트 텐서 `B = (Z ⊙ r²)ᵀZ/N`이며 `p ← p/p.sum()`으로 trace를 나눈다. 저자는 이를 "`tr(B) = E‖z‖⁴`가 곧 SHJ의 반경 첨도이므로 겹치는 성분의 명시적 제거"라고 서술하나, `src/models/branches/shj.py`의 첨도 특징은 **표준화 초과첨도**(`standard.pow(4).mean() − 3`)이지 원시 4차 모멘트 `E[r⁴]`가 아니다. 따라서 trace 제거는 SHJ 성분의 **근사적** 제거이며 구조적 보장이 아니다(`미검증`). 최종 특징은 두 스펙트럼 `q, p`의 정규화 엔트로피·최대값·participation ratio·조건수·Frobenius 이방성 8D이며, 무차원·회전 불변이다. 다만 조건수 `log(q_max/q_min)`은 상계가 없으므로 저자의 "전부 유계" 주장은 성립하지 않는다 — `q_min` 클램프 1e-8은 수치 방어이지 유계성 보장이 아니다. 판독은 기존 `solve_kernel_ridge(linear, λ=1.0)`을 그대로 쓴다.

**왜 지금인가.** 저장소 실측 근거는 두 가지이며, 둘 중 하나는 초안보다 약하게 읽어야 한다.
첫째, **§217 시점 기준으로 직교 축은 BD 하나뿐이었고**(|r| ≤ 0.16), 이후 §218에서 SH(max |r| = 0.418), §220에서 SHJ(전 과제 최대 |r| = 0.227)가 형상 계열로 추가되었다. 즉 "직교 축은 BD 하나뿐"은 **§217 시점 사실**이지 현 시점 사실이 아니다. 현 시점 형상 계열은 BD·SH·SHJ 3개이며, 직교 축을 늘린 경로가 지금까지 전부 형상 계열이었다는 점과 §2.2가 남긴 유일한 열린 방향이 형상 계열 확장이라는 점이 AKS의 위치를 지지한다.
둘째, AKS가 게이트 ②b 1순위로 지목한 SMAD4는 v121_baseline 50-fold에서 **위치 계열 중 BM·QA·DS 세 브랜치가 계통적으로 역전된 유일한 과제**다(BM 0.4212 / QA 0.4202 / DS 0.4283, 전체 105명 풀 기준 BM 0.3906 / QA 0.3729 / DS 0.3959). §215가 계통적 역전으로 판정한 것도 이 세 브랜치뿐이다.
**단, "위치 계열이 통째로 역전되고 형상 계열만 정방향"이라는 서술은 성립하지 않는다.** 같은 위치 계열인 CV는 SMAD4에서 **0.5483으로 정방향이자 SMAD4의 최상 단독 브랜치**이며, 형상 계열의 BD 0.5322·SH 0.5434보다 높다. SMAD4에서 실측된 것은 위치 계열 전체의 실패가 아니라 "동일 top-32 1차 모멘트 신호 1개의 실패가 세 번 계수된 것"(§215)이고, 2차 통계인 CV와 형상 계열이 함께 살아남았다는 사실이다. 따라서 AKS의 SMAD4 사전 등록 근거는 약화된다 — 남는 것은 "1차 모멘트가 실패하는 과제에서 비(非)1차 통계가 상대적으로 유리하다"는 약한 형태이고, **형상 계열이 CV보다 유리하다는 근거는 없다.** 사전 등록 자체는 유지하되 근거 등급을 `실측 지지`가 아니라 `약한 정황`으로 내려 기록한다.

**게이트 ① 통과 근거 — 부분적으로 구조적 보장.** 구면대칭 분포족 전체에서 `M = I/32`, `B ∝ I`가 되어 AKS의 8개 특징이 **항등적으로 상수**가 된다. SHJ는 같은 분포족 위에서 자유롭게 변하므로 AKS는 SHJ의 함수가 아니다. 역방향도 성립한다(반경 고정·방향만 변형 시 SHJ 불변, AKS 가변). BD에 대해서는 백색화가 2차 모멘트를 항등행렬로 만든 뒤 계산하므로 BD가 읽는 성분이 정의상 제거된다. 이는 §217 RM의 "꼬리는 안 읽힐 것"류 기대와 급이 다르다.
그러나 **보장되는 것은 "특징이 서로의 함수가 아니다"까지이고, 게이트 ①이 요구하는 것은 "Dual Ridge를 통과한 마진의 |r| ≤ 0.6"이다.** 두 명제 사이에는 논리적 간극이 있으며(§5-(a) 참조), codex는 정확히 이 지점에서 "4차 이방성이 실제 데이터에서 BD와 동조하거나 무정보일 가능성"을 남겼다. 따라서 통과 근거는 **구조적 보장 + 미검증 간극**으로 기록한다.

**재산출 GPU 비용.** 저자 자가 추정 2.5 GPU-h. **채점자 재산출 부재** — codex·agy 모두 cost_efficiency 4점을 부여했으나 재산출 수치를 산출하지 않았다. 저자 명세상 추가 연산은 슬라이드당 32×32 eigh 2회 + `[32,N]×[N,32]` matmul 2회이고 백색화는 SHJ가 이미 수행 중이므로, §219가 SH 변형 7종을 한 실행에서 wall clock 949초·**과제별 소요 합계 3,262 GPU-초**로 완주한 실측 전례에 비추어 스윕당 오버헤드가 기준선의 수 %를 넘을 구조는 아니다. 다만 이 판단은 채점자 재산출이 아니라 본 문서의 추론이므로 `미검증`이다. 집행 전 1과제 실측 처리량을 먼저 확보한다.

**라벨 무관 조기 사망 조건.** 각도 산포 스펙트럼 엔트로피 `H_q`가 전체 슬라이드의 90% 이상에서 > 0.99이고 슬라이드 간 표준편차가 0.01 미만이면, UNI2 토큰 구름은 백색화 후 32D에서 사실상 등방이며 각도 자유도에 변별 정보가 없다. 라벨을 전혀 쓰지 않으므로 1과제 스크린 직후 즉시 판정 가능하다. 부수 조건으로 `m_akd`(각도만)와 `m_akf`(4차만)의 |r| > 0.9이면 8D 설계가 중복이므로 4D로 축소 재판정한다.

### 2.2 MDX — 다봉성 깊이 프로파일 (평균 21.5, codex 20 / agy 23)

**무엇을 하는가.** 상위 8개 주성분 방향에 사영한 `T = C V[:, :8]`의 각 열을 자체 평균·표준편차로 표준화해 위치·척도 불변을 확보한 뒤, 고정 격자(G = 257 bins, 범위 [−4, 4])에서 `scatter_add`로 히스토그램을 만들고 데이터 무관 고정 대역폭 사다리 `b ∈ {0.15, 0.25, 0.40, 0.60}`의 가우시안 커널로 conv1d 평활한다. 국소 최대 판정으로 봉우리 수 `K_{j,b}`를 세고, 상위 두 봉우리 사이 최솟값으로 **계곡 깊이 `D = 1 − v/p₂ ∈ [0,1]`**을 정의한다. 최종 8D는 대역폭별 최대 계곡 깊이 4개, 평균 깊이, 평균 모드 수, 임계 대역폭 지수, 다봉 방향 비율이다. 대역폭·격자·범위가 전부 상수이므로 데이터 의존 선택이 없고 결정론이 유지된다.

**왜 지금인가.** §219가 닫은 것은 모멘트 통계의 차원 확대·로버스트화·왜도/첨도 분리이며, MDX의 논거는 **초과첨도가 다봉성과 저첨도 단봉성을 구분하지 못하는 다대일 사상**이라는 반례에 있다(균등분포와 성분 분리 3.7σ 이봉 혼합이 둘 다 −1.2). 이 반례가 참이면 §219의 종료가 다봉성까지 포괄하지 못한다. 게이트 ②b 1순위로 지목한 Grade는 v121_baseline에서 위치 계열이 전부 정상 작동하면서(BM 0.6640 / QA 0.6726 / DS 0.7008) BD만 0.4665로 우연 이하인 과제다 — 즉 Grade에는 형상 정보가 전역 스펙트럼 형태로는 없고, 모드 구조가 남은 후보라는 것이 사전 등록의 근거다.

**게이트 ① 통과 근거 — 기대.** 축퇴 반례는 "모멘트가 다봉성을 결정하지 못한다"는 일방향 명제이고, "다봉성이 모멘트와 비상관"을 함의하지 않는다. 실제 데이터에서 다봉이 희소하면 특징은 상수에 가까워지고, 그 경우 |r|은 낮아지지만 정보량 게이트에서 §218 BS와 같은 "직교하나 무정보" 판정을 받는다. 두 채점자 모두 이 위험을 지적했다(codex: KDE 모드의 단봉 축퇴, agy: 무정보 브랜치 전락). **구조적 보장이 아니라 데이터 조건부 기대**로 기록한다.

**재산출 GPU 비용.** 저자 자가 추정 2.0 GPU-h. **채점자 재산출 부재**(codex·agy 모두 cost_efficiency 4). 참고로 동일 계열인 branch_token_multimodality(MM, 1D PCA-KDE)에 대해서는 claude가 저자 1.5 → **2.5~5 GPU-h**로 재산출했으며, 초과 요인은 대역폭·격자·피크 임계 미고정으로 인한 재스윕이었다. MDX는 이 세 값을 전부 상수로 사전 고정했으므로 그 재산출의 초과 요인이 구조적으로 제거되어 있으나, MM 재산출값이 MDX 비용의 **보수적 상한**임은 부인되지 않았다.

**라벨 무관 조기 사망 조건.** `b = 0.25`에서 (슬라이드 × 8방향) 쌍의 95% 이상이 `K = 1`(단봉)이면 UNI2 표현공간의 상위 주성분 방향에 모드 구조가 경험적으로 존재하지 않는다. 1과제 50 fold, 저자 추정 0.11 GPU-h로 판정 가능하다. 이것이 관측되면 §2.2의 "모드 수" 하위축을 닫는 근거가 되고, 동시에 CT가 k-means로 얻으려던 정보가 애초에 희박했다는 독립 증거가 된다. 부수 조건: 계곡 깊이 특징의 슬라이드 간 표준편차 < 0.02, 격자 대조군(G = 129)과의 |r| < 0.9.

### 2.3 LID — 국소 이웃 스케일링 프로파일 (평균 20.5, codex 20 / agy 21)

**무엇을 하는가.** AKS와 동일한 `_whiten()` 헬퍼로 아핀 불변 좌표 `Z ∈ R^{N×32}`를 얻고, 결정론적 스트라이드로 m = 4096 토큰을 뽑아 512행 블록 단위 정확 k-NN(topk 21)으로 `r₁ ≤ … ≤ r₂₀`을 구한다. 특징 8D는 전부 **거리의 비**다: TwoNN 국소 내재차원 2종(median 로버스트 / MLE), 스케일 5와 20의 ID, 그 차 `ΔID`(다중 스케일 서명), 중심화 `log r₁₀`의 IQR과 P90−P10(국소 밀도 이질성), 국소/전역 스케일 비. 거리비이므로 척도 불변이고 백색화 덕에 아핀 불변이다.

**왜 지금인가.** BD는 투영 공분산의 고유값 스펙트럼을, SHJ는 **슬라이드 자체 평균·공분산으로 백색화한 뒤의 반경 분포 형상**을 읽으며(`src/models/branches/shj.py`: `centred = proj − proj.mean(0)` → 자체 공분산 eigh 백색화 → `whitened.norm(dim=1)`) **둘 다 지지집합의 국소 차원에는 항등적으로 무감각하다**. CT가 k-means로 접근했다가 k-means 병목(태스크당 8~12분)으로 §214-V에서 비교 대상에서 배제된 "클러스터 구조" 정보를 사전·반복 없이 얻는 경로다. 게이트 ②b 2순위로 지목한 ARID1A는 §225에서 subsampling 강도에 단조 상승한 유일한 과제(+2.97%p, |ρ| = 1.000)이며, 이는 신호가 소수의 국소 조밀 영역에 집중되어 있다는 실측이고 특징 [6][7]이 그 성질을 직접 측정한다.

**게이트 ① 통과 근거 — 기대.** "국소 차원과 전역 스펙트럼은 다르다"는 논거는 굽은 다양체 반례에 기반한 이론적 주장이며 구성적 보장이 아니다. 두 채점자 모두 별도 위험을 지적했다 — agy는 **토큰 수(슬라이드 크기)에 의한 대리 변수화**를, codex는 4096² 거리행렬을 8만 회 이상 반복하는 비용 누락을 지적했다. 저자는 크기 대리변수화를 falsifier 5(특징과 log N의 |r| > 0.5면 해당 특징 제거)로 스스로 걸었으나, 이는 사후 제거 규칙이지 사전 보장이 아니다.

**재산출 GPU 비용.** 저자 자가 추정 3.0 GPU-h. **채점자 재산출 부재이나, 두 채점자 모두 cost_efficiency를 2점으로 낮추고 비용 신고를 신뢰할 수 없다고 명시**했다(codex: "30초 주장은 신뢰할 수 없습니다"). Tier 1 세 건 중 유일하게 비용 신고에 명시적 불신이 붙은 건이다. 동일 계열의 실측 앵커는 sw_dgs 재산출뿐이며(1024점 coreset + 라플라시안 고유분해 기준 낙관 2.5 / 중앙 4.5~5.8 / 비관 11~13 GPU-h per 스윕), LID는 고유분해가 없고 kNN만 하므로 그보다 가볍지만 m = 4096으로 sw_dgs coreset(1024점)의 4배 규모 점집합을 다룬다. 따라서 **집행 전 1과제 실측 처리량 확보를 필수 선행 조건으로 건다.** 그 값 없이 350-fold 전량 스윕을 승인하지 않는다.

**라벨 무관 조기 사망 조건.** 슬라이드 간 `ID_TwoNN` 표준편차 < 0.2차원이면 모든 토큰 구름이 같은 국소 차원을 가지며 변별 자유도가 없다(1과제 50 fold, 저자 추정 0.13 GPU-h). 전체 슬라이드의 95% 이상에서 `|ID₂₀ − ID₅| < 0.3`이면 다중 스케일 구조 부재로 기각한다. 부분추출 강건성(m = 4096 vs 1024 마진 |r| < 0.8)과 크기 대리변수화(특징-log N |r| > 0.5)도 라벨 없이 판정된다.

### 2.4 Tier 1 공통 — 게이트 통과와 승격 사이의 간극

세 건 모두 게이트 ②b(1개 과제 40/50 fold)까지는 도달 가능성이 있으나, **최종 승격(macro Δ ≥ +1.0%p 및 sign agreement ≥ 5/7 동시 충족)의 전례는 0건**이다. 실측 전례는 SH +0.29%p (sign agreement 4/7), SHJ +0.32%p (sign agreement 3/7), 브랜치 부분집합 31종 중 최고인 CV+BD+DS가 +1.04%p (sign agreement 3/7)이며, 집계 8종·부분집합 31종 전수에서 5/7 도달은 0건이다.

**병목을 절사로 귀속하는 설명은 저장소가 이미 실측 반증했다.** §215가 관측한 절사 편중(CV 83.6%, BD 62.1% vs BM 13.3% / QA 18.9% / DS 22.1%, **5-branch 균등 기대 40%**)은 **기존 5개 브랜치에서 관측된 현상**이며, 신규 직교 브랜치로 확장해서는 안 된다. §218 §4가 7-branch 절사율을 직접 측정한 결과 신규 후보는 **BS 22.3% / SH 22.3%로 7-branch 균등 기대 28.6%보다 오히려 덜 절사**되었다(같은 표의 기존 브랜치는 CV 78.8% / BM 5.1% / BD 46.6% / QA 13.5% / DS 11.5%). archive.md는 **"'신규 직교 브랜치가 절사된다'는 설명은 성립하지 않는다 (측정 전 예상과 반대였다)"** 고 명시한다. 균등 기대치는 브랜치 수에 따라 **5-branch 40% / 6-branch 33.3% / 7-branch 28.6%** 로 구분해 읽어야 하며, 5-branch의 40%를 6·7-branch 논의에 옮겨 쓰는 것 자체가 오류다. 초안 단계에서 이 기전 위에 세웠던 "직교할수록 절사되는 자기모순" 논거는 철회한다.

**§218이 같은 절에서 내린 결론은 다른 것이다** — "SH·BS가 나르는 독립 정보의 양 자체가 적다", 즉 **형상 계열의 병목은 절사가 아니라 정보량**이다. 이 진단은 §218 §5의 판정(BS는 "직교하나 무정보"로 기각, SH는 7/7 과제에서 우연 이상이나 macro +0.29%p·sign agreement 4/7로 승격 불가)과 §218 §3의 관측(랭크 효율 45% → 50%로 올려도 macro 무개선, Oracle 상한 0.6565 무이동)에 일관된다. 따라서 Tier 1 3건에 대해 예측해야 할 실패 양식은 "직교해서 잘려나간다"가 아니라 **"직교하되 나르는 정보가 분해능 하한을 넘지 못한다"**이다. 세 건의 조기 사망 조건이 전부 정보량·자유도 부재(등방성, 단봉 축퇴, 국소 차원 무변별)를 겨냥하고 있는 것은 이 진단과 정합적이며, 이 점은 세 건의 강점으로 기록한다.

따라서 Tier 1의 현실적 목표는 승격이 아니라 **②b 과제 특화 채택 + 기전의 라벨 무관 반증**이다. 세 건 모두 조기 사망 조건이 라벨 없이 1과제 규모로 판정 가능하다는 점이 채택 근거이며, 이는 "값싸게 죽일 수 있다"는 성질이지 "성공 확률이 높다"는 뜻이 아니다.

### 2.5 Tier 1 공통 — 불변식 1·2·3 하드 트리거 점검

두 채점자(codex·agy)는 Tier 1 3건에 대해 `fatal = false`만 기록하고 `fatal_reason` 필드를 **비워 두었다**(`norm_score_codex.json` / `norm_score_agy.json`에서 확인). 즉 하드 트리거 점검 기록이 채점 원본에 없다. 아래는 제안서 명세와 저장소 코드를 대조한 **본 문서의 점검**이며 실행으로 확인된 것이 아니므로 전부 `미검증`이다. 점검 항목은 claude 채점(`norm_score_claude.json`)이 다른 제안 11건에 적용한 하드 트리거 목록과 같다.

| 후보 | 불변식 1 (Zero Leakage) | 불변식 2 (라벨 반대칭) | 불변식 3 (결정론) |
|---|---|---|---|
| AKS | **통과 예상** — basis는 Context 전용 within-slide PCA이고 백색화는 **슬라이드 자체** 평균·공분산이므로 Query가 basis·토큰 사전·정규화 통계에 개입하지 않는다. readout `solve_kernel_ridge`의 표준화가 Context 전용임은 `src/models/common/solvers.py`에서 확인된다 | **통과 예상** — 8D 특징이 전부 라벨 무관이고 class-balanced Dual Ridge 해가 `t = 2y−1`에 선형이므로 `y→1−y`에서 마진이 정확히 반전된다 | **통과 예상** — 난수·k-means 없음. eigh 고유벡터 부호 모호성은 `M → DMD`, `B → DBD`로 **고윳값이 불변**이라 특징이 부호 선택에 무관하다. §222 전례대로 `autocast(enabled=False)` fp32 강제를 명세에 포함 |
| MDX | **통과 예상** — 동일 구조(Context 전용 basis + 슬라이드 자체 표준화 + Context 전용 readout 표준화) | **통과 예상** — 동일 | **조건부** — 대역폭·격자·범위가 전부 상수라 데이터 의존 선택이 없고, 고유벡터 부호 반전은 밀도의 좌우 반전일 뿐이라 봉우리 수·계곡 깊이가 불변이다. 그러나 **고윳값 축퇴 시 상위 8개 고유벡터가 회전**하면 특징이 흔들린다. 저자는 인접 고윳값 비 < 1.01 발생 빈도 로깅과 5% 초과 시 4방향 강등을 걸었으나 이는 사후 대응이지 결정론 보장이 아니다 |
| LID | **통과 예상** — 동일. 변형 `m_lids`는 슬라이드 자체 고유기저를 쓰므로 fold 독립이라 누수 경로가 더 짧다 | **통과 예상** — 동일 | **조건부** — 부분추출이 `idx = (arange(m) · N/m).long()` 고정 스트라이드라 난수가 없고 DS 브랜치가 이미 같은 방식을 쓴다. 그러나 `topk` 동점 처리와 `r₁ = 0`인 중복 토큰의 처리가 미명세다(저자는 중복 비율 로깅만 건다). 집행 전 동점 규칙을 사전 선언할 것 |

세 건 모두 **불변식 6(fold 고정)·7(SEAL 미사용)** 위반 경로는 없다. **불변식 9(Oracle 금지)** 에 대해서는, 세 건이 인용한 게이트 ②b가 §220 개정판이며 그 첫 조건("해당 과제에서 기존 최상 단독 브랜치를 능가")은 브랜치 간 비교를 포함하나 §220이 채택 기준으로 명문화한 조항이므로, §224가 금지한 "Oracle 값을 상한·미회수 성능·연구 방향 판정 근거로 사용"과는 구분된다. 오염 검사(SMAD4 0.4421 / PBRM1 0.5553이 v121_baseline과 4자리 일치)도 세 건 모두 명시했다.

---

## 3. 조건부 후보 (Tier 2)

재설계가 선행되어야 심사가 성립하는 건이다. 재설계 없이 GPU를 집행하면 결과 해석이 불가능하다.

### 3.1 saf_cov_filtration — 살리언스 형상 여과곡선 (평균 19.0, claude 20 / agy 18)

살리언스 여과 전 구간의 형상 변화율을 하나의 브랜치로 만들어 강도 선택 자체를 회피한다. 커브(용량반응 경로)를 descriptor로 삼은 것은 20건 중 유일하다.
**고쳐야 통과 가능한 것**: (a) 여과 순서를 정의하는 `u_j = Σ_k assignment_jk · s_k`가 DS가 쓰는 바로 그 살리언스 장이고, 3개 거리 중 하나가 정규화 고유값 JS 거리로 BD와 동일 대상의 스펙트럼 통계다 — 기존 5개 중 2개를 통과해 읽으므로 BD·DS와의 분리 논거를 "level이 아니라 derivative"라는 기대에서 구조적 논거로 교체해야 한다. (b) 최종 특징 차원·ridge λ·표준화·principal angle 부분공간 차원·불균등 간격 기울기 정규화·`u_j` 동점 처리가 미명세다. (c) f = 0.15에서 패치 수가 적은 슬라이드는 32×32 공분산이 준특이가 되어 행렬 log가 불안정해지고, 그 불안정성이 슬라이드 크기와 상관되면 새 교란 변수를 만든다(εI만으로 불충분).
**비용**: 저자 2.4 → claude 재산출 **1.7~3.0 GPU-h**(SCREEN_ONLY 1회 0.85~1.5, paired arm은 전처리 공유로 2배가 아니라 약 1.1배). 저자 추정은 과대 쪽이다. 집행은 SCREEN_ONLY 1회분만 우선 승인한다.

### 3.2 sbca_logcorr — 슬라이드 균형 형상기저와 Log-Correlation (평균 18.5, claude 18 / agy 19)

패치 수·총분산에 지배되는 현 PCA를 슬라이드 균등·trace 정규화 기저로 교체하고, `R_i = D^{-1/2} S_i D^{-1/2}`의 행렬 로그 상삼각을 특징으로 쓴다. SHJ가 백색화로 버리는 상관 구조를 읽는다는 점에서 SHJ와 구조적 상보성이 있다. 절차 설계(랭크 효율 임계 2.72/6 = 45.3%, 사후 구제 금지 사전 선언)는 이 라운드에서 가장 엄격한 축이다.
**고쳐야 통과 가능한 것**: (a) 게이트 ①이 CV에서 깨질 개연성이 높다 — `extract_bag_descriptor`가 이미 각 bag을 자기 평균으로 중심화한 뒤 공분산을 뽑고 CV가 그 off-diagonal 32,640D를 읽으므로 **CV도 이미 슬라이드 단위 위치 불변인데 위치 계열에 0.44~0.69로 묶였다.** "위치 불변성"은 이 프로젝트에서 직교성의 보증이 된 적이 없다. B_SB와 현 PCA는 동일한 per-slide 공분산 집합의 가중만 다른 평균의 상위 고유벡터이므로 상위 32차원 부분공간이 거의 겹친다. (b) `torch.autocast(enabled=False)` + float32(고유분해 float64) 강제를 명시할 것 — `D^{-1/2}`와 matrix log는 §222가 확인한 bf16 취약 경로 그 자체다. (c) ε를 임의 상수가 아니라 `tr(S_i)/32` 대비 상대값으로 사전 선언할 것. (d) 게이트 ① 측정 시 CV와의 상관을 과제별·fold별로 보고하고 7과제 중 하나라도 |r| > 0.6이면 성능 조회 없이 즉시 종료할 것.
**비용**: 저자 3.6 → claude 재산출 **1.9~2.6 GPU-h**(1회 0.95~1.25). 저자 추정이 1.4~1.9배 보수적인, 이 라운드에서 드문 자기 과대 신고다.

### 3.3 sw_dgs — 자기백색화 확산그래프 스펙트럼 (평균 17.5, claude 17 / agy 18)

**본 라운드 20건 중** 모멘트·공분산이 아닌 대상(kNN 그래프의 확산 스펙트럼)을 읽는 첫 제안이며(프로젝트 역사 전체에서 최초인지는 확인하지 않았다), 불변성으로 직교성을 강제하는 §218/§219 계보를 따른다.
**고쳐야 통과 가능한 것**: (a) 스크린 집합에 **SHJ와 SH가 빠져 있다** — 최대 상관 위험원이 CV/BM/QA/DS가 아니라 SHJ인데 rationale에서만 언급하고 스크린에서 제외했으며, §218에서 정식 채택된 SH(`m_sh`)도 집합에 없다. (b) coreset이 측정 목적과 모순된다 — farthest-point sampling은 정의상 경계·외곽점을 우선 선택해 밀도를 균질화하는데, 재려는 것이 밀도 구조(모드 수·연결성)다. 밀도 보존형 샘플링으로 사전 교체해야 한다(사후 변경은 §214 양식). (c) shrinkage 강도, heat time t 집합, "유효 연결성 지표" 정의, FPS 시작점·동점 규칙이 전부 미명세다. (d) mutual-kNN k=8에서 그래프가 분해되면 32개 최소 고유값이 전부 ~0으로 포화하는 퇴화 케이스 대비가 없다.
**비용**: 저자 7.0 → claude 재산출 **2회 실행 기준 낙관 5 / 중앙 9~12 / 비관 23~25 GPU-h**. 가장 낙관적인 구현에서도 최대 태스크(KRAS 312 슬라이드)가 약 18분/태스크라는 것이 채점자 추정이며, 이 값이 맞다면 **CT가 배제된 사유(태스크당 8~12분; §214-V 실측 KEAP1 6m41s, KRAS 12m18s)보다 무겁다.** 18분/태스크는 저장소 실측 근거가 없는 **채점자 추정치**로 `미검증`이고, KRAS 312 슬라이드만 저장 예측 파일에서 확인된다. 원안 전량 실행은 비권장이며, k = {16} 단일값·coreset 256점으로 축소해 ARID1A + KRAS 2개 과제(100 fold)만 선행 스크린하는 경로(0.3~0.6 GPU-h)로만 진행 가치가 있다.

### 3.4 P3-RFF — Random Fourier Feature 특성함수 임베딩 (평균 16.0, claude 16 / codex 16)

백색화로 1·2차 모멘트와 고유값 스펙트럼이 전부 상수가 되므로 위치 계열과의 직교성은 구조적 불변성으로 보장된다. 이 점은 §217 RM보다 논거의 급이 높다.
**고쳐야 통과 가능한 것**: (a) 실제 구속 조건은 SHJ인데 논거가 "128D vs 8D이므로 낮을 것"뿐이다 — 마진은 Ridge를 통과한 스칼라이므로 차원 수는 상관을 낮추는 근거가 아니다. (b) **ZCA(대칭) 백색화 강제가 명세에 없다** — `cos(ωᵀx^w)`는 고정 ω에 대해 회전 불변이 아닌데 백색화 행렬은 직교 변환까지만 정의되므로, 현행 eigh 고유벡터 백색화에서는 기술자가 분포의 함수가 아니게 되어 슬라이드 간 비교 가능성이 무너진다. SHJ가 반경 노름을 취한 이유가 정확히 이것이다. (c) σ 확정 절차를 라벨·성능 미참조로 사전 등록할 것. mechanism은 median heuristic, risk는 전역 상수 σ로 명세가 자기모순이다. (d) d = 32에서 `E[cos] ≈ exp(−‖ω‖²/2)`, `‖ω‖² ≈ 32σ²`이므로 신호가 살아있는 σ 대역이 좁고, N ≈ 4096의 경험적 CF 표본오차 1/√N ≈ 0.016과 겹치면 저자 스스로 건 falsifier 3(σ 민감도)이 발동할 개연성이 있다.
**비용**: 저자 2.0 → claude 재산출 **2.5~3.5 GPU-h**(누락 항목은 백색화 fp32 재검증 1회분).

### 3.5 LSAK · QJF — 커널 수준 통합 (평균 17.5 / 17.5)

두 건 모두 마진 투표 대신 커널 차원에서 중복을 제거하거나 상호작용을 곱해 단일 Ridge로 통합한다.

**먼저 §199 Closed Axis(Non-linear KRR) 저촉 여부를 판정한다.** 다른 제안에는 Closed Axis 저촉을 fatal 사유로 적용했으므로, 커널 계열인 이 두 건에도 같은 기준을 적용해야 한다(초안은 §199를 과적합 전례로만 인용하고 저촉 판정을 빠뜨렸다 — 기준의 비대칭 적용이다). §199가 닫은 것은 "슬라이드 수준에서 이미 32차원으로 압축된 디스크립터에 RBF·Cosine 등 범용 비선형 커널을 씌워 선형 Ridge readout을 대체하는" 축이며, 기각 기전은 `N_ctx ≈ 40`을 사실상 무한차원 RKHS로 보내 Gram 행렬이 대각 지배가 되고 40개 표본의 국소 잡음을 암기하는 것이다(§199 §2-1, "Readout 단은 선형 Ridge 체제를 유지해야 함").

- **QJF — §199 비저촉으로 판정한다.** QJF가 쓰는 커널은 `K_b = X_b X_bᵀ`의 trace 정규화 **선형 그람 행렬**뿐이고, 잔차화 `S_L = K_L(K_L+I)⁻¹`도 선형 사영이다. 최종 `K_QJF = (K_L + K_C⊥ + K_BD)/3`에 λ=1 Dual Ridge를 푸는 것은 스케일 조정된 특징을 이어붙인 **선형 Ridge와 표현력이 동등**하며 비선형 사상이 없다. 따라서 §199가 닫은 축에 해당하지 않는다. QJF의 재설계 요구는 아래의 별개 결함에서 나온다.
- **LSAK — `미해결`.** `K_int = K̃_L ⊙ K̃_S`는 두 중심화 선형 커널의 아다마르 곱이고, 이는 위치 특징과 형상 특징의 **텐서곱 특징공간을 여는 2차 다항 커널**이다. 즉 형식상 비선형 readout이며 §199가 닫은 축과 인접한다. 위험 방향도 같다 — 아다마르 곱은 성분 커널보다 비대각 원소를 작게 만들어 Gram을 대각 쪽으로 밀고, 텐서곱 차원(위치 BM 32 + QA 128 + DS 32 = 192D × 형상 BD 1 + SHJ 8 = 9D ≈ 1,728)이 `N_ctx ≈ 40`을 크게 웃돈다. 반면 §199의 실패 기전이 **유한차원 다항 커널에서도 재현되는지는 저장소에 실측이 없고**, 저자의 반론(§199는 단일 디스크립터에 범용 커널을 적용한 축이고 LSAK는 사전 정의된 두 불변 구조군의 중심화 ANOVA 상호작용 성분만 쓴다)도 형식상 성립한다. **판정 불확실 → `미해결`로 표시하고 규범 판정을 Phase 0에 배치한다.** 이 판정 전에는 LSAK에 GPU를 집행하지 않는다.

- **LSAK**(claude 17 / agy 18, 저자 3.6 → claude 재산출 **2.0~2.8 GPU-h**): 게이트와 승격 구성이 불일치한다 — 게이트 ①에서 6-branch 랭크 효율을 재놓고 승격에서는 LSAK 단독 Ridge를 기준선과 비교하므로 심사가 공전한다. 승격 구성을 "5-branch + LSAK 6번째 브랜치"로 정정하고, `N_ctx ≈ 40`에서 텐서곱 특징공간이 §199와 같은 과적합에 빠지지 않는다는 사전 논거를 게이트 ① 이전에 서면 등록해야 한다.
- **QJF**(claude 15 / agy 20, 저자 3.0 → claude 재산출 **3.0~3.5 GPU-h**): 결함이 더 깊다. BD는 Ridge 브랜치가 아니라 스칼라 + `ordered_typicality` 판독이므로 `K_BD`가 정의되지 않으며, 커널로 접는 순간 BD — §217 시점의 유일한 직교 축이자 형상 계열 3개(BD·SH·SHJ) 중 유일하게 기본 앙상블에 승격된 브랜치 — 가 원형 그대로 보존되지 않는다. 또 `N_ctx ≈ 40`에서 표준화 스칼라의 `K_BD`는 랭크 1이라 tr = 40이 단일 고유값에 몰리고, λ=1 Ridge의 수축계수 `s/(s+1)`로 BD 방향 0.93 대 나머지 0.25가 되어 **"세 구조군 동일 질량"의 정반대로 BD 지배 판별함수로 붕괴**한다. 게다가 게이트 ① 적용 자체가 범주 오류다 — 게이트 ①은 브랜치 추가 시 중복 계수를 막는 장치인데 QJF는 기준선 완전 대체를 규정하므로, 기존 신호의 더 나은 요약이 그 신호들과 비상관이어야 한다는 요구는 자기모순이다. 재설계 없이는 승인 불가다.

### 3.6 승격 가능성이 낮은 잔여 건 (착수 비권장)

| id | 평균 | 착수 비권장 사유 (요약) | 재산출 비용 |
|---|---|---|---|
| branch_token_multimodality (MM) | 14.5 | MDX와 동일 축의 덜 완결된 버전. 대역폭·격자·피크 임계 미고정, '골 깊이'와 밀도 히스토그램 엔트로피가 축별 log-variance 변형이라 §218 BS 및 BD와 겹칠 소지 | 저자 1.5 → **2.5~5** |
| P1-PH (Persistent Homology) | 14.5 | 32D 백색화 구름에서 거리 집중으로 Rips 필트레이션이 근사적으로 반경 분포의 함수가 되어 SHJ로 붕괴할 경로가 미대응. `sqrt(N)` 서브샘플 64~150점의 H₁은 전량 단수명 잡음이라 **잡음이 \|r\| ≈ 0으로 게이트 ①을 위장 통과**한다. 250D 특징은 §219 반증 1과 정면 충돌 | 저자 8 → **10~20**(꼬리 30+) |
| P2-SW (Sliced Wasserstein) | 14.0 | 위치 불변성 주장이 거짓 — Context 전역 아핀 백색화는 슬라이드별 중심화가 아니므로 `W₁(θ)`가 `\|⟨μ_i, θ⟩\|`을 하한으로 가져 BM과 겹친다. S³¹ 위 64점은 방향 프로파일이 아니라 SW 거리 1개의 잡음 섞인 추정치. 저장소에 이미 §203 SW 브랜치와 단독 macro 0.5976 실측이 있다 | 저자 3 → **0.8~1.1**(과대 신고) |
| branch_subspace_orientation (SO) | 12.0 | 직교성 논거를 BM·BD에 대해서만 세우고 **CV를 한 번도 언급하지 않는다.** `Σ_i = V_i Λ_i V_iᵀ`의 비대각은 고유벡터와 고유값이 함께 결정하므로 SO가 뽑는 `diag(VᵀUUᵀV)`는 CV가 이미 보고 있는 행렬의 저차원 요약이다. §217 RM과 동형 | 저자 1 → **1.8~2.5** |
| arch_unsupervised_manifold_selector (UMLA) | 11.5 | 승격 산술이 자기모순 — 라우팅되지 않은 과제는 기준선과 동일해 sign agreement 상한이 라우팅 과제 수로 제한되는데, §225는 KEAP1·KRAS·Prog가 강도 증가에 단조 하락함을 실측했다. 인용된 +2.97%p는 **§225의 DS 단독·과제 수준 Δ이며 앙상블 macro 기여로 환산된 값이 아니다**(라우팅의 macro 기여는 미측정, `미검증`). §223이 정정한 것도 앙상블 희석이 아니라 **§213의 비교 열 오지정**이다 — §213의 'DS Standalone Full' 열이 실제로는 §212의 v120 6-branch Trimmed Mean 값이었고, 동일 실행 통제 비교에서 ARID1A DS 단독 Δ는 0.6193 → 0.6236 = **+0.43%p**(과제 단독값이므로 sign agreement 정의 대상 아님)다. 따라서 초안이 쓴 '약 1/18 희석'과 그로부터 파생한 'macro 환산 +0.03%p'는 저장소에 근거가 없어 철회한다. S는 BM이 읽는 부분공간 상위 3열의 절편이라 BM context 적합도의 정보적 부분집합이다 | 저자 0.5 → **2.5~3.5** |

---

## 4. 절차 위반 기각

### 4.1 양쪽 벤더가 fatal로 합의한 건

**P2-SELECTOR-CEILING — 기각 (codex fatal / agy fatal, 점수 16 / 16).**
제안은 "§221·§225가 막힌 이유는 선택 신호 부재가 아니라, 완벽하고 비용 0인 선택기를 가정해도 macro 상한이 1%p 미만이기 때문"이라며 `mean_t max_j T[t,j]`를 연구 방향 종료의 근거로 사용했다. `agent_handoff.md` 불변식 5의 **Oracle 금지(§224)** 조항은 브랜치 간 최댓값을 "판정 기준·상한·미회수 성능"으로 쓰지 말라고 하며, "참고 수치로 표시할 수는 있으나 **어떤 채택·승격·연구 방향 판정의 근거로도 쓰지 않는다**"고 명시한다. P2는 정확히 그 금지된 용도다. 두 벤더가 독립적으로 동일 조항을 지목했다. **절차 위반으로 기각한다.**

> **진단용 각주 (판정 근거 아님).** codex가 저장 마진에서 **오프라인 재집계**로 독립 재계산한 값을 진단으로만 남긴다. 재집계 대상은 `v121_ds_sweep` 실행이 저장한 여섯 DS 강도 arm(`m_ds_full, m_ds_f070, m_ds_f050, m_ds_f030, m_ds_f010, m_ds_f000`)이며, 신규 파이프라인 실행이 아니다.
> - DS arm 5-branch 기준: macro **0.617066** (비교 원점이므로 sign agreement 정의 대상 아님)
> - 과제별 최상 arm 재집계: macro **0.619802** (sign agreement 정의상 기준선 대비 엄격 상승 6/7)
> - Δ = **+0.002736** (+0.27%p, sign agreement 6/7 — 위 줄과 동일 정의)
>
> **이 수치의 한정 (반드시 함께 읽을 것).** 이것은 "현재 존재하는 여섯 DS 강도 중 과제별로 하나를 고르는 선택기"라는 **유한 arm 집합에 한정된 진단**이다. 상한이 아니며, 연속 강도·arm 혼합·슬라이드별 선택·신규 특징을 포괄하지 않는다. §224가 명시한 대로 평가셋에서 고른 유한 arm의 최댓값은 승자의 저주로 상향 편향되고 애초에 상한도 아니다(Prog에서 앙상블 0.7892 > 최상 단독 0.7779). 따라서 이 값은 UMLA류의 **DS 강도 라우팅**에 1%p 상금이 없다는 좁은 결론까지만 지지하며, FC·PSW·TGW나 신규 형상 브랜치의 가치를 판정하지 않는다. 본 문서의 어떤 채택·기각 판정도 이 수치를 근거로 삼지 않았다.

**P1-RESIDUAL-GATE — 기각 (codex fatal / agy fatal, 점수 24 / 15).**
게이트 ①·②가 앙상블 기여도를 예측하지 못하며 실제 예측 통계량은 기존 5개 마진 스팬에 대한 잔차 마진의 단독 AUROC(resAUC)뿐이라는 주장이다. 두 벤더 모두 `agent_handoff.md` 불변식 4의 **Closed Axes — DD branch revival (§147, §195)**을 지목했다. 제안이 이 게이트로 아카이브를 재심해 DD를 현행 구성에서 다시 승격하는 것을 핵심 산출물로 삼기 때문이다.

> **기록해야 할 긴장.** codex는 이 제안에 **24/30을 부여했다 — 40개 채점값(본 라운드 20건 × 2회 채점) 중 2위**이며(단일 채점 최고점은 agy가 P3-LIMIT-CURVE에 부여한 25점이다), "잔차 정보량 분석과 +1.29%p·5/7 예비 재집계는 강한 증거지만, 사용자 승인 없이 명시적 종료 축인 DD를 부활시키므로 점수와 무관하게 탈락"이라고 적었다. agy는 같은 제안에 15/30을 주며 잔차 게이트가 라벨을 사용해 평가 기준의 일관성을 해친다는 별도 사유를 추가했다. 즉 **내용 평가는 9점 갈렸고 절차 판정만 일치했다.** 예비 재집계값 +1.29%p / sign agreement 5/7은 본 문서에서 검증하지 않았으며 `미검증`이다. 이 축을 다시 열려면 절차상 필요한 것은 재설계가 아니라 **사용자의 Closed Axis 해제 결정**이며, 그 전까지 기각 상태를 유지한다.

### 4.2 벤더 간 fatal 판정이 엇갈린 건 — 미해결

아래 4건은 한 벤더가 fatal, 다른 벤더가 정상 점수를 부여했다. **어느 한쪽을 임의로 채택하지 않는다.** 양쪽 논거를 병기하고 `미해결`로 표시한다. 해소에는 사용자 또는 프로젝트 리드의 규범 판정이 필요하다.

**P3-LIMIT-CURVE (평균 22.0 — codex 19 [FATAL] / agy 25) — 미해결.**
기저 차원 K ∈ {64,128,256,512}, ridge λ ∈ {0.01…100}, 컨텍스트 분율 {0.25,0.5,1.0}, 중심화 모드 2종의 학습 곡선을 그려 0.6171이 **정보 한계인지 추정 한계인지** 판별한다.
- **codex(fatal)**: Primary 7에서 여러 arm의 성능 곡선을 본 뒤 단조 끝점을 채택 후보로 정하는 절차는 불변식 3의 라벨 무관 후보 선택 원칙을 위반하고 §214식 평가셋 하이퍼파라미터 선택을 재현한다. 진단 가치는 높으나 promotion 계획 때문에 현 프로토콜에서 실행 불가.
- **agy(25점, 본 라운드 40개 채점값 중 단독 최고점 — 차순위는 24점 1건(codex → P1-RESIDUAL-GATE), 그 다음이 23점 4건: agy의 FC·AKS·MDX, codex의 AKS)**: 브랜치 설계라는 국소 시야에서 벗어나 파이프라인 근본 상수를 타격해 교착의 성격을 분할 진단하는 우수한 제안. 단 단일 브랜치 추가로 +1%p 가능성은 낮다.
- **쟁점**: 진단(학습 곡선 관측)과 선택(곡선 끝점 채택)이 분리 가능한가. 분리 가능하다면 promotion_plan만 삭제하고 진단 전용으로 재제출하는 경로가 열리며, 분리 불가능하다면(곡선을 본 것 자체가 이후 모든 선택을 오염시킨다면) codex 판정이 맞다. **미해결.** 비용은 저자 8 GPU-h이며 재산출 부재다.

**FC — 초점 대비 브랜치 (평균 20.5 — codex 18 [FATAL] / agy 23) — 미해결.**
여러 살리언스 초점 강도에서 계산한 슬라이드 평균의 **차분**을 특징 벡터로 쌓아 강도 가중을 context 라벨의 폐형해로 결정한다.
- **codex(fatal)**: 핵심 계산이 여러 salience top-k 강도의 재가중 평균 사다리이므로 `current_status.md`의 종료 축 '평균의 사영·재가중 브랜치'와 `agent_handoff.md` §2.2-3 '평균의 새로운 사영·재가중은 시도하지 않는다'를 직접 재포장한다. 평행이동 불변 차분이라는 개선은 명확하나 Closed Axis 판정을 뒤집지 못한다.
- **agy(23점)**: 차분을 형상 특징으로 쓰는 발상은 타당하며 fatal 아님. 다만 DS 등 기존 위치 브랜치와의 잔여 상관으로 게이트 ①에서 기각될 위험이 크다.
- **쟁점**: "평균들의 차분"이 평균의 재가중인가 아닌가. 차분은 슬라이드 수준 평행이동·척도변환에 불변이므로 저자는 형상 계열이라 주장하고, codex는 구성 요소가 전부 재가중 평균이라는 점을 본다. 주목할 점은 **양쪽이 서로 다른 이유로 같은 결과(진행 불가 또는 게이트 ① 탈락)를 예측한다**는 것이다. **미해결.** 저자 3 GPU-h, 재산출 부재.

**PSW — 브랜치 예측분산에 의한 슬라이드 단위 정밀도 가중 (평균 18.0 — codex 18 / agy 18 [FATAL]) — 미해결.**
선택 단위를 과제(n=7)에서 질의 슬라이드(Primary 7 전체 **17,723건** — 저장 예측 파일 재집계, 과제별 2,362 / 2,977 / 2,862 / 2,902 / 3,061 / 2,459 / 1,100)로 내리고, 커널 릿지 사후 예측분산 `σ²_b(x) = k(x,x) − k_xᵀ(K+λI)⁻¹k_x`로 브랜치 확률을 정밀도 가중한다. λ=1.0이 이미 고정이므로 새 하이퍼파라미터가 0개다.
- **agy(fatal)**: Trimmed Mean을 버리고 새 결합 함수를 정의하므로 명시적 종료 축 '집계 함수 변형 탐색(§214-V)'의 재포장이다.
- **codex(18점, fatal 아님)**: 확률만 입력한 기존 집계 탐색과는 구별된다. 다만 예측분산은 정확도가 아니라 특징공간 외삽만 측정하고 BD에는 임의의 대체식을 쓰므로 +1%p·5/7을 예측할 구조적 근거가 없다.
- **쟁점**: §214-V가 닫은 것이 "확률 벡터에 대한 집계 함수 탐색"인지 "브랜치 결합 규칙 일반"인지. 전자라면 슬라이드별 외부 정보(예측분산)를 새로 들여오는 PSW는 축 밖이고, 후자라면 재포장이다. **미해결.** 저자 2 GPU-h, 재산출 부재.

**TGW — Context 신호 기하로 형상 브랜치 가중 결정 (평균 17.5 — codex 18 / agy 17 [FATAL]) — 미해결.**
Context의 클래스 차이가 위치에 있는지 형상에 있는지를 동일 차원·동일 축소량의 두 마할라노비스 이차형식 L, S로 계량하고, 비율 `w = S/(S+L)`로 형상 계열 가중을 정한다.
- **agy(fatal)**: 앙상블 결합 수식을 변형하므로 §214-V 재포장이다.
- **codex(18점, fatal 아님)**: 동일 차원·표본 수·λ만으로 위치와 log-분산 이차형식의 유한표본 편향이 비율에서 상쇄되지는 않으며, §221의 Context→Query 전이 실패를 피했다는 근거도 부족하다.
- **쟁점**: PSW와 동일한 규범 쟁점(집계 축의 경계)에 더해, codex는 편향 상쇄 주장 자체의 수학적 성립 여부를 별도로 의심한다. 두 쟁점은 독립이며 어느 하나만 해소해도 통과하지 못한다. **미해결.** 저자 2 GPU-h, 재산출 부재.

> PSW·TGW의 fatal 여부는 **§214-V Closed Axis의 경계 정의**라는 단일 쟁점으로 환원된다. 이 경계를 `agent_handoff.md`에 명문화하지 않으면 향후 동종 제안마다 같은 불일치가 반복된다. 경계 정의는 GPU를 쓰지 않는 문서 작업이므로 실행 순서 Phase 0에 배치한다.

---

## 5. 세 벤더가 공통 지적한 구조적 공백

### (a) 직교성과 Context→Query 라벨 정보 전이는 별개인데, 대부분의 제안이 전자를 후자로 확대 해석한다

codex의 biggest_gap 원문: "대부분 '통계량이 수학적으로 서로의 함수가 아니다'를 'Dual Ridge가 만든 과제별 마진의 max|r| ≤ 0.6'으로 잘못 확장한다. 더 근본적으로는 새 기술자가 라벨 정보를 담고 Context→Query 방향을 보존한다는 근거가 없다."

이 공백은 **이미 두 번 실측되었다.** §218은 랭크 효율을 45% → 50%로 올려도 Primary 7 macro가 개선되지 않았고 Oracle 상한도 0.6565에서 움직이지 않음을 확인했다 — 직교성은 성능의 예측자가 아니라 필터다. §221은 편향을 제거한 SHJ로도 fold 수준 ρ = −0.091로, **Context 내부에서 관측된 신호가 Query 성능으로 전이되지 않음**을 확인했다(§212의 ρ = −0.27에 이어 두 번째).

제안 20건 중 게이트 ①과 승격 사이의 이 간극을 논거로 다룬 것은 없다. 세 갈래로 정리하면 다음과 같다.
1. **특징 수준 독립** (`f`가 `g`의 함수가 아님) — AKS만 구조적으로 증명했다.
2. **마진 수준 비상관** (`|r(m_f, m_g)| ≤ 0.6`) — 게이트 ①이 요구하는 것. 1에서 2는 따라 나오지 않는다. §217 RM은 1을 가졌으나 2에서 |r| = 0.690으로 탈락했다.
3. **Query 전이** (Context에서 적합한 판별 방향이 Query에서도 유효) — 어느 게이트도 검사하지 않으며, §212·§221이 두 번 실패를 관측한 지점이다.

**권고**: 라벨 무관 스크린에 3번 항목의 대리 진단을 추가한다. 구체적으로 게이트 ① 실행 시 저장하는 마진에서 **Context 재적합 마진과 Query 마진의 fold 수준 상관**을 함께 산출한다(저장 마진 오프라인 재집계, GPU 0h). 이 값이 신규 후보에서 기존 브랜치보다 낮으면 게이트 ②를 조회하기 전에 종료한다. 다만 이 대리 진단 자체가 §212·§221에서 실패한 계열이라는 점은 명시해 둔다 — 진단의 진단이 필요할 수 있다.

### (b) 20건 전부가 브랜치 기하 조작에 몰려 UNI2 기저 임베딩 자체의 정보 누락 가능성을 건드리지 않는다

agy의 biggest_gap 원문: "7개 과제의 라벨 노이즈 한계나 UNI2 기저 임베딩 자체의 근본적인 정보 누락 가능성은 거의 건드리지 않고 브랜치 차원의 기하학적 조작에만 몰두하고 있다."

20건은 전부 **1536D UNI2 임베딩을 고정 입력으로 받아들이고 그 위의 통계량만 바꾼다.** 파이프라인 상수(K = 256, λ = 1.0, 컨텍스트 분율)를 건드린 것은 P3-LIMIT-CURVE 1건뿐이고 그것은 fatal 논쟁 중이다. 패치 추출 배율, 인코더 선택, 패치 크기, 색상 정규화, 다중 배율 결합은 20건 중 0건이 다룬다.

이 공백이 중요한 이유는 저장소 실측에 있다. SMAD4는 전체 105명 풀 기준 BM 0.3906 / QA 0.3729 / DS 0.3959로 세 브랜치가 계통적으로 역전되었고(각각 38/50, 38/50, 35/50 fold에서 AUROC < 0.5), §215는 이를 "1개 신호의 실패가 3번 계수된 것"으로 규명했다. **그러나 그 1개 신호가 왜 뒤집혔는지는 규명되지 않았다.** top-32 PCA 부분공간의 1차 모멘트가 SMAD4에서 역방향인 것이 브랜치 설계 문제인지, 아니면 UNI2 임베딩이 SMAD4 관련 형태를 표현하지 않는 것인지는 브랜치 기하를 아무리 바꿔도 구분되지 않는다.

**권고**: 인코더 교체는 본 라운드의 범위를 넘고 비용도 다른 자릿수이므로 즉시 착수 대상이 아니다. 대신 **라벨 무관·GPU 0h 진단**을 먼저 둔다 — 저장된 임베딩에서 SMAD4의 두 클래스 간 슬라이드 평균 분리도를 다른 6개 과제와 비교 산출한다(저장 임베딩 오프라인 분석). 분리도가 다른 과제와 동등한데 마진만 역전이면 브랜치 설계 문제이고, 분리도 자체가 없으면 표현의 문제다. 이 판정 없이 형상 브랜치를 계속 추가하는 것은 원인이 표현 쪽에 있을 경우 전부 무효가 된다.

### (c) AUROC라는 순위 기반 지표의 분해능 한계를 우회할 평가지표적 접근이 없다

agy의 biggest_gap 원문: "연속형 평가지표가 아닌 순위 기반의 AUROC가 가지는 근본적인 해상도 한계(분해능 하한)를 극복하거나 우회할 평가지표적 접근이 부재하다."

`agent_handoff.md` 불변식 3은 분해능 하한을 **완화 불가능한 영구 제약**으로 규정한다 — fold 증대·재분할 금지, SEAL 사용 금지, Primary 7으로만 선택이라는 세 항의 필연적 귀결이다. 집계 8종·부분집합 31종 전수에서 macro 1%p 미만 변화가 예외 없이 sign agreement 3~4/7에 머물렀고 5/7 도달은 0건이다.

여기서 주의할 구분이 있다. **fold 수·분할·hold-out을 바꾸는 것은 금지되어 있으나, 동일 fold·동일 예측값 위에서 요약 통계를 바꾸는 것은 금지 목록에 없다.** 20건 중 이 경로를 시도한 것은 0건이다. 가능한 방향으로는 fold별 AUROC 대신 슬라이드 수준 순위 상관의 fold 간 일관성, 브랜치 마진의 부호 일치율 같은 순위 기반이되 분산이 다른 통계, 또는 예측 확률의 연속적 성질을 쓰는 통계(캘리브레이션 오차 등)가 있다.

**단, 이 방향에는 두 개의 진입 장벽이 있고 어느 것도 해소되지 않았다.** 첫째, 승격 요건은 `sign agreement ≥ 5/7`로 지표에 종속되어 있으므로 지표를 바꾸면 승격 요건 자체를 재정의해야 하고, 이는 결과를 본 뒤 기준을 바꾸는 §214 실패 양식에 걸릴 위험이 크다. 따라서 **새 지표의 도입은 어떤 후보의 결과도 조회하기 전에 사전 등록되어야 한다.** 둘째, 새 지표가 기존 지표보다 분해능이 높다는 주장 자체가 검증을 요구하는데, 그 검증에 쓸 수 있는 것이 Primary 7뿐이다. 이 순환은 본 문서에서 해소하지 못했다. **미해결로 기록한다.**

---

## 6. 실행 순서 제안

원칙은 하나다 — **라벨 무관 사전 점검으로 값싸게 죽일 수 있는 것을 앞에 둔다.** 게이트 ① 탈락 확률이 상당히 높다는 것이 세 벤더의 공통 판단이므로, 350-fold 전량 스윕은 라벨 무관 점검을 통과한 후보에만 집행한다.

### Phase 0 — GPU 0h (문서·오프라인 작업)

| # | 작업 | 산출물 | 비용 |
|---|---|---|---|
| 0-1 | §214-V Closed Axis의 경계를 `agent_handoff.md`에 명문화 (집계 함수 변형이 "확률 벡터 위의 집계 함수 탐색"인지 "브랜치 결합 규칙 일반"인지) | PSW·TGW의 fatal 여부 확정 | 0 GPU-h |
| 0-2 | P3-LIMIT-CURVE의 진단/선택 분리 가능성에 대한 규범 판정 (사용자 결정 필요) | 진단 전용 재제출 경로 개폐 | 0 GPU-h |
| 0-3 | 게이트 ① 스크린 집합에 **`m_shj`와 `m_sh` 포함**을 강제하도록 `branch_screen.py`가 import하는 BRANCHES(`branch_diagnostics.py:33`, 현재 `m_cv, m_bm, m_bd, m_qa, m_ds` 5개) 확장 | 스크린 유효성 확보 | 0 GPU-h |
| 0-4 | 공백 (b) 진단: 저장 임베딩에서 7과제별 클래스 간 슬라이드 평균 분리도 산출 (저장 산출물 오프라인 분석) | SMAD4 역전의 표현/설계 귀속 판정 | 0 GPU-h |
| 0-5 | §218 §4의 절사 반증을 표기 규칙으로 명문화: 균등 기대치를 브랜치 수별(5/6/7-branch = 40% / 33.3% / 28.6%)로 구분하고, 5-branch 절사 편중을 신규 직교 후보의 예측 근거로 쓰지 않는다는 조항을 `agent_handoff.md`에 추가 | 절사 논거의 오용 차단 | 0 GPU-h |
| 0-6 | **LSAK의 §199 Non-linear KRR 저촉 여부에 대한 규범 판정** (아다마르 곱 = 2차 다항 커널이 "범용 비선형 커널로 선형 Ridge readout을 대체"에 해당하는가; 사용자 결정 필요) | LSAK 심사 개시 가부 확정. QJF는 §3.5에서 비저촉으로 판정 완료 | 0 GPU-h |

0-3은 필수 선행이다. sw_dgs·P1-PH·P3-RFF 세 건이 전부 스크린 집합에서 SHJ를 누락했고, SHJ가 최대 상관 위험원이라는 지적을 세 벤더가 반복했다. Tier 1의 MDX·LID는 이미 `m_shj` 포함을 명시했고 AKS도 falsifier 1에 SHJ를 넣었으나, `branch_screen.py`가 import하는 BRANCHES가 5개뿐임을 코드에서 확인했으므로 도구 쪽 확장이 선행되지 않으면 선언이 집행되지 않는다.

**`m_sh`도 함께 포함한다.** SH는 §218에서 "구조적으로 검증된 신규 브랜치로 채택"되었고 SHJ는 §220에서 과제 특화 채택되었다 — 둘 다 **"채택하되 기본 앙상블 미승격"** 으로 상태가 동일하다. 한쪽만 스크린에 강제하는 것은 기준의 비대칭 적용이므로 스크린 집합을 **기존 5-branch + SH + SHJ = 7개**로 통일한다. SH는 max |r| = 0.418로 형상 계열 중 기존 브랜치와 가장 가까운 축이며, 형상 계열 신규 후보에 대해서는 SHJ(0.227) 못지않은 상관 위험원이다.

### Phase 1 — 라벨 무관 사전 점검 (1과제 50 fold 규모)

| # | 후보 | 판정 조건 (라벨 미사용) | 비용 |
|---|---|---|---|
| 1-1 | MDX | `b=0.25`에서 (슬라이드×8방향) 쌍의 K=1 비율 ≥ 95% → 즉시 종료 | 0.11 (저자 추정) |
| 1-2 | LID | 슬라이드 간 `ID_TwoNN` std < 0.2차원 → 즉시 종료. 동시에 **실측 처리량 확보**(비용 신고 불신 해소용, 필수) | 0.13 (저자 추정) |
| 1-3 | AKS | `H_q > 0.99`가 슬라이드 90% 이상 & 슬라이드 간 std < 0.01 → 즉시 종료. 동시에 실측 처리량 확보 | 0.10~0.14 (기준 단가 1/7 환산, 재산출 부재) |

세 건 모두 조기 사망 조건이 라벨을 쓰지 않으므로 게이트 ②(라벨 사용) 조회 이전에 배치할 수 있다. 세 건 합계 **약 0.30~0.38 GPU-h**(개별 상한 0.11 + 0.13 + 0.14의 합)(저자 추정 합 0.34, 기준 단가 0.7~1.0의 1/7 환산으로 건당 0.10~0.14)로 Tier 1 전체의 기전 전제를 검사한다. 세 값 모두 저자 자가 추정 또는 본 문서 환산이므로 `미검증`이다. 1-2의 실측 처리량 확보 없이 LID의 350-fold 스윕을 승인하지 않는다.

### Phase 2 — 게이트 ① SCREEN_ONLY 350-fold (Phase 1 생존 후보만)

`SCREEN_ONLY=1`로 앙상블에 넣지 않은 채 마진만 기록하는 신규 파이프라인 실행이다. 오염 검사(SMAD4 0.4421 / PBRM1 0.5553이 v121_baseline과 4자리 일치)를 통과해야 한다. 판정 조건은 (i) 기존 5-branch + **SH** + SHJ 전부와 max\|r\| ≤ 0.6, (ii) 6-branch eff.rank ≥ **2.72**(기준선 2.26/5 = 45.2% 비하락 → 6 × 0.452 = 2.712, §3.2가 쓴 2.72/6 = 45.3%와 동일 기준)이며, 미달 시 **§217 RM 선례대로 성능을 조회하지 않고 종료한다.**

| 후보 | 비용 | 근거 |
|---|---|---|
| AKS | 0.75~1.05 (재산출 부재, 본 문서 추정) | 기준 단가 0.7~1.0 + 슬라이드당 32×32 eigh 2회(저자 주장 오버헤드 3% 미만) |
| MDX | 0.75~1.05 (재산출 부재, 본 문서 추정) | 기준 단가 0.7~1.0 + conv1d·histogram(저자 주장 오버헤드 2% 미만). 별도로 동일 계열 MM의 재산출 **2.5~5**가 보수적 상한으로 남아 있으므로 아래 예산표는 MDX 상한에 2.5를 잡는다 |
| LID | **Phase 1-2 실측 처리량 확보 후 확정** (하한 1.0, 상한 미확정 — 예산표에서는 잠정 2.0을 가정) | 4096점 kNN, 두 채점자가 비용 신고를 불신 |

집행 순서는 비용 확실성이 높은 AKS → MDX → LID로 한다. 세 건을 동시에 태우지 않는 이유는 하나가 게이트 ①에서 탈락하면 나머지의 설계 전제(특히 SHJ와의 분리 논거)가 함께 흔들려 재설계가 필요할 수 있기 때문이다.

### Phase 3 — 게이트 ②b 판정 (GPU 0h)

Phase 2가 저장한 마진의 **오프라인 재집계**로 처리한다. §220이 규정한 게이트 ②b는 세 조건의 결합이므로 셋을 모두 요구한다 — ① 해당 과제에서 기존 최상 단독 브랜치를 능가, ② 그 과제의 50 fold 중 40개 이상에서 단독 AUROC > 0.5(이항검정 p < 0.01), ③ 기존 전 브랜치와 |r| ≤ 0.6. ①은 브랜치 간 비교를 포함하지만 §220이 채택 기준으로 명문화한 조항이며, §224가 금지한 "Oracle 값을 상한·미회수 성능·연구 방향 판정 근거로 사용"과는 구분된다. 본 문서 어디에서도 Oracle 값을 그런 용도로 쓰지 않는다.

**절사율은 판정 항목에서 제외한다.** 초안 단계에서는 6-branch 절사율을 승격 시도 여부의 사전 판정 항목으로 두었으나, §218 §4가 신규 직교 후보에서 이 기전을 실측 반증했다(BS 22.3% / SH 22.3% < 7-branch 균등 기대 28.6%). 예측력이 실측으로 부정된 지표를 판정에 쓸 수 없으므로 절사율은 **판정 항목이 아니라 기록 항목**으로 내리고, 6-branch 균등 기대 33.3%와 함께 로그에만 남긴다.

### Phase 4 — 통제 비교 승격 실행 (조건부)

Phase 3을 통과한 후보에 한해 §223 통제 비교 의무를 충족하는 **동일 실행·동일 basis·동일 fold에서 후보 포함 여부만 토글한 paired OFF/ON** 실행을 1회 집행한다. 후보당 약 **0.8~1.1 GPU-h**(기준 단가 0.7~1.0에, 전처리 공유로 2배가 아니라 약 1.1배). 결과는 macro와 sign agreement `n/7`을 같은 줄에 병기해 보고하고, 회귀 과제를 전량 명시한다. **사전 예측: 승격(+1.0%p 및 5/7 동시 충족) 도달 확률은 낮다.** 전례가 0건이고, §218이 실측한 형상 계열의 병목이 절사가 아니라 **정보량**이기 때문이다(§2.4). 현실적 최선의 결과는 `②b 과제 특화 채택 / hold-out 미검증`이다.

### Phase 5 — Tier 2 조건부 착수 (Tier 1 전부 탈락 시에만)

Tier 1 3건이 모두 Phase 1 또는 Phase 2에서 종료되면, 재설계 조건을 서면 반영한 saf_cov_filtration(SCREEN_ONLY 1.0~1.5 GPU-h) 또는 sbca_logcorr(SCREEN_ONLY 1.0~1.3 GPU-h) 중 하나를 착수한다. 두 건을 동시에 태우지 않는다. sw_dgs는 축소안(k = {16}, coreset 256점, 2과제 100 fold, 0.3~0.6 GPU-h) 선행 스크린을 통과한 경우에만 재심 대상이다.

### 총 GPU 예산

**생존 건수 가정을 표에 명시한다.** Phase 2 하한은 "Phase 1에서 1건이 종료되어 2건 진입"을, Phase 4 상한은 "3건 전량 생존"을 가정한다.

| 단계 | 하한 | 상한 | 생존 가정 및 근거 |
|---|---|---|---|
| Phase 0 | 0 | 0 | 문서·오프라인 |
| Phase 1 (라벨 무관 사전 점검 3건) | 0.30 | 0.38 | 3건 전량 수행. 저자 추정 및 기준 단가 1/7 환산, 재산출 부재 |
| Phase 2 (SCREEN_ONLY) | **1.50** | **5.55** | 하한은 **2건 생존** 가정(AKS 0.75 + MDX 0.75); **3건 전량 진입 시 하한은 2.50**(+ LID 1.00). 상한은 AKS 1.05 + MDX 2.50(MM 재산출 하한 차용) + LID 잠정 2.00이며 **LID 성분은 미확정** |
| Phase 3 (게이트 ②b) | 0 | 0 | 저장 마진 오프라인 재집계 |
| Phase 4 (통제 비교, 생존 후보당 0.8~1.1) | 0 | 3.30 | 하한은 생존 0건, 상한은 3건 전량 생존 |
| Phase 5 (Tier 2, 조건부 1건) | 0 | 1.50 | Tier 1 전부 탈락 시에만, 1건만 |
| **합계** | **약 1.8** (3건 전량 진입 시 2.8) | **약 10.73+** | 기준 단가 0.9 GPU-h(§219 실측) 환산 **2.0~12.0회분**, 단가 폭 0.7~1.0 적용 시 1.8~15.4회분 |

**가장 개연성 높은 지출 경로는 하한 쪽이다.** 세 벤더가 공통으로 게이트 ① 탈락 확률을 높게 보았고, Phase 1의 라벨 무관 점검(합계 0.30~0.38 GPU-h)에서 하나 이상이 종료될 수 있다. 상한 약 10.7 GPU-h는 세 건 모두 게이트 ①·②b를 통과해 승격 실행까지 가고 그 뒤 Tier 2까지 여는 경우이며, 그 시나리오의 개연성은 전례상 낮다. 상한이 확정되지 않은 항목은 LID의 Phase 2 하나뿐이고(예산표에서는 잠정 2.0 가정), Phase 1-2의 실측 처리량으로 확정한다. 기준 단가 자체가 0.7~1.0의 폭을 갖고 있으므로 합계도 ±약 20%의 폭을 함께 읽어야 한다.

---

## 7. 부록: 제안 20건 전체 목록

점수는 30점 만점. `—`는 자사 제안이라 자기 채점에서 제외된 칸이다. 비용은 `저자 자가 추정 → 채점자 재산출`이며 재산출이 없으면 `(재산출 부재)`로 표기한다. 채점 원본 40개 채점값의 최댓값은 **25점(agy → P3-LIMIT-CURVE)**, 차순위는 24점 1건(codex → P1-RESIDUAL-GATE), 그 다음이 23점 4건(agy의 FC·AKS·MDX, codex의 AKS)이다.

| # | id | 제목(축약) | 출처 | claude | codex | agy | 평균 | GPU-h (저자→재산출) | 한 줄 평결 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | AKS | 백색화 구름의 각도·4차 텐서 스펙트럼 | claude_shape | — | 23 | 23 | **23.0** | 2.5 → (재산출 부재) | **Tier 1.** 구면대칭족 위 항등 상수라는 구조적 분리 논거가 본 라운드 20건 중 유일. 마진 수준 \|r\|은 미검증이며, SMAD4 사전 등록 근거는 `약한 정황`으로 하향(§2.1) |
| 2 | P3-LIMIT-CURVE | 정보 한계 vs 동결 하이퍼파라미터의 학습 곡선 | claude_refute | — | 19 **[FATAL]** | 25 | 22.0 | 8 → (재산출 부재) | **미해결.** codex: §214식 평가셋 하이퍼파라미터 선택 / agy: 교착을 분할 진단하는 최우수 제안 |
| 3 | MDX | 다봉성 깊이 프로파일 (다중 대역폭 KDE) | claude_shape | — | 20 | 23 | **21.5** | 2 → (재산출 부재, MM 2.5~5를 상한 차용) | **Tier 1.** 초과첨도의 다대일 축퇴 반례는 타당하나 단봉 축퇴 시 무정보 브랜치 |
| 4 | FC | 초점 대비 — 강도를 특징 축으로 주변화 | claude_selector | — | 18 **[FATAL]** | 23 | 20.5 | 3 → (재산출 부재) | **미해결.** codex: §217 '평균의 재가중' 재포장 / agy: 발상은 타당, 게이트 ① 탈락 위험 |
| 5 | LID | 국소 이웃 스케일링 프로파일 (국소 내재차원) | claude_shape | — | 20 | 21 | **20.5** | 3 → (재산출 부재, 두 채점자 모두 신고 불신·cst2) | **Tier 1(조건부).** 본 라운드 20건 중 축이 가장 새롭다는 것은 codex 채점 원본 best_three의 평가이며 프로젝트 역사 전체에 대한 진술이 아니다. kNN 비용 미확정, 실측 처리량 확보가 착수 전제 |
| 6 | P1-RESIDUAL-GATE | 잔차 정보량 게이트와 DD 재심 | claude_refute | — | 24 **[FATAL]** | 15 **[FATAL]** | 19.5 | 2 → (재산출 부재) | **기각(양쪽 fatal).** DD 부활(§147·§195). codex는 24점(40개 채점값 중 2위, 최고는 agy가 P3-LIMIT-CURVE에 준 25점)을 주고도 절차로 탈락시킴 |
| 7 | saf_cov_filtration | 살리언스 형상 여과곡선 | codex2 | 20 | — | 18 | 19.0 | 2.4 → **1.7~3.0** | **Tier 2.** 본 라운드 20건 중 커브를 descriptor로 삼은 유일 안이나 u_j가 DS 살리언스 장, JS 거리가 BD와 동일 대상 |
| 8 | sbca_logcorr | 슬라이드 균형 형상기저 + Log-Correlation | codex2 | 18 | — | 19 | 18.5 | 3.6 → **1.9~2.6** | **Tier 2.** 절차는 최상급이나 CV도 이미 슬라이드 위치 불변이라 게이트 ①이 CV에서 깨질 개연성 |
| 9 | PSW | 예측분산 기반 슬라이드 단위 정밀도 가중 | claude_selector | — | 18 | 18 **[FATAL]** | 18.0 | 2 → (재산출 부재) | **미해결.** agy: §214-V 집계 변형 재포장 / codex: 기존 집계 탐색과 구별되나 승격 근거 부재 |
| 10 | TGW | Context 신호 기하로 형상 브랜치 가중 결정 | claude_selector | — | 18 | 17 **[FATAL]** | 17.5 | 2 → (재산출 부재) | **미해결.** agy: §214-V 재포장 / codex: 편향 상쇄 주장 자체가 성립하지 않음 |
| 11 | QJF | 몫공간 합동 커널 Ridge | codex1 | 15 | — | 20 | 17.5 | 3 → **3.0~3.5** | **재설계 필요(§199 비저촉으로 판정).** 커널이 전부 선형 그람이라 Non-linear KRR 축에 저촉되지 않으나, BD가 Ridge 브랜치가 아니라 K_BD 미정의, 랭크 1 커널이 BD 지배로 붕괴, 게이트 ① 적용이 범주 오류 |
| 12 | LSAK | 위치×형상 ANOVA 상호작용 커널 | codex1 | 17 | — | 18 | 17.5 | 3.6 → **2.0~2.8** | **재설계 필요 + §199 저촉 `미해결`.** 아다마르 곱이 2차 다항 커널이라 비선형 readout 해당 여부가 미판정(Phase 0-6). 게이트(6-branch 랭크)와 승격(단독 Ridge) 구성도 불일치해 심사가 공전 |
| 13 | sw_dgs | 자기백색화 확산그래프 스펙트럼 | codex2 | 17 | — | 18 | 17.5 | 7 → **낙관 5 / 중앙 9~12 / 비관 23~25** | **축소안만 가치.** SHJ 스크린 누락 + FPS coreset이 밀도 측정 목적과 모순. 낙관 구현에서도 CT보다 무거움 |
| 14 | P3-RFF | RFF 특성함수 임베딩 | agy2 | 16 | 16 | — | 16.0 | 2 → **2.5~3.5** | **조건부 보류.** 위치 계열 직교는 구조적 보장이나 SHJ 논거가 차원 수뿐, ZCA 백색화 미명세로 비교 가능성 붕괴 |
| 15 | P2-SELECTOR-CEILING | 완전 선택기 상한이 분해능 하한 미만 | claude_refute | — | 16 **[FATAL]** | 16 **[FATAL]** | 16.0 | 4 → (재산출 부재) | **기각(양쪽 fatal).** 불변식 5 Oracle 금지(§224) 위반. 재집계 수치는 §4.1 진단용 각주로만 보존 |
| 16 | branch_token_multimodality (MM) | 토큰 1D 투영 다중모드 | agy1 | 15 | 14 | — | 14.5 | 1.5 → **2.5~5** | **비권장.** MDX와 동일 축의 덜 완결된 버전. 대역폭·격자·피크 임계 미고정 |
| 17 | P1-PH | Persistent Homology Landscape | agy2 | 13 | 16 | — | 14.5 | 8 → **10~20** (꼬리 30+) | **비권장.** 32D 거리 집중으로 SHJ에 붕괴할 경로 미대응, H₁ 잡음이 게이트 ①을 위장 통과, 250D는 §219 반증 1과 충돌 |
| 18 | P2-SW | Sliced Wasserstein 자기거리 프로파일 | agy2 | 15 | 13 | — | 14.0 | 3 → **0.8~1.1**(저자 과대) | **비권장.** 위치 불변성 주장이 거짓(BM 하한 존재), 저장소에 §203 SW 단독 macro 0.5976 실측이 이미 존재 |
| 19 | branch_subspace_orientation (SO) | 슬라이드 내부 매니폴드 방위 | agy1 | 11 | 13 | — | 12.0 | 1 → **1.8~2.5** | **기각 권고.** 직교성 논거에서 CV를 한 번도 언급하지 않음. §217 RM과 동형 |
| 20 | arch_unsupervised_manifold_selector (UMLA) | 비지도 매니폴드-라벨 정렬 라우팅 | agy1 | 9 | 14 | — | 11.5 | 0.5 → **2.5~3.5** | **기각.** sign agreement 상한이 라우팅 과제 수로 제한되는 자기모순. 인용된 +2.97%p는 §225의 DS 단독·과제 수준 Δ이며 앙상블 macro 기여는 미측정(`미검증`) |

### 부록 주석

1. **fatal 표기 규칙**: `[FATAL]`은 해당 벤더가 절차 위반으로 판정했다는 뜻이며 점수와 독립이다. 6번 항목처럼 24점과 fatal이 공존한다.
2. **평균 계산**: 자사 채점 제외로 각 제안의 평균은 2개 값의 산술평균이다. 3개 값의 평균과 직접 비교할 수 없다.
3. **미검증 항목**: 적대적 검증 미실행(세션 한도 오류 — 다만 "2건"이라는 수는 입력 자료의 `items/`·`log_*.txt`·`out_*.json`·`merged_rank.json` 어디에도 산출물이나 실패 로그가 없어 확인되지 않는다), SEAL 10-task hold-out 전량, P1-RESIDUAL-GATE의 예비 재집계 +1.29%p / sign agreement 5/7(**제안서 자가 보고값**이며 저장 마진으로 재현하지 않았다), Tier 1 3건의 GPU 비용 재산출과 Phase 1·2 비용 전량(저자 자가 추정 또는 본 문서 환산), §2.5의 불변식 1·2·3 점검(본 문서 검토이며 실행 확인이 아니다), AKS의 "trace 제거 = SHJ 성분의 명시적 제거" 주장(근사이며 구조적 보장이 아니다), sw_dgs의 18분/태스크(채점자 추정치), LSAK의 §199 저촉 여부(`미해결`), 공백 (c)의 대체 지표 분해능 주장.
4. **sign agreement 병기 규칙**: 본 문서에서 macro 수치에 `n/7`이 병기되지 않는 경우는 정의상 sign agreement가 성립하지 않는 세 유형이다 — (i) 비교 원점인 기준선 0.6171 및 DS arm 기준 0.617066, (ii) 단독 브랜치·단독 과제 AUROC(예: §203 SW 0.5976, v121_baseline 브랜치별 표, §223의 ARID1A DS 단독 +0.43%p), (iii) 판정 근거로 쓰지 않는 Oracle 참고값 0.6565. **초안 단계에는 이 규칙에 두 곳의 위반이 있었다** — §3.6 UMLA 행의 "앙상블 실효 +0.43%p / macro 환산 +0.03%p"(현재는 §223의 과제 단독 Δ로 정정해 (ii) 유형에 편입했다)와 §4.1 각주의 Δ 줄(현재 sign agreement 6/7을 같은 줄에 병기했다). 따라서 "예외 없이 병기했다"는 초안의 자기 진술은 사실과 달랐으므로 삭제한다.
5. **비교 모집단 표기**: "최고점"·"유일"·"첫"·"가장 새롭다" 같은 순위·유일성 진술에는 모집단을 함께 적는다. 본 문서의 기본 모집단은 **본 라운드 제안 20건** 및 **40개 채점값**(20건 × 2회 채점)이며, 프로젝트 역사 전체에 대한 진술은 저장소 문서로 대조한 경우에만 그렇게 표기한다.
6. **용어 구분**: 본 문서에서 "오프라인 재집계"는 이미 저장된 마진·예측 파일로부터의 재계산이며 `logs/`·`predictions/`에 신규 산출물이 생기지 않는다. "신규 파이프라인 실행"은 350-fold 스윕을 새로 도는 것이다. §4.1의 +0.27%p와 Phase 3의 게이트 ②b 판정은 전자다. Phase 3에서 기록 항목으로 남기는 6-branch 절사율 산출도 전자다.
