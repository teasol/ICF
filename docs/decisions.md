# 결정 기록 (Decision Records) — append-only

> **이 파일은 "왜 지금 이 규칙인가"의 단일 출처(SSOT)다.**
>
> **왜 분리했는가.** 이전 체계는 정정이 원문을 덮어쓰지 않고 **주석으로 누적**됐다.
> `agent_handoff.md` §2.2 한 문단에 §215 원문 + §217 분류 + §218 반증 + §226 정정이 겹쳐
> 쌓였고, 그 결과 §226에서 **낡은 사실 2건이 브리핑 팩을 통해 7개 에이전트 전부에 전파**되는
> 사고가 났다. 이제 **본문은 현행 사실만 담고, 이력은 여기에만 남긴다.**
>
> **규칙**
> 1. **추가만 한다.** 기존 레코드를 수정·삭제하지 않는다. 결정이 바뀌면 새 레코드를 쓰고
>    옛 레코드의 `이후` 줄에 링크만 추가한다.
> 2. **결정만 담는다.** 실험 결과와 관측은 [`history/research_units_all.md`](history/research_units_all.md)에 있다.
>    여기 들어오는 것은 **규범·기준·구성의 변경**이다.
> 3. **`재검토 조건`은 필수다.** 비어 있으면 그 결정은 영구 규칙이라는 뜻이며, 명시해야 한다.
> 4. 축을 닫거나 여는 결정은 [`closed_axes.md`](closed_axes.md)에도 반영한다.

**Last updated**: `2026-09-06`

---

## D-001 · 2026-08-09 · CV-only 대전환 — 죽은 4개 분기 영구 제거

- **맥락**: §68 분기 기여도 진단에서 attention·rare instance 계열 분기가 **상수값만 출력**하며
  공분산 능선의 성능을 깎고 있음이 확인됐다.
- **결정**: 4개 분기를 영구 제거하고 CV-only로 전환한다. 11,000줄 이상의 코드를 정리했다.
- **근거**: 분기 활성 ≠ 상보 정보. v36/v37 제안이 실패한 원인이 이 죽은 분기였다.
- **재검토 조건**: 없음 (구현 결함 제거).
- **출처**: §68 · RU-25

## D-002 · 2026-08-16 · 학습 파라미터 시대 종료 — training-free 확정 *(사용자 결정)*

- **맥락**: 수백 에포크 GPU 분산 학습 계보(v18~v105)가 within-context PCA + closed-form
  ridge에 일반화·OOD 양쪽에서 밀렸다.
- **결정**: **v106을 활성 baseline으로 확정한다.** 학습 파라미터 0개, 사영은 fold context의
  within-slide PCA, head는 라벨 반대칭이 강제하는 상수.
- **근거**: 학습 비용 0으로 더 나은 일반화. seed std 0.00000의 완전 결정론.
- **재검토 조건**: 사용자의 명시적 결정. 프로젝트 정체성에 해당한다.
- **이후**: `closed_axes.md` `CA-14`로 축을 닫았다.
- **출처**: §139 · RU-38

## D-003 · 2026-08-17 · 결정론적 arm에 t·p·CI를 쓰지 않는다 *(사용자 지적)*

- **맥락**: seed std가 정확히 0인 결정론적 구성에 표본 기반 통계량을 적용하고 있었다.
- **결정**: **t-통계량·p-value·신뢰구간을 승격 판정에 사용 금지**한다. 판정은 Primary 7
  sign agreement와 독립 집단 재현으로 한다.
- **근거**: 분산이 0인 대상에 표본 분산 기반 검정을 적용하는 것은 정의상 무효다.
- **재검토 조건**: 구성에 확률적 요소가 도입되는 경우.
- **출처**: §151-1 · RU-40

## D-004 · 2026-08-22 · Trimmed Mean Voting 채택

- **맥락**: 9대 voting/ensembling 기법을 전수 비교했다.
- **결정**: 최저·최고 각 1개를 절사하고 중앙값을 평균하는 **Trimmed Mean**을 집계 규칙으로 한다.
- **근거**: 단일 브랜치의 파멸적 오작동을 차단하면서 다종 브랜치 신호를 보존한다.
- **재검토 조건**: `closed_axes.md` `CA-08` 참조 — 집계 함수 변형 탐색은 이후 닫혔다.
- **출처**: §199(voting 비교 절) · RU-58

## D-005 · 2026-09-03 · 공식 비교 기준을 CT 제외 5-branch로 변경

- **맥락**: CT 브랜치가 k-means 병목으로 350-fold 소요를 45분 → 12분만큼 늘리는 반면
  기여가 확인되지 않았다.
- **결정**: **모든 승격 비교는 `CV, BM, BD, QA, DS` 5-branch에서 수행한다.** 공식 기준선은
  `v121_baseline` Primary 7 Macro `0.6171`(50-fold)이다. v120 6-branch `0.6265`와 직접
  비교하지 않는다.
- **근거**: 브랜치 집합이 다르면 비교 불가능하다. 수치는 §214-V에서
  `predictions/*_v121_baseline_official50_bf16.pt`로부터 독립 재계산해 확인했다.
- **재검토 조건**: CT의 기여가 통제 비교로 확인되는 경우.
- **출처**: §214-V · RU-69 · [`PROJECT.md` §3.2](PROJECT.md)

## D-006 · 2026-09-03 · 집계 함수 변형 탐색 축 종료 + 보고 무결성 계약 신설

- **맥락**: §214가 `adaptive_trimmed`의 상승 3개만 적고 하락 3개(ARID1A −2.00%p,
  PBRM1 −2.22%p, Grade −0.41%p)를 누락한 것이 재현 검증에서 드러났다.
- **결정**: ① 집계 방식 8종·부분집합 31종 전수에서 5/7 도달이 0건이므로 **축을 닫는다.**
  ② **보고 무결성 계약**을 신설한다 — 회귀 전량 명시, sign agreement 병기, 측정 방식 정확
  기술, 비교 모집단 한정, 승리 수식어 금지, 미검증 항목 표기, 자기 검증 우선.
- **근거**: 모든 변화량이 분해능 하한 아래였다. 그리고 실제 보고 위반이 확인됐다.
- **재검토 조건**: 분해능 하한이 개선되는 평가 체계가 확립되는 경우.
- **출처**: §214-V · RU-69 · `closed_axes.md` `CA-08`

## D-007 · 2026-09-04 · 평가 프로토콜 확정 *(사용자 결정)*

- **맥락**: 분해능 부족을 fold 재분할이나 hold-out 조회로 해소하려는 유혹이 반복됐다.
- **결정**: ① **fold는 PathoBench 공식 분할 그대로 고정**하고 재분할·증대를 금지한다.
  ② **SEAL 10-task는 최종 hold-out 전용**이며 선택·튜닝에 어떤 형태로도 사용 금지, 현재
  유보(DEFERRED). ③ **모델 선택은 Primary 7만으로** 수행한다.
- **귀결(§216이 명시)**: 분해능 하한 ±1.0%p는 **완화 불가능한 영구 제약**이다. 따라서 후보
  선택의 근거를 성능이 아닌 **라벨 무관 구조 기준**에 두고 Primary 7은 사후 확인용으로만 쓴다.
- **재검토 조건**: 없음 — 사용자 결정으로 확정된 영구 규칙.
- **주의**: 이 결정은 RU-54(2026-08-21, Primary 7과 SEAL 역할 전환)를 **후속 정정**한 것이다.
  RU-54 시점 규칙으로 내려진 과거 판정은 당시 사실로 읽는다.
- **출처**: §216 · RU-70 · [`PROJECT.md` §3.1](PROJECT.md)

## D-008 · 2026-09-04 · 신규 브랜치 2단계 게이트 신설

- **맥락**: §217에서 `RM`을 성능 조회 없이 기각한 절차가 §214식 실패를 처음으로 차단했다.
- **결정**: 신규 브랜치는 **게이트 ① 직교성(라벨 무관, 성능 조회 이전)** → **게이트 ② 정보량**
  순으로 심사한다. 게이트 ① 탈락 시 성능을 조회하지 않고 종료한다.
- **근거**: 성능 순위로 후보를 고르는 절차는 §214에서 실패가 입증됐다.
- **재검토 조건**: 게이트 기준값(max |r| 0.6, eff.rank 비하락)의 근거가 반증되는 경우.
- **이후**: D-009(②b 신설) · D-011(Oracle 폐기로 ②b 개정) · D-013(게이트 ① 기준 집합 수정)
- **출처**: §218 · RU-71 · [`PROJECT.md` §5](PROJECT.md)

## D-009 · 2026-09-04 · 게이트 ②b 과제 특화 경로 신설 *(사용자 지시)*

- **맥락**: 게이트 ②a(전 과제 단독 AUROC > 0.5)를 기존 브랜치에 소급 적용하니 **현행 5개
  전부 불통과**였다.
- **결정**: **②b 과제 특화 경로**를 신설한다. ②b 통과는 `과제 특화 채택`이며 승격은 별개다.
- **근거 (§219 결과와 독립)**: 앙상블의 목적은 과제별 강점의 결합이므로, 전 과제 유효성은
  앙상블 구성원이 아니라 단독 모델에 대한 요건이다. ②a는 신규 후보에게만 더 엄격한
  **이중잣대**였다. 이 논거는 §219 이전에도 성립했다.
- **⚠️ 경위 보존 의무**: ②b는 **§219 결과를 본 뒤에** 신설됐다. 결과를 본 뒤 기준을 완화하는
  것은 §214의 실패 양식이므로, 이 레코드의 `근거` 줄은 삭제하지 않는다.
- **재검토 조건**: 없음. 단 경위는 영구 보존한다.
- **출처**: §220 · RU-73

## D-010 · 2026-09-04 · §213 측정 오류 정정 + 통제 비교 의무 신설

- **맥락**: §213의 "DS Standalone Full" 열이 실제로는 §212의 v120 6-branch Trimmed Mean
  값이었다. "+7.65%p 폭등"의 실제 효과는 **+0.43%p**였다.
- **결정**: **통제 비교 의무**를 보고 무결성 계약에 추가한다 — 어떤 기법의 효과를 주장하려면
  **동일 실행·동일 basis·동일 fold에서 그 기법만 토글한** 비교여야 한다. 서로 다른 실행의
  표를 나란히 놓고 차이를 효과로 해석하지 않는다.
- **재검토 조건**: 없음.
- **이후**: 같은 절이 내린 "축 종료" 판정은 D-012에서 **철회**됐다.
- **출처**: §223 · RU-76

## D-011 · 2026-09-04 · Oracle 폐기 *(사용자 지적)* — 게이트 ②b 개정

- **맥락**: "과제별 최상 단독 브랜치"를 상한·판정 기준으로 써 왔다.
- **결정**: **브랜치 간 최댓값 비교를 어떤 채택·승격·연구 방향 판정의 근거로도 쓰지 않는다.**
  게이트 ②b의 "기존 최상 단독 브랜치를 능가" 조건을 **fold 재현성(50 중 ≥40, 이항검정
  p < 0.01)** 으로 교체한다.
- **근거**: ① 평가 데이터로 브랜치를 고르므로 **승자의 저주로 상향 편향**된다(split-half
  측정 +0.0063, ARID1A에서 +0.0144). ② 애초에 상한이 아니다 — 앙상블이 최상 단독 브랜치를
  넘는 과제가 실재한다(Prog: trimmed 0.7892 > 최상 0.7779).
- **검증**: 개정 후 기존 4개 판정을 그대로 재현했다 (SHJ 48/50, SH 44/50, BS 31/50 기각
  p=0.059, RM 게이트 ① 탈락).
- **재검토 조건**: 없음.
- **출처**: §224 · RU-77

## D-012 · 2026-09-04 · §223의 Salience Anchor 축 종료 철회

- **맥락**: §225 강도 스윕(full / 74.5% / 40.5% / 15%)에서 **4개 과제 완벽한 단조 용량반응**
  (|ρ| = 1.000)이 확인됐다.
- **결정**: **§223의 축 종료 판정을 철회한다.** 기법이 무효였던 것이 아니라 §213의 설정이
  너무 약했다. 다만 **전역 이득은 없고**(macro는 full 부근 최대) 가치는 과제별이다.
  draw 안정성 선택 신호는 기각한다(ICC 0.9996~1.0000, 판별력 없음).
- **미해결**: 과제별 강도를 고를 **선택 신호가 없다.** §221 SHJ 교착과 동일 구조다.
- **재검토 조건**: 라벨 없이 과제를 판별하는 선택 신호가 확립되는 경우.
- **출처**: §225 · RU-77 · `closed_axes.md` `CA-R1` · `CA-11`

## D-013 · 2026-09-05 · 게이트 ① 기준 집합에 채택 브랜치를 포함

- **맥락**: `branch_screen.py` STEP 1의 기준 집합이 `BRANCHES`(공식 5-branch)와 형제 후보뿐이라,
  **이미 채택된 `SH`(§218)·`SHJ`(§220)와의 상관을 한 번도 재지 않았다.** 후보가 마주한 적
  없는 심사를 통과할 수 있는 상태였다.
- **결정**: `--adopted`(기본 `m_sh,m_shj`)를 추가해 STEP 1 기준 집합에 포함한다.
  `BRANCHES`(공식 비교 기준)는 불변식에 따라 변경하지 않는다. 해당 마진이 없는 태그에서는
  경고를 출력해 무단 통과를 막는다.
- **재검토 조건**: 형상 계열에 브랜치가 추가될 때마다 기본값을 갱신한다.
- **출처**: §226 · RU-78

## D-014 · 2026-09-05 · 낡은 사실 2건 정정 — 브리핑 팩 전파 사고

- **맥락**: §226 브리핑 팩이 저장소에서 낡은 사실 2건을 옮겨와 **7개 에이전트 전부에 전파**했다.
- **정정 ①**: "직교 축은 `BD` 하나뿐"은 **§217 시점 사실**이다. §218 `SH`(max |r| 0.418)와
  §220 `SHJ`(0.227) 이후 무효이며, 현 형상 계열은 **`BD`·`SH`·`SHJ` 3개**다.
- **정정 ②**: "Trimmed Mean이 독립 신호를 우선 폐기한다"는 **기존 5-branch에서 관측된 현상**이며,
  §218 §4가 신규 직교 브랜치로의 확장을 **실측 반증**했다(7-branch에서 `BS` 22.3% / `SH` 22.3%
  vs 균등 기대 28.6% — "측정 전 예상과 반대였다"). 형상 계열의 병목은 절사가 아니라 **정보량**이다.
  균등 기대치는 5/6/7-branch = 40% / 33.3% / 28.6%로 구분해 읽는다.
- **재검토 조건**: 없음.
- **⚠️ 이 레코드가 존재하는 이유**: 정정을 본문 주석으로만 남기면 같은 사고가 반복된다.
  본문은 현행 사실만 담고 시점 한정 사실은 이 파일에서 찾는다.
- **출처**: §226 · RU-78

## D-015 · 2026-09-06 · 문서 체계 전면 재구성 · 현재 목표 슬롯 비움 *(사용자 결정)*

- **맥락**: 450커밋 전수 총람(RU-01~78)이 세 가지를 정량으로 드러냈다.
  ① 연구 단위가 파편화됐다 — Era 6은 RU당 커밋 **2.6개**(Era 3은 14.1개)이고 78개 중 17개가
  단일 커밋이다. ② **2026-08-23(v120 승격) 이후 19 RU / 46커밋 / 13일간 공식 승격 0건**이다.
  ③ 총람이 기록한 정정·철회 관계 **6건이 전부 RU-54 이후에 집중**됐다.
  구조적 원인은 **탐색 공간의 기대 효과가 측정 장치의 분해능(±1.0%p) 아래**에 있다는 것이다
  (§226 채점자 전원이 `promotion_realism` 0~3점, 4점 이상 0건).
- **결정**:
  1. **문서 체계를 전면 재구성한다.** 수치 SSOT([`PROJECT.md`](PROJECT.md)), 닫힌 축
     레지스트리([`closed_axes.md`](closed_axes.md)), 결정 기록(이 파일)을 분리하고,
     `current_status.md`는 상태만 담는 짧은 문서로 되돌린다.
  2. **진행 단위를 커밋이 아니라 RU(연구 단위)로 되돌린다.** 착수 전 RU 카드에
     질문·가설·판정 기준·예산·**중단 조건**·결과별 후속 행동을 등록한다.
  3. **연구 방향(현재 목표) 슬롯은 비워 둔다.** 정비 완료 후 사용자가 설정한다.
- **근거**: 방향성 상실이 아니라 목표 함수가 측정 불가 영역에 있는 것이 원인이므로, 후보를
  더 만들기 전에 판정 체계와 기록 체계를 먼저 고친다.
- **재검토 조건**: 정비 완료 후 연구 방향 설정 시점.
- **출처**: 본 세션 · [`history/research_units_all.md`](history/research_units_all.md)

## D-016 · 2026-09-06 · 실행 환경 정정 — 로그인 노드 + Slurm 클러스터 *(사용자 지시)*

- **맥락**: `current_status.md`는 실행 환경을 "`nexgem-s1` · 8× RTX A5000 · driver 550.127.08"로
  기록하고, 그 드라이버가 `torch 2.14.0+cu130`(드라이버 ≥580 요구)과 맞지 않는다는 이유로
  **GPU 트랙 전면 차단**을 선언하고 있었다. 실측 결과 `nexgem`은 **GPU가 없는 로그인 노드**였다.
  차단 판정은 계산 노드가 아니라 로그인 노드에서 측정한 값에 근거한 것이었다.
- **결정**:
  1. **모든 계산은 Slurm `batch` 파티션에 `sbatch`로 제출한다.** 로그인 노드에서 실행하지 않는다.
  2. **노드는 충분한 최저 등급부터 쓴다** — `node1-5`(GPU 없음) > `gnode1-4`(A5000) >
     `gnode5`(A6000) > `gnode6`(H100). 같은 등급에서는 한산한 노드를 고른다.
  3. **job 제출 전 사용자 승인을 받는다.** job 로그는
     `slurm_outputs/YYYY-MM-DD/HHMM/%x-%j.{out,err}`에 남긴다.
  4. 노드 스펙·우선순위·로그 규약의 **정본은 `/home/kimds/slurm_rules.md`** 로 둔다 (저장소 밖,
     여러 프로젝트 공용).
- **근거**: `gnode6`(H100 80GB · 드라이버 595.71.05 · CUDA 13.2)에서 기존 `.venv`가 재설치 없이
  동작함을 job 131263으로 확인했다 — `cuda_available=True`, 4096² matmul 성공. 따라서 D-015 시점의
  "torch 재설치" · "드라이버 상향" 해소 경로는 불필요하다. 공용 클러스터이므로 상위 등급 노드는
  그 등급이 아니면 불가능한 작업을 위해 남긴다.
- **재검토 조건**: 클러스터 구성 변경, 또는 `.venv`의 torch 빌드가 바뀌어 드라이버 요구가 달라질 때.
- **미해결**: `gnode1`~`gnode5`의 드라이버는 미실측이다. cu130이 동작하는지 노드별로 확인해야 한다.
  (`scripts/node_env.sh`의 `NGPU`가 Slurm 할당을 무시하던 문제는 본 세션에서 해소했다.)
- **출처**: 본 세션 · Slurm job 131263
