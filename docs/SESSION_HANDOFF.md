# 세션 인계 — 2026-09-18 16:45 KST

정본은 [`current_status.md`](current_status.md)다. 이 파일은 **재개 직후 30초 안에 알아야
할 것**만 담는다.

## 먼저 할 일

**Qwen 서버 3대가 모두 죽어 있다.** GPU 5·6·7이 비어 있고(`4 MiB`), 로그가 정상 동작 중에
오류 없이 끊겼다 — 외부에서 종료된 것으로 보이며 **원인은 미확인**이다. 이것 없이는 회차를
소집할 수 없다.

```bash
cd /NHNHOME/WORKSPACE/kimds/LLM
GPU=7 PORT=8000 ./start_server_vllm.sh &
GPU=6 PORT=8001 ./start_server_vllm.sh &
GPU=5 PORT=8003 ./start_server_vllm.sh &
# 기동에 수 분 걸린다. 확인:
for p in 8000 8001 8003; do curl -s -m 3 http://127.0.0.1:$p/v1/models >/dev/null && echo "$p ok"; done
```

그다음 회차 20(A/B 대조군)이다. 19와 짝이며, 19만 돌고 20이 막혀 **대조가 미완이다.**

```bash
cd /NHNHOME/WORKSPACE/kimds/ICF
bash scripts/ops/turn_end.sh
bash scripts/ops/run_round.sh talks/council/C-20260918-20_card.json
diff <(cat talks/council/C-20260918-19/metrics.json) \
     <(cat talks/council/C-20260918-20/metrics.json)
```

## 오늘 확정된 것 (전부 실행으로 검증)

1. **여섯 실행 러너가 전부 같은 7-branch 설정으로 수렴해 있었다**(`D-053`). 설정 경로는
   폴백으로 교체됐고 `ICF_*` 경로는 아무도 읽지 않았다. **과거 arm 간 차이 보고는 전부 적합
   변동이다.** 폴백은 이제 큰 소리로 실패한다.
2. **5-branch 대 7-branch 첫 실제 비교**: macro Δ `+0.0057`. 문서의 기준선·계보값·승격
   근거가 전부 재현되지만, 과제 군집 95% CI `[-0.0114, +0.0228]`가 **0을 포함**하고 7개 중
   3개가 악화된다. **판별 불가**다.
3. **fold 적합 1회 `20.0`초.** 2 arm paired 확증은 예산의 97%, `R=4` 2×2는 7.8배.
   **4 GPU-hour 안에서 확증은 불가능하다.**
4. **교차 query 누수 없음** — `tests/test_query_independence.py`.
5. 협의체에 **Phase B(자유 대화)·Phase Q(문서 질의)** 신설. 대화록은 넘어가지 않고 `가설:`
   한 줄만 넘어간다.

## 판단이 필요한 것 (사용자)

- **예산.** 확증 프로브가 4 GPU-hour에 안 들어간다. 증액인지 설계 축소인지는 사용자 결정이다.
- 적합 분산 포함 승격 절차와 `R`이 미승인이다.

## 걸려 넘어지기 쉬운 곳

- 회차는 **반드시 `run_round.sh`로** 띄운다(블록되므로 완료 알림이 곧 회차 종료 알림이다).
  `nohup`·`&`로 분리하지 않는다.
- 턴 종료 전 **`turn_end.sh`** 를 실행한다. 유휴 노드·잡무 실패·감독자 코드 노후를 잡는다.
- **앞선 회차 보고서를 좌석 입력에 넣지 않는다.** 검증기가 거부한다(회차 11 실측).
- **실험을 잡무 큐에 넣지 않는다.** 큐는 `routine`만 받는다.
- **`pgrep -f`로 자기 프로세스를 찾지 않는다.** 이 저장소에서 셸이 세 번 죽었다.
- `test_docs_consistency.py`의 RU 번호 1건 실패는 **기존 결함**이다(`RU-91`\~`96` 공백).
  없는 기록을 지어낼 수 없어 미수정으로 둔다. 나머지는 통과해야 한다.

## 오늘의 교훈 두 개

- **오진을 두 번 했다.** 첫 번째는 "계측이 망가졌다", 두 번째는 "폴백이 원인"에서 멈춘 것.
  둘 다 **사슬을 끝까지 따라가지 않아서** 생겼다. 원인을 지목했으면 **그 원인이 실제로
  발동하는 경로**를 확인해야 한다.
- **지표가 없었으면 회차 17을 성공으로 읽었을 것이다.** 가설 18건·지지 8건이 나왔지만
  실제로는 Phase 1을 두 번 돌린 것이었다. 새 장치를 넣을 때는 **그것이 작동했는지 코드가
  재는 수치**를 함께 넣는다.

[작성자: Claude Code / 소집자 / claude-opus-5 · 2026-09-18 16:45 KST]
