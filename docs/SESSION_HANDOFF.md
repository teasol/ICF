# 세션 인계 — 2026-09-18 12:45 KST (컴팩트 직전)

> 컨텍스트가 압축되므로 지금 살아 있는 상태를 여기 남긴다. 재개할 때 이 파일과
> `docs/current_status.md`를 읽으면 된다.

## 지금 돌고 있는 것

```bash
bash scripts/ops/turn_end.sh     # 노드가 비었는지 먼저 확인
cat talks/ops/state.md           # 감독자가 유지하는 노드 상태
```

- **회차 `C-20260918-14`** — 계측 오진의 사후 분석. `run_round.sh`로 띄웠으므로 종료 시
  작업 완료 알림이 온다.
- **5-branch 앵커 검증** (GPU 4) — `evaluate_pure.py --config configs/baseline/v121_active.yaml`로
  `SMAD4`·`PBRM1` 실행 중. 결과를 문서 앵커 `0.4421`/`0.5553`과 대조해야 한다.
  로그: `/tmp/anchor5_v121.log`
- **문헌 요약 8건** 큐 대기·진행. 감독자가 병렬 발사한다.

## 방금 끝난 큰 정정 — 반드시 알고 재개할 것

**계측은 망가지지 않았다.** 어제\~오늘 오전의 "설정 표면 전체가 무효 / 공식 7-branch 재현
불가 / 산출물이 잘못된 태그" 결론은 전부 철회됐다. 상세는
`talks/reports/2026-09-18_ru90_baseline_regen.md` §9, 커밋 `fe1bc02`.

**그런데 이 정정이 아직 반영되지 않은 문서가 셋 있다.** 재개 시 첫 과제다.

| 문서 | 무엇이 틀렸나 |
|---|---|
| `docs/PROJECT.md` §3.2 | 공식 기준선에 붙인 `unverified-hold` 표시의 근거가 사라졌다 |
| `docs/PROJECT.md` §3.4 | "오염 검사 수행 불가"의 근거가 사라졌다. 앵커 검증 결과를 보고 다시 쓴다 |
| `docs/history/archive.md` `D-050` | 계측 결함 5개 항목 중 2\~4번이 철회 대상이다 |

`D-050`은 사용자 결정이므로 **덮어쓰지 말고 정정 레코드를 새로 추가**한다.

## 여전히 사실인 것 (철회되지 않음)

- `src/`에서 환경변수를 읽는 파일이 0개다. `ICF_*` export는 전부 무효이고
  `run_ru90_shape_triple.sh`의 머리말은 낡았다.
- 저장된 예측에 branch list·가중치·코드 해시가 없다. provenance 부재는 그대로다.
- `RU-97` GF 단독 탐색은 음성으로 종료(`D-052`). 탐색 공간 전체에서 구분되는 축이 없었다.
- 적합 분산이 승격 기준의 최대 2.8배다. 승격 절차는 `D-051`로 승인됐다.

## 미결 (사용자 결정 대기 없음 — 소집자가 판단해 진행)

사용자가 **결정 최소화**를 지시했다(`D-052` 참조). 앞선 결정에서 읽히는 기준으로 판단하고,
기록에는 **"소집자 판단(근거: D-0xx)"** 으로 남겨 사용자 결정과 구분한다.

- 13회차가 프로브를 2×2 요인설계(baseline / PCA-only / subsample-only / combined)로
  재설계했다. 사전 등록 후 탐색 기록으로만 진행한다(`D-052`).
- 14회차 산출물 중 "어떤 검사를 먼저 했어야 하는가"의 일반 규칙은 회귀 스위트에 넣을 후보다.

## 운영 규칙 (잊으면 노드가 멈춘다)

1. **회차는 `bash scripts/ops/run_round.sh <card>`를 백그라운드 작업으로** 띄운다.
   `nohup`·`&`로 분리하면 종료 알림이 끊긴다.
2. **턴을 끝내기 전 `bash scripts/ops/turn_end.sh`**. 오늘 두 번 노드를 놀렸다.
3. **앞선 회차의 `report.md`·`blackboard.json`을 좌석 입력에 넣지 않는다.** 카드 검증이
   거부한다. 쟁점은 소집자가 자기 말로 다시 쓴다.
4. **실험은 큐에 넣지 않는다.** 잡무는 "아무도 읽지 않아도 되는 것"만이다.
5. `pgrep -f`로 자기 프로세스를 찾지 않는다 — 검사 스크립트 자신과 매칭돼 셸이 죽는다.
   오늘 세 번 겪었다. pid 파일을 쓴다.

[작성자: Claude Code / 소집자 / claude-opus-5 (effort: 미확인) · 2026-09-18 12:45 KST]
